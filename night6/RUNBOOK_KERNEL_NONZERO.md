# Prepared response: if E3 reports a support-legal nonzero kernel

Written BEFORE the answer is known, so excitement never improvises.

1. No announcement of any kind. The kernel element is a measurement.
2. Cross-prime check: identical kernel dimension and matching bases at
   999983 and 1000003, on corresponding face solutions. Disagreement =
   instrument problem, stop.
3. Integration test: feed (p,s) = kernel direction into the FULL five
   identities (E0..E4) with unknowns f, g, r (supports per handoff
   section 2). Solve mod both primes (restored cascade or direct linear
   algebra — at fixed (q,t,p,s) the remaining system is linear in f,g,r).
   No full solution => the direction does not integrate; record, done.
4. If a full (u,z) solution exists mod both primes: lift via the tested
   lift kit (smoothness minor required; singular points stay
   CANDIDATE-UNVERIFIED per the external audit protocol).
5. If an exact char-0 (u,z) solution exists: REVERSE THE REDUCTION.
   Prop 4.3 is ONE-WAY (handoff section 1). The pair must be hauled to
   original (x,y) coordinates, denominators cleared, supports checked
   against the actual polygons, and [P,Q] there is x^2 — NOT a
   counterexample by itself. What it would be: the first exact char-0
   point of subcase 2, i.e. the (72,108) closure FAILS to lift, the
   exceptional case reopens, and the object goes to GGHV-machinery
   analysis — not to any announcement.
6. Detectors run last, on the original-coordinate object only: tail
   (night4/tail.py) and, when built, the tear evaluator. Both, plus the
   full external (F) checklist, before the word "candidate" is typed.
