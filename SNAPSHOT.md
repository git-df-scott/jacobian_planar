# Backup snapshot (git proxy down since container restart)

Local branch claude/opus-5-counterexample-plan-sep6yk holds 4 unpushed commits
(through a60c371). Queued content summary:

1. d=7 DECIDED EMPTY on the sound Z/N split (chart Z instant; chart N [-1] in
   939s / 12.7 GB). B=16 ladder now d<=7 EMPTY (d<=6 char-0 proof standard,
   d=7 sound-split mod p=1000003).
2. RESONANCE LAW (verified d=3..14): descent linear coefficient
   L_k = -(16d-12-4k)a_{2d} - 3; singular levels on a top root exist iff
   sqrt(12d) rational, i.e. d = 3k^2. d=3 root -1/4 resonates at k=6 (GGV's
   known degenerate family -- seeded cascade control recovers it EXACTLY:
   a3=-mu3/2, all else 0, mu0=mu1=mu2=0, mu3 free). d=12 root -1/12 resonates
   at k=36 -- first untested resonance; seeded rational cascade running.
3. Wave 6 virgin sweep (uncovered cases 125<=max<=150, no triage filters):
   max=126 both shapes EMPTY (nondegeneracy enforced); max=135 shapes TIMEOUT
   at 240s (re-queue long); sweep continuing through 40 shapes / 8 cases.
   Compile map: 8 of 12 uncovered cases compile; 3 blocked by non-integer
   twist, 1 by unprinted A'_t.
