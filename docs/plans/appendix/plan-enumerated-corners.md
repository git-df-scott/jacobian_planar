```json
{
  "angle": "Exhaust the published enumeration: treat arXiv:1708.07936 §6's 34-case corner list as the finite, printed search space, and drive every case that no published argument has ever discarded to a verdict that meets char-0 proof standard — or to an explicitly labelled NO VERDICT.",

  "thesis": "This campaign's biggest structural error was not a bad solver run; it was searching one survivor ((72,108)/(108,72)) for forty sessions while 24 of the 34 cases the same authors printed sat untouched, and while the two exclusions that created that framing (GGHV Cor 5.7 and the max<125 sweep) were never re-derived. The enumeration angle inverts that: the published corner list is finite, its chain data is printed, and the chain→reduced-polygon map already exists and reproduces 6/6 published reduced pairs (wt/canon/TRACKD_CHAIN_MAP.md, wt/canon/campaign/audit_tracks/ABOVE_125_STATUS.md 'Status: RESOLVED'). I verified the reproduction directly: wt/canon/gghv_audit/all_cases_max_le_150.json holds exactly 34 records, 10 with max<125, and record 22 is A0=[8,1,28], mid=[], final=[7,4,3], (m,n)=(3,4) → (deg P, deg Q)=(108,144). So the 24 untouched cases are compilable TODAY — they need no chain-compiler extension past 150, which is the blocker for the disjoint 429 NO-CHAIN census in wt/canon/wave6/frontier_151_300_map.json. The honest expected outcome is 24 more EMPTY verdicts and a paper that raises the published floor from 108 toward 150; a counterexample is not the modal result and I put the probability that this whole plan surfaces one at roughly 1-2%. The conjecture is widely believed; Moh, GGV, GGHV and Nguyen have all searched here and found nothing; every 'hit' this campaign ever produced was a gauge artefact (wt/canon/STATUS.md §6.6). What the plan can deliver with high probability is the thing the archive never once produced: a characteristic-zero, ideal-membership-certified EMPTY on real systems, and a defensible statement about which regions are genuinely unsearched. The value is in closing the enumeration, and a counterexample is the low-probability tail of doing that honestly.",

  "leads": [
    {
      "id": "L1",
      "title": "Compile and decide (8,28)/(m,n)=(3,4) at (108,144) — the flagged first untouched case",
      "target": "arXiv:1708.07936 §6 record #22: A0=[8,1,28], mid=[], final=[7,4,3], (m,n)=(3,4), deg pair (108,144), gcd 36.",
      "premise_with_citations": "FABLE-006, the unanswered head of the agent thread (wt/mailbox/AGENT_MAILBOX.md:3909-3985, commit 156ba7a), names this as the first new target: same corner A0=[8,1,28] as the campaign's pentagon but a different (m,n), so it is a genuinely different reduced-polygon system that nobody has ever built. I confirmed the row exists in wt/canon/gghv_audit/all_cases_max_le_150.json (record 22). The literature reader flags a live bookkeeping risk: this is an ABOVE-125 case at max 144, NOT part of the sub-125 frontier, and the campaign may have conflated it with the (m,n)=(3,2) row at (108,72) (READER: literature, disagreements item 1). Nothing published discards it — GGHV's elimination stops strictly below 125. The verdict machine (trackB1_polygon.py / trackB1_shapes.py) verifies a reduced pair but does not guess one, so the only missing piece is the Prop-4.3 analogue for this corner, and the chain→polygon map for max<=150 is RESOLVED (wt/canon/campaign/audit_tracks/ABOVE_125_STATUS.md).",
      "method": [
        "1. LIBRARIAN: read wt/canon/papers/1708.07936.pdf §6 by page-image (poppler/pypdf are absent per READER: toolchain gap 1; use a page render + read, never a text-extraction guess) and confirm record 22's A0/mid/final/(m,n) byte-for-byte against gghv_audit/all_cases_max_le_150.json. Fable's own caveat is that his table came from PDF text extraction and he had retracted three extraction errors that session (AGENT_MAILBOX.md:3976-3982).",
        "2. COMPILER: derive the reduced polygon pair N(P), N(Q) and the bracket exponent r for this chain via GGHV Sec 4 + [C] Thm 2.20 as already mechanised in wt/canon/TRACKD_CHAIN_MAP.md. Do NOT pattern-fit from (9,27) and (8,28)/(3,2) — that exact fit produced three retracted results (ABOVE_125_STATUS.md).",
        "3. GATE: check the certified invariant eps_P + eps_Q = (r+1, 1) on the candidate pair (verified on both published reduced shapes: (9,27)→(1,1)+(1,0)=(2,1) with r=1; (8,28)→(1,0)+(2,1)=(3,1) with r=2). Any candidate violating it is wrong and is discarded before compute.",
        "4. EXPORTER: build the coefficient system, fix gauges with a recorded admissibility proof (forced-nonzero variable of nonzero weight + computed torus rank), compute eqs−vars excess, and REFUSE to launch if excess <= 0 (the pentagon burned two weeks at W=19 which was underdetermined by 6).",
        "5. PRE-FLIGHT: measure the grading-torus rank of the exported ideal. If rank > 0 the system is positive-dimensional and msolve solve-mode cannot terminate — this is exactly why the two p108 systems 'timed out' (AGENT_MAILBOX.md:1610-1625). Slice with a validated gauge or run msolve -g 2 (Groebner-only), which decides emptiness at any dimension.",
        "6. RUN mod p at three compliant primes (p ≡ 1 mod 3, good reduction), each through the wt/canon/wave4/w4_msformat.py sanitizer, stderr+stdout teed and grepped for 'Error when parsing' / 'cannot be 0 modulo'.",
        "7. If EMPTY at 3 primes with agreeing leading-term ideals: escalate to L5's char-0 certification pipeline. If NON-EMPTY or positive-dimensional: extract points, run wt/canon/lift/lift_pipeline.py (Hensel to p^(2^K) + half-extended-Euclid reconstruction + exact substitution), then wt/canon/reruns2/w3_hit_protocol.py H1-H6 with H6 shown to have fired."
      ],
      "agent_roles": ["Librarian (PDF page-image verification, no text extraction)", "Compiler (chain→reduced polygon, GGHV Sec 4)", "Exporter/Sanitizer (gauge proof, excess check, w4_msformat gate)", "Solver-runner (msolve -g 2 / Singular, stderr discipline)", "Lifter (Hensel + rational reconstruction)", "Red-team adjudicator (must attempt to break every EMPTY before it is recorded)"],
      "inputs": ["wt/canon/papers/1708.07936.pdf §6", "wt/canon/gghv_audit/all_cases_max_le_150.json record 22", "wt/canon/TRACKD_CHAIN_MAP.md", "wt/canon/campaign/audit_tracks/ABOVE_125_STATUS.md", "wt/canon/wave4/w4_msformat.py", "wt/canon/lift/lift_pipeline.py"],
      "outputs_with_labels": ["reduced polygon pair for (8,28)/(3,4) — [CERTIFIED] only if the eps invariant and the 6/6 published-pair regression both pass", "per-prime solver verdicts — EMPTY-mod-p(p1,p2,p3), never promoted", "NO VERDICT ledger row on timeout/OOM/SIGSEGV, with the measured torus rank attached so it is not blindly requeued at a longer budget"],
      "stop_rule": "Stop and record EMPTY-mod-p after 3 agreeing compliant primes with agreeing leading-term ideals; hand to L5 for char-0. Stop and record NO VERDICT after 2 x 1800 s at two primes with clean stderr, plus the torus rank. Abandon the case entirely only if the eps invariant fails or the LIBRARIAN finds the row is not in the published table.",
      "cost": "Compiler + export: 2-4 agent-days, near-zero CPU. Solver: 3 primes x 1800 s = ~1.5 CPU-hours if it decides; open-ended if it resists. Needs Singular 4.3.2 and msolve 0.10.1 installed (absent from the current container) and >=32 GB RAM.",
      "p_hit": "~0.5% that this case yields a NON-EMPTY reduced-polygon point, and conditional on that maybe 1-in-5 that it survives lifting and H1-H6 rather than being a degenerate/gauge point — so ~0.1% for an actual counterexample. Reasoning: it is genuinely unsearched, which is the only thing raising it above zero, but the base rate across every system this campaign ever ran is 0 non-EMPTY verdicts in ~46 decided systems, and the necessary-condition direction means a non-empty polygon system is not yet a counterexample. Its real value is as the first row of a publishable 24-case closure.",
      "why_not_already_closed": "It was surfaced only in the final, unanswered mailbox message (FABLE-006, HEAD 156ba7a) and the artifacts it references (FABLE_24_OPEN_CASES.md, fable_xcol/) live on branch claude/fable-counterexample-sweep-yyj5vf which is not in any local worktree. No solver was ever pointed at it."
    },

    {
      "id": "L2",
      "title": "Sweep the remaining 23 published-but-untouched cases (max in [125,150]) — the finite closure",
      "target": "The 24 records of gghv_audit/all_cases_max_le_150.json with max >= 125 (including the boundary case (75,125) at max exactly 125, which GGHV's 'max < 125' sweep does NOT cover), minus L1's record 22.",
      "premise_with_citations": "GGHV eliminate max < 125 only; arXiv:1708.07936 enumerates to 150 and discards nothing above the frontier (READER: literature, campaign_vs_literature row 4: 'agree — no published exclusion covers 125-150 or beyond'). I counted the file: 34 records, 10 below 125, and records 11-34 span max 125..150. Critically, these 24 have their full chain data PRINTED, so they need no chain-compiler extension — that blocker (wt/canon/OPEN_ITEMS.md §6, 429 NO-CHAIN records in wt/canon/wave6/frontier_151_300_map.json) applies only to the 156-300 census where the library stops. Prior above-125 work certified 20 shapes across 6 chains EMPTY but at the single prime F_65521 (ABOVE_125_STATUS.md certification log) and left the (11,33) chain with 115 surviving shapes — so 'above 125 partially done' is much weaker than it reads.",
      "method": [
        "1. Build the case register: one row per published record, keyed by (A0, mid, final, m, n, orientation) — orientation is part of the key (L is not a function of the degree pair: (72,108)→L=3, (108,72)→L=4).",
        "2. COMPILER runs TRACKD_CHAIN_MAP.md's reduction on all 24; regression-gate on the 6 published reduced pairs before trusting any new one; eps_P+eps_Q=(r+1,1) on every output.",
        "3. Content-hash (md5 + tail-hash) every exported system BEFORE scheduling and check it against the existing register — p108_821326 and p108_843700 were md5-identical and their EMPTY was double-counted; 49 TIMEOUTs collapsed to 16 unique systems.",
        "4. Sort by eqs/vars and sweep SMALLEST FIRST at 900 s x 2 compliant primes, escalating resisters to 1800 s and to the second engine.",
        "5. For every resister, run the cheap grading/torus finder (exponent-vector linear algebra, minutes per shape) BEFORE spending a long budget — a positive-dimensional system is a structural non-terminator, not a slow one (CROSSDOOR.md §2).",
        "6. For resisters that survive that, try the degree-bounded Nullstellensatz certificate route (1 = sum h_i f_i mod p): it is a LINEAR system in the h coefficients, streamable at fixed memory and parallel across the fleet (CROSSDOOR.md §6). One experiment at certificate degree <= 3 on a single resister decides whether the route is feasible at all.",
        "7. Bridge the 20 existing single-prime F_65521 EMPTYs to a second and third compliant prime — they are currently the weakest class of verdict in the archive.",
        "8. Record every case's disposition; NO VERDICT rows carry torus rank, eqs/vars, wall time, RSS and stderr hash."
      ],
      "agent_roles": ["Compiler (24 parallel reductions)", "Registrar (content-hash dedup, orientation-keyed ledger, NO-VERDICT ledger)", "Fleet scheduler (serial-queue enforcer process, not a prose 'one-heavy' rule — policy without an enforcer already caused two OOM kills)", "Solver-runners x4 (one per core, disk AND memory swept)", "Nullstellensatz specialist (linear certificate search)", "Red-team adjudicator"],
      "inputs": ["wt/canon/gghv_audit/all_cases_max_le_150.json", "wt/canon/gghv_audit/ggv_algorithms.py", "wt/canon/TRACKD_CHAIN_MAP.md", "wt/canon/campaign/audit_tracks/ABOVE_125_STATUS.md", "wt/canon/CROSSDOOR.md §2, §6", "wt/canon/wave4/w4_msformat.py"],
      "outputs_with_labels": ["24-row closure table, each row EMPTY-mod-p(list) / [PROVED-exact] / NO VERDICT(reason, torus rank) — no row may be blank", "Nullstellensatz feasibility verdict at certificate degree <= 3 (one experiment)", "the enumerable list of resisters, which the archive has never possessed (the '41 timeout shapes' are folklore — no file lists them)"],
      "stop_rule": "Per case: 2 primes x 1800 s + one torus-rank probe + one Nullstellensatz attempt at degree <= 3, then NO VERDICT and move on. Fleet-wide: stop the sweep when every one of the 24 has a labelled row, or after 200 CPU-hours, whichever first.",
      "cost": "24 cases x ~2 primes x 1800 s = ~24 CPU-hours if they behave; realistically 100-200 CPU-hours with resisters. Compiler build ~1 agent-week. This is the cheapest large block of genuinely new territory on the board.",
      "p_hit": "~1-1.5% that at least one of the 23 yields a NON-EMPTY, and ~0.3% that one survives lifting + H1-H6. Reasoning: 23 independent-ish lottery tickets in a region no published argument covers is the strongest structural argument available, but the priors are brutal — Moh/GGV/GGHV believe the conjecture, the corner enumeration exists precisely because they expected all cases to die, and every analogous case this campaign decided came back EMPTY. The dominant value is the closure statement, not the hit.",
      "why_not_already_closed": "The campaign chased a '804 pairs above 125' figure that FABLE-006 disavows as coming from lost sessions with no supporting artifact, and an unbuilt chain-compiler extension for the 429 NO-CHAIN records at max 156-300 — and never noticed that the 24 published cases at max<=150 need neither. 20 of 167 legacy targets were decided at one prime on an ephemeral container that was reclaimed."
    },

    {
      "id": "L3",
      "title": "Finish the (9,27)/(m,n)=(2,3) reopening — the first independent test of GGHV Cor 5.7",
      "target": "wave6/ms/p108_192622.ms (40 vars; 1 unresolved leaf, 139 eq / 38 vars) and p108_525122.ms (28 vars; 5 leaves, 3 EMPTY, 2 unresolved) — the reduced (9,27) polygons of GGHV Prop 4.1.",
      "premise_with_citations": "The (9,27) orientation of (72,108) is killed in print ONLY by GGHV Cor 5.7, whose proof was found invalid at gghv.txt:1430-1433: after translation the bracket is 1/2 + (lambda/2)x^{-1/2}, not in K^x, so [1, Cor 7.2] does not apply, and 51 of the 66 conditions (5.12) asserts are unsupported (wt/canon/CATCHES.md 'GGHV COROLLARY 5.7 IS UNPROVEN'; wt/canon/WEEKEND_PLAN.md §P3). The literature reader confirms no published erratum, follow-up, or re-derivation exists, so the reopening is defensible rather than contradicted. OPUS43-012 (wt/mailbox/AGENT_MAILBOX.md:1597-1680) showed both systems are grading-torus RANK 5, hence positive-dimensional — their 1800 s msolve timeouts were STRUCTURAL, not budget — sliced both with a validated gauge (weight-minor determinants -1/24 and -1/14, nonzero), said 'running now', and no verdict was ever reported: wt/mailbox/wave6/frontier/P108_RESULTS.md still shows both NO VERDICT. Note the label hazard: FABLE-004 argues the campaign's '(9,27)' label named a case the paper had already discarded; always carry (m,n) with the corner.",
      "method": [
        "1. Re-derive the rank-5 grading independently (a second agent, different code path) before reusing the slice — the gauge must come with a proof that every orbit meets the slice, not just a nonzero minor.",
        "2. Re-export the two sliced systems, sanitize through w4_msformat.py, hash and register.",
        "3. Run msolve -g 2 (Groebner-only, decides emptiness at any dimension) on the 2 unresolved leaves of 525122 and the 1 leaf of 192622, at 3 compliant primes, 3600 s each, on a machine with >=32 GB and >2.5 h uptime (the archive's container died every ~30 min and had a measured ~2.5 h process ceiling).",
        "4. In parallel, PAPER track: attempt to REPAIR Cor 5.7 — supply the missing 51 conditions by a different argument, or exhibit that they fail. A repair closes (9,27) with no compute; a failure makes the compute mandatory.",
        "5. Pre-registered verdict standard (already recorded in AUDIT_EOD Sec 10 and reaffirmed here): EMPTY at one prime is replication-grade only; NON-EMPTY requires the full prime tower plus a char-0 lift before the word 'refutation' is used about GGHV §5."
      ],
      "agent_roles": ["Grading auditor (independent re-derivation of the torus rank and slice)", "Solver-runner (msolve -g 2, leaf-parallel across cores)", "Paper repairer (Cor 5.7 line-by-line, working from wt/canon/papers/2204.14178.pdf and gghv.txt)", "Lifter", "Red-team adjudicator"],
      "inputs": ["wt/mailbox/wave6/frontier/P108_RESULTS.md", "wt/mailbox/AGENT_MAILBOX.md:1597-1680", "wt/canon/CATCHES.md 'GGHV COROLLARY 5.7 IS UNPROVEN'", "wt/canon/WEEKEND_PLAN.md §P3", "wt/canon/papers/2204.14178.pdf §5"],
      "outputs_with_labels": ["per-leaf verdicts EMPTY-mod-p(p1,p2,p3) or NON-EMPTY→lift queue", "a repaired-or-broken status for GGHV Cor 5.7, labelled [PROVED-exact] / REFUTED / UNVERIFIED-HERE — never 'confirmed by citation'", "if all leaves EMPTY at 3 primes: an independent, non-Cor-5.7 closure of the (9,27) orientation, which is publishable on its own"],
      "stop_rule": "Per leaf: 3 primes x 3600 s with clean stderr → EMPTY-mod-p, hand to L5. Any leaf returning a zero-dimensional non-empty parametrization → freeze the fleet, lift, and run H1-H6 before anything is said aloud. Paper track: 2 agent-weeks, then declare Cor 5.7 UNVERIFIED-HERE and let the compute stand alone.",
      "cost": "3 leaves x 3 primes x 3600 s ≈ 9 CPU-hours if they decide; the systems are small (28-40 vars) and the previous failure was structural, so this is genuinely likely to finish. Paper track: 1-2 agent-weeks of reading.",
      "p_hit": "~2-3% of a NON-EMPTY verdict on some leaf — the highest per-run figure in this plan, because the only thing that ever closed this orientation is a proof with a hole in it, and 3 of 5 leaves already came back EMPTY (which cuts both ways: it is evidence the remaining 2 also die). Conditional on NON-EMPTY, maybe 1-in-4 that it is a real point rather than a slice/gauge artefact, and then still a necessary-condition system — so ~0.3-0.5% for a counterexample. A NON-EMPTY here would be a major result even without one: it would mean GGHV §5 is wrong and the published floor of 108 is not established.",
      "why_not_already_closed": "The runs were launched but the session ended; the mailbox head is an unanswered message and P108_RESULTS.md still records NO VERDICT. This is a MISS in the archive's own taxonomy (run believed launched, verdict never harvested), not a mathematical obstruction."
    },

    {
      "id": "L4",
      "title": "Build GGHV Prop 4.3's never-built second sub-case, and attack pentagon case (1) as a rank-drop rather than a 186-variable Groebner",
      "target": "(a) Prop 4.3 sub-case (2): 70 unknowns / 92 equations, 21 variables after gauge. (b) Pentagon case (1) at (8,28)/(3,2): FABLE-003's 57-variable rank-drop on a 303x124 structured matrix, replacing the 186-unknown/302-equation monolith.",
      "premise_with_citations": "FABLE-004 (wt/mailbox/AGENT_MAILBOX.md:3676-3801) states GGHV Prop 4.3 has a SECOND sub-case the campaign never built — 70 unknowns/92 eqs vs the pentagon's 184/302, reducing to 21 variables after gauge — and that the transfer is necessary-only, so EMPTY is the publishable direction. It is UNANSWERED at head. FABLE-003 (:3562-3675) says 'STOP SOLVING 186 VARIABLES': Q is linear and redundant, the pentagon is a 57-variable rank-drop on a 303x124 structured matrix with exactly ONE inhomogeneous equation. Pentagon case (1) has NO VERDICT by any method anywhere: msolve on pent_L23.ms OOM exit 137 at 13.9 GB with 0-byte output, Singular slimgb on L18 exit 137, monolithic measured >40 GB (wt/canon/STATUS.md §3, wt/canon/pent/RUNLOG.tsv). Every pentagon failure has been MEMORY, never mathematics. Two P0-style shortcuts are already dead and must not be re-run: trackB1_pentagon.py:432 witness() certifies every truncation W=12..19 NON-EMPTY over Q (so the truncation-ladder kill plan is futile, and W=19 was underdetermined by 6 besides), and three rational-function cascade attempts all failed their own two-sided self-tests, attempt #3 reporting 57 dead branches on a system with an obvious solution.",
      "method": [
        "1. LIBRARIAN verifies Prop 4.3's two sub-cases against wt/canon/papers/2204.14178.pdf directly, and tests whether D=1 is compatible with the sub-case (2) polygon at all — FABLE-004 says that alone might kill it in one line.",
        "2. Build sub-case (2) from the polygons (70/92), gauge to 21 variables with a recorded admissibility proof, hash, sanitize, and run at 3 compliant primes. At 21 variables this is well within reach of msolve -g 2 and Singular, unlike anything else in the pentagon territory.",
        "3. Independently re-derive FABLE-003's reduction (Q linear/redundant → 57-variable rank-drop on a 303x124 matrix) with a second agent; require the reduction to reproduce a known EMPTY and a known NON-EMPTY control before it is trusted (three cascades died exactly here).",
        "4. If the rank-drop formulation holds: the question becomes a determinantal/rank condition on a structured 303x124 matrix over a small parameter space — attack it with modular rank profiles across many primes plus exact minor certificates, NOT with a monolithic Groebner basis.",
        "5. Continue the exact coupled level ladder from level 16 downward on both surviving branches (wt/l16/breakthrough/PENTAGON_LEVEL16.md, PENTAGON_LEVEL15_BRANCH2.md), keeping the W-variables (W9 = g9 − (3c1/2c0)z^4 h5, W8 = g8 − (3c1/2c0)z^4 h4) and the bounded-support end conditions — dropping either manufactures false obstructions and spurious solutions respectively.",
        "6. Settle the bottom-edge Galois question the cheap honest way: FACTOR the degree-9 eliminant over Q directly (the char-0 bottom edge already solves in 316 s), rather than inferring orbit structure from Chebotarev averages — a five-prime average already falsified two claims made off four primes."
      ],
      "agent_roles": ["Librarian (Prop 4.3 sub-case verification, D=1 compatibility test)", "System builder (sub-case 2, 70/92 → 21 after gauge)", "Reduction auditor (independent re-derivation of the rank-drop, with two-sided controls)", "Rank-profile specialist (modular ranks + exact minor certificates)", "Ladder mathematician (levels 14 and below, coupled W-variables)", "Red-team adjudicator"],
      "inputs": ["wt/mailbox/AGENT_MAILBOX.md:3562-3675 (FABLE-003), :3676-3801 (FABLE-004)", "wt/canon/papers/2204.14178.pdf Prop 4.3", "wt/canon/wave1/pent_L23.ms", "wt/canon/pent/RUNLOG.tsv", "wt/l16/breakthrough/PENTAGON_LEVEL16.md, PENTAGON_LEVEL15_BRANCH2.md", "wt/canon/wave6/bottomedge/be_c2is1_q.ms, analyse.py, orbit_data.txt"],
      "outputs_with_labels": ["sub-case (2) verdict — EMPTY-mod-p(3 primes) or NON-EMPTY→lift; this is a first-ever verdict on a system GGHV printed and nobody built", "rank-drop reformulation — [CERTIFIED] only after both controls fire, otherwise shipped as a FAILING tool (the archive's own convention: keep failed cascades committed with their self-tests)", "degree-9 bottom-edge eliminant factored over Q — [PROVED-exact], replacing the RETRACTED Chebotarev orbit story", "pentagon case (1): most likely still NO VERDICT — say so"],
      "stop_rule": "Sub-case (2): 3 primes x 3600 s, then verdict or NO VERDICT. Rank-drop: if the reduction fails either control, stop and record the failure; do not run it on the pentagon (attempt #3 would have falsely killed the campaign's best lead). Ladder: stop when a level branches without closing twice in a row. Eliminant factorization: 1 CPU-hour box.",
      "cost": "Sub-case (2) at 21 gauged variables: hours of CPU, days of build — the best cost/novelty ratio in the (72,108) territory. Rank-drop audit: 1-2 agent-weeks. Eliminant factorization: ~1 CPU-hour. Full pentagon solve: still out of reach, do not schedule it.",
      "p_hit": "~1% for sub-case (2) returning NON-EMPTY (never searched, small, and the campaign's whole framing assumed only one sub-case existed), ~0.2% for the pentagon proper (it has resisted every engine, but only on memory, so the resistance carries no information about emptiness). Combined counterexample probability ~0.2-0.3%. Being honest: FABLE-004 says the transfer is necessary-only, so even a NON-EMPTY here is a candidate to lift, not a counterexample.",
      "why_not_already_closed": "Sub-case (2) was named three times (FABLE-004, -005, -006) and never built; the referenced fable_xcol/subcase2.py is not in any local worktree. The pentagon was never closed because both engines OOM'd, and the archive is explicit that OOM is not a verdict."
    },

    {
      "id": "L5",
      "title": "Turn EMPTY-mod-p into certified char-0 EMPTY: modular Gröbner + CRT + ideal-membership, and the case-(2) residual over K",
      "target": "First: case (2) at (72,108), chart d_3_3=1, currently EMPTY at 65521/32003/65537 (two of which violate the campaign's own p ≡ 1 mod 3 hygiene rule) and re-run compliantly at 65539/65599 — never char 0. Then every EMPTY produced by L1-L4, and the 20 single-prime above-125 EMPTYs.",
      "premise_with_citations": "The archive's standing rule forbids promoting mod-p to Q, and it proved the rule necessary: trackA_eliminator.py --mod p closes branches with genuine rational solutions (x−3y=0, x+4y−z=0 with nonzero=[y,z] has the rational point (3,1,7) but reports '1 closed, 0 open leaves' mod 7, and --verify replays it OK, exit 0). So all 46 EMPTY verdicts in the archive are permanently short of a char-0 statement (READER: toolchain gap 7). But gap 7 also names the fix, and nobody built it: compute the GB at several good primes, CRT + rationally reconstruct the reduced GB over Q, then VERIFY by exact ideal membership of the original generators in the reconstructed basis — 1 in the reconstructed ideal is a checkable characteristic-zero proof. The case-(2) edge eliminant is already degree 1144, squarefree and PROVED irreducible over Q by a Dedekind subset-sum sieve at 8 primes plus an independently factored 9th (100153), so all 1144 edge points are Galois-conjugate and the Q-bar question is ONE yes/no about the generic point (wt/canon/STATUS.md §2.5). The 13-variable residual over K = Q[theta]/(f), [K:Q] = 1144, is MISS-4: planned, never executed. The gcd shortcut for it was retracted (substituting the RUR eliminates only the 7 edge variables, leaving 13 free, so there is no univariate polynomial).",
      "method": [
        "1. Build the certified modular GB lift as a reusable fleet tool: (a) GB at k >= 8 good primes; (b) lucky-prime filter — compare leading-term ideals and discard minority primes, recording the batch and the majority signature; (c) CRT + rational reconstruction of the reduced GB (reuse the half-extended-Euclid reconstruction in wt/canon/lift/lift_pipeline.py, |r|,|s| <= sqrt(m/2)); (d) VERIFY over Q by exact ideal membership — reduce each original generator against the reconstructed basis and require remainder 0, and for an EMPTY require 1 in the ideal. Step (d) is what makes it a proof rather than a guess: it never trusts the primes.",
        "2. Two-sided self-test the tool before any real data: a planted-consistent system must NOT certify 1, and a planted-inconsistent one must. Ship it failing rather than delete it if it does not pass.",
        "3. Apply to case (2) chart d_3_3=1, driving through the existing marker-resumable wt/pq/trackB_exactQ.py (QELIM_TIMEOUT env, JCLEAF=1 leaf selection) so a container restart costs a stage, not the run.",
        "4. Separately, attack the 13-variable residual over K directly: work in K = Q[theta]/(f) with deg f = 1144. Do NOT expand over Q. Use the RUR representation and modular-over-K arithmetic; if that is infeasible, state so and record the case as EMPTY-mod-p, not char-0.",
        "5. Apply the same tool to every EMPTY coming out of L1, L2, L3, L4 — this is the pipeline that converts the whole enumeration sweep from evidence into a theorem."
      ],
      "agent_roles": ["Pipeline builder (CRT + reconstruction + ideal-membership verifier, with two-sided self-test)", "Lucky-prime certifier (leading-term-ideal majority vote)", "Number-field specialist (13-var residual over degree-1144 K)", "Solver-runner (marker-resumable trackB_exactQ driver)", "Red-team adjudicator (must try to make the verifier accept a planted-consistent system)"],
      "inputs": ["wt/canon/lift/lift_pipeline.py", "wt/pq/trackB_exactQ.py, trackB_Q_elim.sing, RESUME_STATE.md", "wt/canon/wave1/edgeQ_eliminant.txt, edgeQ_param.out", "wt/canon/STATUS.md §2.4, §2.5, §4", "wt/canon/campaign/audit_tracks/CASE2_VERDICT.md"],
      "outputs_with_labels": ["a reusable certified-modular-GB tool — [CERTIFIED] only with both self-tests firing", "case (2) char-0 verdict: [PROVED-exact] EMPTY over Q, or NO VERDICT with the stage it died at", "promotion of L1-L4 EMPTYs from EMPTY-mod-p to [PROVED-exact] where the membership check passes", "explicit statement of which EMPTYs could NOT be promoted — that list is itself a result"],
      "stop_rule": "Tool: if the two-sided self-test fails, stop and fix; never run it on real data first. Case (2): 3 attempts at increasing prime counts (8, 16, 32) and 10800 s per stage; if reconstruction does not stabilize, record NO VERDICT and the reconstruction bound reached. Residual over K: one 24 h attempt, then declare it out of reach and say so.",
      "cost": "Tool build: 1-2 agent-weeks, the highest-leverage engineering on the board. Case (2): the elimination route already collapses the residual to 27 conditions in 6 parameters and decides in hundredths of a second mod p, so the char-0 run is genuinely the remaining step; the brute-force 71-variable route is out (OOM at 10 GB). Budget 20-50 CPU-hours.",
      "p_hit": "~0% of finding a counterexample — case (2) is EMPTY at five primes and will almost certainly be EMPTY over Q. I include it because it is the only lead that changes the epistemic status of everything else, and because the archive's own retracted-evidence history means an unpromoted EMPTY is worth less than it looks. If the char-0 run instead returns NON-EMPTY, that is a five-prime modular result being wrong, which would be extraordinary and would immediately go to the HIT protocol — call it 0.2%.",
      "why_not_already_closed": "It is MISS-4: identified as plan item P4 across multiple sessions, run repeatedly as a partial (trackB_exactQ_run2..runD.log), and never finished, because the container had a measured ~2.5 h process ceiling while the fallback charts carried 6 h timeouts they could never reach. The ideal-membership lift for EMPTY verdicts was never built at all."
    },

    {
      "id": "L6",
      "title": "The B=16 ladder as a disjoint door: d=8 chart N, d=12 unsaturated, d=27",
      "target": "GGV Pro Mathematica 27 (2013) Thm 1.2 systems (1.2)+(1.3) on the CORRECTED (1.2), cell deg(q1)=d ↔ degree pair (16(3d−2), 16(2d−1)). Frontier cell d=8 chart N: 30 eq / 23 unknowns, exported as wave5/ms/m16_d8_*.ms. Then d=12 (resonant: 12·12=144 a square, row-0 roots exactly −1/12 and 1/20, both rational) including the UNDECIDED unsaturated family, then d=27 (resonant, 114 eq / 85 unk).",
      "premise_with_citations": "This door is provably disjoint from (72,108): a B=16 solution has gcd 16 while (72,108) has gcd 36, so a hit in either suffices (wt/canon/CROSSDOOR.md §1). GGV 2013's Thm 1.2 is an IFF — B=16 iff a solution with mu_0 != 0 exists, which would BE a counterexample — and it discards nothing; it stalls at deg(q1)=5, and GGHV's 'in [4] this case has been discarded' is a sloppy citation (wt/canon/ADJUDICATION.md §2, §3). On the corrected system d=3..7 are EMPTY in characteristic zero (d=7: 26 eq/20 unk, 1345 s, 6.67 GB, with a positive control showing the pipeline can return non-empty), so d=8 is the frontier. It is exported and NEVER LAUNCHED (MISS-2). Two hard constraints: EVERYTHING built on the printed GGV (1.2) is VOID as a statement about B=16 — the −2·mu3·q1''(0) term in row 3 is a misprint confirmed on four independent legs — so only m16_* corrected artefacts count; and the Z/N chart split is unnecessary work on the corrected export (seeding the row-0 root covers the whole cell because the relation is mu-free), while F2 (mu0 = a2·mu2/3) proves chart Z contains no counterexample.",
      "method": [
        "1. Re-confirm on disk which file is the corrected launch-ready d=8 export — OPEN_ITEMS.md §1 names wave5/ms/m16_d8_*.ms, WEEKEND_PLAN.md §P1 and MORNING_SUMMARY.md point to wave6/w6_seed_d8.py as the generator. Regenerate from the generator and diff against the export; do not launch on an unverified path.",
        "2. Sanitize through w4_msformat.py (assert 0 <= c < p, no zero terms, no nonzero constant generator, round-trip re-parse) — the msolve silent-lie mode produced a fake 20-second d=8 'EMPTY' via exactly this path.",
        "3. Choose primes where 12d is a square (the row-0 roots are sqrt(12d)-irrational); prime SIZE buys nothing here (16-bit bought nothing; the bottleneck is Gröbner structure, not coefficient arithmetic). Use 3 such primes.",
        "4. Run msolve -g 2, a_16 left FREE so one run covers both roots of the row-0 quadratic; capture stderr; >=32 GB, uninterrupted 2-8 h.",
        "5. If EMPTY at 3 primes → hand to L5 for char-0 (d<=6 already have char-0 proofs, so this extends a real ladder). If NON-EMPTY → this is the single most valuable outcome on the board: an mu_0 != 0 solution IS a counterexample by GGV's iff. Lift and run H1-H6 immediately.",
        "6. Then d=12 chart N (a_{2d} unseeded so one run covers −1/12 and 1/20) and, separately, the d=12 UNSATURATED family (mu0 free, mu2 gauged to 1) — the corrected unsaturated form is EMPTY at d=3,4,5,6 and was NEVER attempted at 12; this is the campaign's own named biggest anomaly since the d=3 analogue solves instantly.",
        "7. d=27 (114/85) only if a rational-function cascade exists — direct Gröbner scales ~32x per ladder level (d=6: 42 s, d=7: 1345 s), so d>=8 char-0, d=12 and d=27 are out of reach by the direct route. If the cascade is attempted, it must pass a two-sided self-test first; three prior attempts failed exactly there.",
        "8. Do NOT cite the rank/bifurcation criterion for anything: it is a can't-fail certifier (the system contains 6·mu0 − 2·a2·mu2 = 0 with a2 = mu2 = 0 at the quasi-homogeneous point, so dmu0 = 0 is forced at every d). Its 'ALWAYS OBSTRUCTED at d=3..27' is bookkeeping, not evidence.",
        "9. Separately and cheaply: attempt the GGV conjecture (all solutions have mu_1 = mu_2 = 0) symbolically in d. A proof closes B=16 outright; a refutation at some d is a CONSTRUCTIVE counterexample. Closed forms and the resonance law are already in hand and the proof was never attempted."
      ],
      "agent_roles": ["Export verifier (regenerate + diff, sanitize)", "Solver-runner (msolve -g 2, prime selection with 12d a square)", "Cascade builder (two-sided self-test mandatory, paper design first)", "Symbolic-d mathematician (GGV conjecture attempt)", "Lifter + HIT gate operator", "Red-team adjudicator"],
      "inputs": ["wt/canon/OPEN_ITEMS.md §1-§4", "wt/canon/wave5/ms/m16_d8_*.ms", "wt/canon/wave6/w6_seed_d8.py", "wt/canon/MORNING_SUMMARY.md", "wt/canon/CATCHES.md 'GGV (1.2) ROW 3 IS MIS-PRINTED'", "wt/canon/wave4/w4_msformat.py", "wt/canon/CROSSDOOR.md §1"],
      "outputs_with_labels": ["d=8 chart N verdict — EMPTY-mod-p(3 primes) then [PROVED-exact] via L5, or NON-EMPTY → HIT queue", "d=12 chart N and d=12 unsaturated verdicts", "GGV conjecture: proof attempt outcome, labelled OPEN if unattained (a failed proof attempt is not evidence of anything)", "explicit statement that everything on the printed (1.2) stays VOID and was not reused"],
      "stop_rule": "d=8: 3 primes x 8 h; if all three time out with clean stderr, record 'exact elimination cannot decide d>=8 chart N on this hardware' — which is what the archive already measured across four formulations — and do not requeue at longer budgets without a new formulation. d=12/d=27: do not launch until a self-tested cascade exists. GGV conjecture: 3 agent-weeks, then OPEN.",
      "p_hit": "~0.5% for d=8 returning NON-EMPTY, falling to ~0.2% for d=12 and lower still for d=27 — but this is the ONE lead where a non-empty verdict is a counterexample by an iff rather than a necessary condition, which is why it earns its slot despite being off the enumeration angle. Countervailing: d=2..7 all died, the excess of conditions over unknowns grows roughly like d, and the campaign's exact structural results all point toward rigidity. ~0.3% overall.",
      "why_not_already_closed": "d=8 chart N is MISS-2: exported and never launched. The four prior d=8 attacks all ran on the now-VOID printed system. d=12 chart N seed 1/20 was required and never run (MISS-3) with its export already on disk. d=27 is untouched. The shared blocker (the rational-function cascade, P2) is an unbuilt machine, not a mathematical wall."
    },

    {
      "id": "L7",
      "title": "Audit the exclusions, not the survivor: A'_t=(1,0), the 3 twist-blocked cases, and the tail-closure test",
      "target": "(a) The unprinted A'_t = (1,0) assumption in arXiv:1708.07936 for the (10,40) case at max 250; (b) the 3 twist-blocked cases (c_t non-integer), unclassified; (c) the tail-closure conjecture, which decides whether the 429 NO-CHAIN records at max 156-300 are finite work or a research programme.",
      "premise_with_citations": "OPUS43-012's strategic ask was to audit the EXCLUSIONS rather than the survivor, naming four never-checked load-bearing inputs: the 125 bound, GGHV Cor 5.7, Nguyen 104, and the unprinted A'_t=(1,0) (wt/mailbox/AGENT_MAILBOX.md:1597-1680). Two have since moved: Cor 5.7 was broken (L3), and Nguyen 104 is recorded as real and refereed after a retraction — though the literature reader could NOT independently confirm a Nguyen 2025 paper and recommends citing GGHV's 108 until it is located. A'_t remains: wt/canon/AUDIT_EOD.md §4 item 3 and wt/canon/CATCHES.md:32 record that [5] assumes A'_t=(1,0) without printing the derivation, and wt/canon/STATE_FULL.md §C classifies '3 twist-blocked + 1 A'_t-assumed case' as unclassified. A'_t is a corner of the chain's terminal polygon data — it determines which (m,n)/degree pairs the chain admits — so if it is not forced, the enumeration itself gains rows that appear on nobody's list. Meanwhile CROSSDOOR.md §5 records that reduced systems depend only on the chain TAIL, that the predictor (last-2-segments, shape index) → system hash has ZERO violations across all 16 groups the campaign generated, and that the library is 34 chains → 26 distinct tails. Its proposed test — extend the compiler on 20 sample cases across (150,300] and count new tails vs reused — was assigned to Codex in OPUS43-012 and NEVER REPORTED BACK.",
      "method": [
        "1. Re-derive A'_t for the (10,40) chain inside the existing independent re-implementation wt/canon/gghv_audit/ggv_algorithms.py, which already reproduces 34/34 published cases at max<=150 and 10/10 GGHV Sec 2 rows with four negative controls. Enumerate the admissible A'_t from the algorithm's OWN filters instead of importing (1,0); check whether any alternative survives and whether it produces a degree pair. Same pattern the campaign already used successfully for the F6 gcd(m,n)=2 discrepancy (D7).",
        "2. Decide whether the 3 twist-blocked cases (c_t non-integer) form a territory or admit a closure theorem — pure math, no compute.",
        "3. Run the tail-closure test: extend the compiler to 20 sample cases across (150,300], hash each resulting system, and count new tails vs reused. A low new-tail rate supports saturation and collapses the 429-case frontier to finitely many tail-systems, most already decided; a high rate means the frontier is a research programme and should be de-prioritized honestly rather than quietly carried as 'open'.",
        "4. Run the same enumeration re-derivation forward: gghv_audit already produces 474 possible counterexamples at max<=300 independently of the campaign's queue — reconcile that against the 429/464/804 figures, which the archive states three incompatible ways and never reconciles.",
        "5. Do NOT propagate the figures '189 non-closing records' or '184 new tail hashes': two independent readers grepped the whole tree and found nothing matching them. Treat them as non-facts.",
        "6. Locate the Nguyen 2025 paper or downgrade the campaign's citation of a 104 floor to GGHV's 108."
      ],
      "agent_roles": ["Enumeration auditor (re-derive A'_t inside ggv_algorithms.py, with the existing negative controls)", "Compiler engineer (20-case tail extension)", "Registrar (reconcile 429 / 464 / 474 / 804 into one orientation-keyed count)", "Librarian (Nguyen 2025, GGV Pro Mathematica 27 §3.5 for the (80,112) kill that GGHV cites without argument)"],
      "inputs": ["wt/canon/gghv_audit/ggv_algorithms.py", "wt/canon/gghv_audit/all_cases_max_le_150.json", "wt/canon/AUDIT_EOD.md §4", "wt/canon/CATCHES.md:32", "wt/canon/STATE_FULL.md §C", "wt/canon/CROSSDOOR.md §5", "wt/canon/wave6/frontier_151_300_map.json"],
      "outputs_with_labels": ["A'_t: [PROVED-exact] forced, or NEW ROWS added to the enumeration — the only outcome in this plan that can make the published search space bigger", "3 twist-blocked cases: classified or declared a territory", "tail-closure: measured new-tail rate over 20 samples, labelled MEASURED (the saturation claim itself stays CONJECTURE)", "one reconciled above-125 count replacing four circulating numbers"],
      "stop_rule": "A'_t: if ggv_algorithms.py's filters force (1,0), record [PROVED-exact] and close; if they do not, the new rows go to the front of L2's queue. Tail test: 20 cases, then report the rate whatever it is — do not extend to 40 hunting for a nicer number. Whole lead: 3 agent-weeks.",
      "cost": "Hours of CPU; the work is reading and re-running an existing enumerator. 2-3 agent-weeks of careful attention. Essentially free relative to its leverage.",
      "p_hit": "~0% directly — this lead finds no counterexample. Its value is that it is the only thing that can make the search space LARGER, and it is the cheapest insurance against the failure mode that already cost this campaign two months twice (a misprinted equation and an unproven corollary, both imported without re-derivation). Probability that A'_t is not forced and new rows appear: maybe 10-15%, which is high enough to run first.",
      "why_not_already_closed": "OPUS43-012 assigned the tail test to Codex and no answer exists anywhere in the mailbox. A'_t has been sitting in the ledger as 'literature fact' since it was first flagged. The campaign consistently audited its survivor and not its exclusions — which is precisely how Cor 5.7 survived for weeks."
    }
  ],

  "audit_gates_before_compute": [
    "ENGINES: Singular 4.3.2 and msolve 0.10.1 are NOT installed in the current container (only python3 + sympy/flint/gmpy2 and PARI/GP). apt-get/build them first; a fresh container comes up with neither, and every campaign script assumes Singular on PATH. Make the runner fail with an explicit MISSING-ENGINE status, not a generic nonzero rc.",
    "VOID-SOURCE GATE: no artefact built on GGV's PRINTED (1.2) may be reused for any B=16 statement — only corrected m16_* exports count. Grep every scheduled input for provenance before launch.",
    "MOH GATE: refuse to schedule any ansatz whose maximum realizable total degree is <= 100; also assert gcd(deg P, deg Q) = 16 or > 20, and below max 125 that the pair is (72,108) in one of its two orientations. A 1728-shape sweep was killed mid-run for failing exactly this.",
    "EXCESS GATE: compute eqs − vars before every emptiness run; refuse if excess <= 0 (the variety is positive-dimensional and cannot be empty). Two weeks were spent on W=19, underdetermined by 6.",
    "TORUS-RANK GATE: measure the grading-torus rank before requeueing any timeout at a longer budget. Timeouts in this archive are frequently STRUCTURAL (msolve solve-mode cannot terminate on positive-dimensional input); use -g 2 or slice with a gauge carrying an admissibility proof.",
    "EXPORT SANITIZER: every .ms goes through wave4/w4_msformat.py — coefficients reduced mod p, vanishing terms dropped, raise on any nonzero constant generator, round-trip re-parse. This is what prevents msolve's silent-lie mode ('[-1]' written, exit 0, on a parse failure).",
    "STDERR DISCIPLINE: tee stdout AND stderr for every solver call; grep for 'Error when parsing' / 'Error when reading' / 'cannot be 0 modulo'. Ignore exit codes entirely — msolve exits 0 on timeout, crash, OOM and parse error alike. A verdict may be read only from a non-empty output file.",
    "PRIME HYGIENE: p ≡ 1 mod 3 for the (72,108) family; for the B=16 ladder, primes where 12d is a square. Run a lucky-prime certifier (leading-term-ideal majority vote across a batch) and record the batch and majority signature with every EMPTY. Prime size buys no speed here.",
    "CONTENT-HASH DEDUP: md5 + tail-hash every export and check the register before scheduling; verdicts transfer by hash identity. Two md5-identical p108 systems had their EMPTY double-counted, and 49 TIMEOUT records collapsed to 16 unique systems.",
    "CAN'T-FAIL SCAN: AST-scan the tree for compile-time-constant check conditions (45+ exist) and require every certifier to carry a negative control that is OBSERVED to fail. A [PASS] inside a docstring is prose, not a computation.",
    "TWO-SIDED SELF-TEST: every branch-and-reduce, cascade or reduction tool must keep a planted-solution system OPEN and CLOSE a planted contradiction before touching real data. Three cascades failed here; attempt #3 would have falsely killed the campaign's best lead.",
    "GAUGE ADMISSIBILITY: every gauge ships with a proof that every orbit meets the slice (a forced-nonzero variable of nonzero weight) plus the computed torus rank of the exact system it is applied to. Both historical false-positive 'hits' were gauge artefacts.",
    "NUMERICAL LANES ARE FINDERS ONLY: no multi-start Newton result may be cited as evidence of emptiness. Planted roots at residual exactly 0 were not found at 25 or 165 unknowns. A hit is real; a miss is zero information.",
    "LITERATURE ANCHORING: every load-bearing published step is located by exact substring match against a file on disk (file:line) or labelled UNVERIFIED-HERE. A negative literature claim requires a positive source (erratum, retraction, refereed contradiction), never failed searches. No venue/volume/year unless read off the published record.",
    "DISK AS WELL AS MEMORY: poll free disk and inodes before and during long runs; ENOSPC silently killed the W=10 eliminator mid json.dump while both memory monitors read healthy.",
    "PROCESS HYGIENE: pkill -f / pgrep -f on your own script name is banned (it killed the invoking shell four times); use a pgrep -x loop and stop jobs by handle. Every 'running' claim carries a live PID check. Push after every commit — the working tree rolled back twice."
  ],

  "risks": [
    "The dominant risk is that the conjecture is TRUE in the plane. Moh, GGV, GGHV, Borisov and Nguyen have all searched this exact territory expecting emptiness; Tao's July 2026 digestion says explicitly the plane case remains open after the dimension-3 refutation, and Gao gives no reason the plane should fail. Everything below is conditional on a ~1-2% overall chance that any counterexample exists in the enumerated region at all.",
    "NON-EMPTY on a reduced-polygon system is a NECESSARY-condition result, not a counterexample. FABLE-004 says the transfer is necessary-only. A hit must survive Hensel lift, rational reconstruction, exact substitution, and then H1-H6 including H5 gauge invariance under random affine changes on source AND target — the check both historical false positives would have failed. Expect most non-empty verdicts to be degenerate or gauge artefacts.",
    "The compiler is the plan's single point of failure. If the chain→reduced-polygon reduction is wrong for the 24 cases, every downstream EMPTY is a statement about the wrong variety — which is exactly the GGV-(1.2) failure mode replayed. Mitigation: the 6/6 published-pair regression plus the eps_P+eps_Q=(r+1,1) invariant, both mandatory, both cheap. Naive pattern-fitting from the two known examples is known to fail: (9,27) has A0 = n·(3,9) exactly while (8,28) is not n·(lattice point), and fitting a rule to them produced three retracted results.",
    "Infrastructure defeated the previous campaign more often than mathematics did: ~13.3 GiB cgroup cap, restarts every ~30 min, a measured ~2.5 h process ceiling, and a git tree that rolled back twice. Nothing in this plan works on that hardware. It needs >=32 GB, persistent uptime, and marker-resumable per-branch pipelines rather than monolithic Gröbner runs.",
    "Fleet-scale re-derivation of the same wrong thing. Route 1 / Route 2 for case (2) were file-level disjoint but both descend from the same GGHV polygon derivation — code disjointness is not independence. Agents given the same brief will reproduce the same misreading; independence must be enforced by giving auditors the PAPER, not the campaign's transcription.",
    "Verdict inflation under fleet pressure. The archive's own history is a chain of results announced and retracted within hours (four-prime Chebotarev falsified by the fifth; a 20-second 'd=8 EMPTY' that was a parse error; '36 new conditions' retracted; a five-day-old char-0 eliminant that was actually real-solution boxes). A larger fleet makes this worse, not better. Every claim needs an adversarial red-team agent whose only job is to break it, and a contradiction linter that makes two records under one key with incompatible labels a hard error.",
    "Reading the wrong artifact. Several referenced files (fable_xcol/subcase2.py, FABLE_24_OPEN_CASES.md, session43/LEADS.md, trackB1_sat_Q.ms, the 164/288/6821 'strongest inherited asset') are named in the record but exist in NO local worktree, and the README's dimensions for that asset (164 vars/288 quadratic eqs) contradict the mailbox's (166 vars/284 eqs/degree 5). Do not plan around artifacts nobody has opened.",
    "Scope creep into the 429 NO-CHAIN records at max 156-300. That region needs either a genuine extension of GGV's shape analysis past 150 (a paper's worth of work, weeks to months) or the tail-closure conjecture to hold. It is also gated by the L>=5 cubic wall, where Gröbner timed out at 1200 s and iterated resultants stalled on d_-3 — both dying at the same place because it is a shape change, not a compute limit. Keep it in L7 as a measurement, not as a search."
  ],

  "what_would_change_my_mind": [
    "A NON-EMPTY, zero-dimensional verdict on either p108 leaf (L3) at two or more compliant primes with clean stderr. That would mean GGHV Cor 5.7 is not merely unproven but false, the published floor of 108 is not established, and the (9,27) orientation is live territory. I would move the whole fleet onto lifting that point and would raise my overall counterexample probability from ~1-2% to something I would not be embarrassed to call 10%.",
    "A NON-EMPTY on the B=16 ladder at d=8 chart N on the CORRECTED system (L6). GGV Thm 1.2 is an iff, so a solution with mu_0 != 0 is a counterexample, not a candidate — this is the only place on the board where the implication runs the right way. Even a single-prime non-empty would justify dropping everything else.",
    "A'_t = (1,0) turning out NOT to be forced by ggv_algorithms.py's own filters (L7), producing degree pairs that appear on nobody's list. That would mean the published enumeration is incomplete and the entire 'exhaust the enumeration' framing is searching a strict subset — which changes what 'closure' even means.",
    "The tail-closure test coming back with a high new-tail rate over 20 samples. That would kill the cheap route to the 429-case frontier and tell me the above-150 region is a multi-year programme, not a compute queue — which should push effort toward L1/L2/L6 and toward publishing the [125,150] closure rather than chasing the tail.",
    "The published §6 table of arXiv:1708.07936, read from page images, disagreeing with gghv_audit/all_cases_max_le_150.json in any row. The entire plan rests on that reproduction; the literature reader calls verifying it 'the single highest-value remaining check' and it has never been done. A mismatch invalidates L1 and L2 as scoped.",
    "The certified modular-GB lift (L5) failing its own two-sided self-test, or succeeding and then failing the exact ideal-membership check on a system that is EMPTY at eight primes. Either would mean the campaign's 46 EMPTY verdicts are even weaker than 'evidence only' — and would make the honest conclusion that almost nothing here has been decided at all.",
    "Conversely: if L1-L4 all return EMPTY promoted to [PROVED-exact] via L5, and L6's d=8 dies too, I would conclude the enumerated region below 150 is genuinely closed, say so plainly, and argue the remaining effort belongs in the tail region or in a structural theorem (the GGV conjecture mu_1 = mu_2 = 0) rather than in more solver time."
  ]
}
```