```json
{
  "angle": "Get characteristic-zero verdicts on the strongest inherited assets — convert the campaign's mod-p walls and OOM walls into exact, checkable char-0 statements, and in the same motion open the only lanes that can emit an actual witness.",

  "thesis": "Every strong inherited asset is stuck at one of exactly two places: (i) a mod-p unit Gröbner basis that the campaign's own rule forbids promoting to Q (case (2) both charts; the README's alleged p=1000003 unit GB on the bottom-seed target; the whole B=16 chart-Z ladder at d>=7), or (ii) a memory/time wall with a 0-byte output (pent_L23 msolve exit 137 at 13.9 GB; Singular slimgb L18 exit 137; seed-pinned twin exit 124 at 5400 s / 9.5 GB; four d=8 chart-N formulations TIMEOUT with clean stderr). Neither is a verdict. The single object that dissolves BOTH walls without ever computing a characteristic-zero Gröbner basis is an explicit cofactor certificate 1 = sum h_i f_i over Q. Finding it is a LINEAR system in the unknown coefficients of h_i — and under the L = 2*alpha - beta torus grading and the bilinear block structure that linear system is block-diagonal by weight, because the constant 1 sits in a single L-weight and only receives contributions from h_i of weight -L(f_i). Linear algebra is restartable, memory-bounded, embarrassingly parallel over primes, and — decisively — its output is verified by one exact polynomial identity that either holds or does not. That check is immune to every failure mode in the archive's pitfall list: it cannot time out into a fake EMPTY, cannot be msolve's silent '[-1]', cannot be a bad-prime artifact, cannot be a can't-fail certifier. It is also the a-posteriori luckiness proof that the modular-GB literature (Arnold/Pauer) demands. So the plan is: build the certificate machine once, point it at the four or five smallest live objects, and run the monolithic systems only after structural reduction (bilinear determinantal projection; torus-graded cascade) has cut them below the engines' real ceilings. Separately and honestly: a certificate lane produces PROOFS OF EMPTY, not counterexamples. Exactly one lane here can emit a witness — exhaustive/branching search on the seed-pinned, linearly reduced pentagon twin (241 eq / 123 unk) and its truncations — and its prior probability is low. I put P(this whole program yields a genuine JC2 counterexample) at roughly 1.5-3%, and P(it yields at least one new characteristic-zero verdict on a named inherited asset) at roughly 60-75%. The honest expected product of this angle is proofs, plus a much sharper map of where a counterexample could still hide.",

  "leads": [
    {
      "id": "L1",
      "title": "The graded Nullstellensatz certificate machine (build once, then fire at the four smallest live objects)",
      "target": "Infrastructure + first applications: (a) pentagon truncation W=19, 21 eq / 27 unk (canon/WEEKEND_PLAN.md §P0; export via pq/trackB1_msolve_export.py from pq/trackB1_trunc19.json); (b) case-(2) w=-4 residual, 27 conditions in 6 parameters (canon/HUNT_REPORT.md Item 1b/1c); (c) B=16 d=8 chart N corrected export, 30 eq / 23 unk (wave5/ms/m16_d8_*.ms, regenerable by wave6/w6_seed_d8.py); (d) the two p108 (9,27) sliver leaves that survive of five (mailbox/wave6/frontier/TARGET_QUEUE.md).",
      "premise_with_citations": "Emptiness over Q-bar of an ideal I = (f_1..f_m) is EQUIVALENT to the existence of cofactors h_i over Q with 1 = sum h_i f_i (Hilbert Nullstellensatz, weak form). Finding h_i with deg h_i <= D is a linear system over Q. Two facts from the archive make this the right engine here rather than a curiosity. First, the campaign's blockers are all memory/time on Gröbner engines, never mathematics: 'Every pentagon failure to date has been memory, never mathematics' (canon/WEEKEND_PLAN.md §P0); msolve L23 exit 137 at 13.9 GB with 0-byte output (canon/wave1/L23_VERDICT.txt, canon/pent/RUNLOG.tsv); Singular slimgb L18 exit 137; four d=8 chart-N formulations 'ALL TIMEOUT with clean stderr — resistance measured, not a verdict' (canon/CATCHES.md 08:00Z). Second, the grading is real and independently verified: 'the L = 2*alpha - beta grading and the bilinearity of every monomial is INTACT' (p11/CATCHES.md:1600-1604), with a level census of deficits +1,+2,+3,+3,+1 (READER bottom-seed, 'structure'). A graded certificate decomposes: writing L(1)=0, only h_i of weight exactly -L(f_i) contribute, so the coefficient matrix is block-diagonal by weight and each block is small. Third, the archive already possesses the extraction primitive it needs and never used it: Singular's `lift(I, ideal(1))` returns the cofactor matrix directly, and Singular's degBound-truncated std is SOUND ONE-SIDED — every element std emits lies in I, so a unit at any degBound rung is a genuine 1 ∈ I. The campaign recorded exactly this idea as an owed handoff ('degBound = 7 then 8 with std(I) ... a unit at either rung is a real EMPTY verdict', mailbox/AGENT_MAILBOX.md:236-250) and never ran it. Finally: 'truncate(W) in trackB1_pentagon.py builds a CLOSED subsystem — every full solution restricts to a truncation solution — so an EMPTY truncation kills pentagon case (1) of (72,108) outright' ([PROVED-exact] structural, canon/WEEKEND_PLAN.md §P0). W=19 at 21 eq / 27 unk is the smallest live object in the entire campaign, and pentagon case (1) has NEVER had a verdict by any method (canon/STATUS.md §3, canon/ADJUDICATION.md §3).",
      "method": [
        "1. Re-derive the truncate(W) closure lemma from pq/trackB1_pentagon.py by hand and by a random-point test (sample 200 F_p points of the full system, check every one restricts to a truncation solution) BEFORE any W=19 result is allowed to mean anything. If closure fails, W=19 EMPTY proves nothing and this sub-target dies.",
        "2. Build cert_find.py: input a .ms/Singular ideal over Q; compute the L-weight of every generator and every monomial; for a target Nullstellensatz degree D, emit the graded linear system A*x = e_1 whose unknowns are the coefficients of the h_i restricted to the single contributing weight block. Use python-flint 0.9.0 (fmpz_mat / fmpq_mat, present in the container) for exact rank and solving; fall back to multi-prime solve + CRT + rational reconstruction (gmpy2 present) when a block exceeds ~40k unknowns.",
        "3. Degree ladder: probe D = maxdeg(f)+1, +2, +3, ... mod p=1000003 first (cheap rank test only, no solution vector). The smallest D at which the mod-p system is consistent is the working D. If no D <= 12 is consistent mod p, the object is NOT empty mod p at that prime — record that as a POSITIVE signal and hand it to L5.",
        "4. At the working D, solve exactly over Q (flint fmpq_mat, or CRT over 8 primes ≡ 1 mod 3 from {1000003, 1000033, 1000039, 999983, 1000081, 100153, 65539, 65599} + rational reconstruction).",
        "5. VERIFY: expand sum h_i f_i in exact rational arithmetic (sympy Poly / flint) and assert it equals 1 identically. This check is the entire proof; the solver is untrusted scaffolding. Store the certificate as a file with a sha256 and the exact expansion log.",
        "6. In parallel, run the independent Singular route on the same object: `ring r=0,(...),dp; option(redSB); degBound=7; ideal G=std(I); degBound=0;` then `matrix M = lift(I, ideal(1));` — if G contains a unit, extract cofactors and verify by step 5. Two disjoint routes to the same certificate.",
        "7. Controls before any verdict counts: (positive) plant a known solution into the same pipeline and confirm NO certificate is found at any D <= working D; (negative) feed a provably empty toy of the same shape and confirm a certificate IS found and verifies. Refuse to report without both."
      ],
      "agent_roles": ["Reconciler (audit gates, file provenance, hash pinning)", "Structurist (computes L-weights, block decomposition, degree ladder)", "Certificate-Engineer (writes and runs cert_find.py + the Singular lift route)", "Adversarial Verifier (re-expands every certificate independently in a second CAS, owns the positive/negative controls, has authority to void)", "Ledger scribe (records D, prime set, wall time, RSS, sha256 per artifact)"],
      "inputs": ["pq/trackB1_trunc19.json", "pq/trackB1_pentagon.py", "pq/trackB1_msolve_export.py", "wave5/ms/m16_d8_*.ms (or regenerate: python3 wave6/w6_seed_d8.py)", "canon/HUNT_REPORT.md Item 1b/1c (w=-4 residual, 27 conditions / 6 params)", "p108_525122 surviving leaves", "python-flint 0.9.0, gmpy2 2.3.1, sympy 1.14.0 (present); apt-get install singular msolve on every fresh container"],
      "outputs_with_labels": ["cert_W19.txt + expansion log -> if it verifies: 'pentagon case (1) of (72,108) EMPTY over Q-bar' [PROVED-exact], conditional on the step-1 closure lemma", "cert_w4res.txt -> 'case (2) w=-4 residual EMPTY over Q' [PROVED-exact]", "cert_m16d8N.txt -> 'B=16 ladder d=8 chart N EMPTY over Q' [PROVED-exact], first char-0 verdict past the d<=6 frontier", "cert_p108_leafK.txt -> first independent char-0 confirmation of any part of GGHV Cor 5.7", "NO-CERTIFICATE-AT-D<=12 report per object -> [NO VERDICT], and a referral to L5 as a live witness target"],
      "stop_rule": "Per object: abandon the certificate route when the graded linear system at the smallest mod-p-consistent D exceeds 2e5 unknowns in a single weight block after grading, OR when D reaches 12 with no mod-p consistency, OR after 8 CPU-hours on the object. Never report a failed certificate search as evidence of non-emptiness — it is [NO VERDICT] in exactly the same sense as a timeout.",
      "cost": "Build: ~2 agent-days of engineering, no compute. Per small object (a)-(d): minutes to ~4 CPU-hours each, under 4 GB, fully inside the 4-core/15 GB/30-min-restart box because every stage checkpoints to disk (weight block granularity). No rented machine needed for this lead.",
      "p_hit": 0.02,
      "why_not_already_closed": "The campaign never built it. The idea appears once as an owed handoff (degBound ladder, mailbox/AGENT_MAILBOX.md:236-250) and was lost to container restarts; `lift()` is never invoked anywhere in the tree. Every recorded attack was a monolithic Gröbner run on a box with a 13.3 GiB cap and ~30-minute restarts, which is precisely the shape of computation that box cannot host."
    },

    {
      "id": "L2",
      "title": "Bilinear determinantal projection of trackB1_sat_Q.ms — cut 166 variables to the d-block before asking any engine anything",
      "target": "wave6/frontier/trackB1_sat_Q.ms — the reconstructed characteristic-zero pentagon case (1) system: 166 vars, 284 eqs, degree 5, max integer coefficient 468, 8,774 coefficients, common-integer-lifted from p=1000003 and p=65521 and re-reduced 284/284 to both (mailbox/AGENT_MAILBOX.md:246-260). Saturated on c_1_0, c_8_14, d_12_21, s_4_8.",
      "premise_with_citations": "Four Gröbner attacks across two representations and a 10x budget range all ended NO VERDICT: 60 vars deg 22 p_16_8-saturated -> OOM 13.9 GB in 18 min; 186 vars deg 2 all-vertex-saturated -> timeout 2.3 GB at 40 min and again at 3 h; 148 vars deg 2 p_10_2-saturated -> timeout 1.5 GB at 50 min (mailbox/AGENT_MAILBOX.md:1419-1432). msolve is STRUCTURALLY excluded from this object: hard 2^25 hash-table ceiling, segfaults above ~180 variables (mailbox/AGENT_MAILBOX.md:1053,1233). So the engine question is closed — no engine reaches this object as posed, and adding saturation variables makes it worse. But the system is BILINEAR (p11/CATCHES.md:1600-1604) and block-structured into c-block and d-block variables with a square-block index at wave6/frontier/tb1_square_block.json. A bilinear system is, for each fixed value of the d-block, a LINEAR system in the c-block. Hence: a solution with the saturated coordinates nonzero exists iff there is a d-block point at which the c-linear system M(d)*c = b(d) has a solution whose saturated coordinates are nonzero — a rank/minor condition on M(d) alone. That eliminates the entire c-block as unknowns and replaces 166 variables by the d-block plus the ideal of appropriate minors. The archive also hands over a free branch: 'c_1_0*d_0_1 = 0 and c_1_0*d_1_1 = 0 with c_1_0 saturated nonzero kills one branch instantly and forces d_0_1 = d_1_1 = 0' (mailbox/wave6/frontier/TARGET_QUEUE.md).",
      "method": [
        "1. AUDIT GATE FIRST (see audit_gates_before_compute G1/G2): fetch wave6/frontier/ from branch claude/opus-5-counterexample-plan-sep6yk (head 24a06fc), count the header of trackB1_sat_Q.ms directly, and reconcile against the main README's '164 vars / 288 quadrics / 6,821 terms over Q(alpha)'. Those numbers are not the mailbox numbers and no file on disk reconciles them. Do not compute on an unreconciled object.",
        "2. Re-verify the char-0 lift at a THIRD prime never used in the reconstruction (e.g. 100153 or 999983): independently regenerate the mod-p export at that prime from the generator script and check it agrees monomial-for-monomial with the reduction of trackB1_sat_Q.ms. A common lift from two primes is not evidence of correctness; a third independent prime is.",
        "3. Split the variable list into c-block and d-block using tb1_square_block.json; verify by symbolic inspection that every one of the 284 equations is bilinear (degree <= 1 in each block separately). If any equation is not, record the exceptional set and handle it as side conditions.",
        "4. Apply the free branch: set d_0_1 = d_1_1 = 0 in the c_1_0-saturated branch; re-run the sound constant-coefficient linear fixed-point reduction (the pattern that took 267/148 to 241/123, p11/CATCHES.md:1622-1645) with every coefficient reduced mod P before export to avoid the recorded msolve coefficient trap.",
        "5. Build M(d) symbolically over the d-block; compute its generic rank r over F_p by random specialization at 50 points. The emptiness condition becomes: the ideal generated by (rank(M(d)) < r_required minors) + the d-block's own equations + saturation is empty.",
        "6. Feed THAT system — expected d-block-only, order tens of variables rather than 166 — to msolve -g 2 (now under the 180-var ceiling) at 3 primes ≡ 1 mod 3, and to Singular facstd (factorizing GB, which exploits the product structure of minor ideals) in parallel.",
        "7. Whatever verdict emerges mod p, do NOT promote it. Hand the reduced system to L1's certificate machine for the char-0 statement, and to L6 for lucky-prime certification.",
        "8. If any branch returns NON-EMPTY mod p, stop everything else and route to L5's witness pipeline (Hensel lift, exact substitution, HIT protocol H1-H6)."
      ],
      "agent_roles": ["Reconciler (G1/G2 gates, branch fetch, header counts)", "Structurist (block split, bilinearity proof, minor-ideal construction)", "Prime-Farmer (parallel msolve/Singular jobs, one prime per job, each sized under 25 min)", "Certificate-Engineer (hands off to L1)", "Adversarial Verifier (planted-solution control on the reduced system: plant a c-solution over a random d-point and confirm the minor formulation recovers it)"],
      "inputs": ["wave6/frontier/trackB1_sat_Q.ms (must be fetched — absent from all local worktrees and from archives/transfer/state_transfer.tgz, excluded as 'regenerable')", "wave6/frontier/trackB1_sat_p1000003.ms", "campaign/audit_tracks/trackB1_case1_full_p65521.ms", "wave6/frontier/tb1_square_block.json", "mailbox/wave6/frontier/TARGET_QUEUE.md"],
      "outputs_with_labels": ["RECONCILIATION.md: the true dimensions of the strongest inherited asset, or a finding that the README object does not exist [audit finding]", "trackB1_dblock_p*.ms + verdicts -> [EMPTY mod p, k primes] only", "cert_trackB1.txt if L1 succeeds on the reduced system -> 'pentagon case (1) at (72,108) EMPTY over Q-bar' [PROVED-exact]", "NON-EMPTY mod p at any prime -> [CANDIDATE-UNVERIFIED], immediately escalated to L5"],
      "stop_rule": "Kill the lead if step 3 shows the system is not genuinely bilinear (the projection is then invalid and the whole lead is void), or if the minor ideal after step 5 still exceeds 120 variables (msolve's practical ceiling here) with no further grading reduction available. Abandon after 40 CPU-hours on the rented box. A timeout at any stage is NO VERDICT and must be logged as such.",
      "cost": "Structural work: ~3 agent-days. Compute: the minor-ideal runs want a rented 64-128 GB / 16-32 core box (roughly $1-3/hr on-demand, $0.4-1/hr spot; budget 60-120 machine-hours, i.e. low hundreds of dollars). The 4-core/15 GB box can host steps 1-5 and the certificate stage but not step 6 at full size.",
      "p_hit": 0.025,
      "why_not_already_closed": "The campaign attacked this object only by throwing whole saturated formulations at Gröbner engines, and its own retrospective says so: 'Stop feeding saturated systems to Groebner' (mailbox/AGENT_MAILBOX.md:1455-1470). The bilinear projection is never attempted anywhere in the tree, and the object itself is not present in any local checkout — the file was excluded from the transfer archive as 'regenerable', which is why nobody re-attacked it after the handoff."
    },

    {
      "id": "L3",
      "title": "Char-0 verdict on case (2) via the w=-4 cascade, NOT via the degree-1144 field",
      "target": "The (72,108) case-(2) characteristic-zero residual. Two routes exist; take the cheap one. Route A (avoid): 13 variables over K = Q[theta]/(f), deg f = 1144, with a 46 MB RUR and a 5.7 MB eliminant carrying a 4666-digit leading coefficient — planned, NEVER EXECUTED (canon/STATE_FULL.md §B MISS-4; canon/OPEN_ITEMS.md P4). Route B (take): the w = j-2i block decomposition of the 72-var/92-eq edge system, whose w=-4 block has a degree-5 eliminant over Q and whose residual collapses to 27 conditions in 6 parameters (canon/HUNT_REPORT.md Item 1, 1b, 1c).",
      "premise_with_citations": "Case (2) is EMPTY mod p at three fresh compliant primes on both charts and by two code-disjoint routes (canon/STATUS.md §2.4-§2.5; HUNT_REPORT records 1000003, 1000033, 1000039 in every Galois orbit, both charts). The campaign forbids promoting that to char 0 and STATUS.md §7 is the stricter, later record. The degree-1144 eliminant is squarefree and PROVED irreducible over Q by a Dedekind subset-sum sieve at 8 primes, independently re-verified at a 9th (100153) — so all 1144 edge points are Galois-conjugate and 'the Q-bar question is one yes/no' (canon/ADJUDICATION.md §1). That is exactly the leverage: irreducibility over Q means we need to decide ONE generic point, not 1144. But the degree-1144 field is a terrible place to compute. The w=-4 route reaches the same conclusion in a degree-5 field: the w=-4 eliminant over Q was CRT-reconstructed from 41 of 96 primes and reproduces msolve's eliminant at 6 held-out primes — squarefree, irreducible over Q, no rational root, Galois group S5 — with the residual collapsing to 27 conditions in 6 parameters (canon/HUNT_REPORT.md 'the edge variety over Q, exactly'). 27 conditions in 6 parameters over a degree-5 field is a genuinely small char-0 object; the direct 71-variable route OOM-killed at 10 GB, which is a failure, not a verdict.",
      "method": [
        "1. Promote the w=-4 degree-5 eliminant from 'CRT-reconstructed and verified at held-out primes' to [PROVED-exact]: re-derive it as an exact char-0 resultant/elimination in Singular over Q on the w=-4 block alone (small), and check the two objects are equal as polynomials. Held-out-prime agreement is strong evidence, not a proof; an exact char-0 computation of the same object is.",
        "2. Prove irreducibility over Q exactly with `factorize()` in Singular / PARI `polisirreducible` — degree 5 is trivial; do not rely on the Chebotarev/Galois-S5 inference (the archive retracted a four-prime Chebotarev read once already, p11/CATCHES.md:2037-2065, commit 7a747c8).",
        "3. Set K = Q[t]/(g), deg g = 5. Load the 27 residual conditions in 6 parameters over K into Singular using an algebraic extension ring (`ring R = (0,a),(x1..x6),dp; minpoly = ...`). This is the exact computation the campaign always deferred.",
        "4. Run std(I) over K. If dim == -1, that is a genuine characteristic-zero EMPTY on the w=-4 block. Immediately extract cofactors with `lift(I, ideal(1))` and verify the identity exactly (L1 step 5) so the verdict rests on a checkable certificate, not on trusting Singular's dim.",
        "5. Repeat for every other w-block of the 72/92 system that is not already closed exactly, and for BOTH charts d_3_3 = 1 and d_3_3 = 0. Emptiness of the whole edge variety requires all blocks; emptiness of ONE block whose vanishing is necessary is enough to kill.",
        "6. Only if L3 stalls: fall back to Route A. There, do not attempt a Gröbner basis over the degree-1144 field. Instead run the 13-variable residual as a certificate search over K with the same graded machinery from L1 — the cofactor linear system over a degree-1144 field is large but is still LINEAR and CRT-parallelizable, whereas a GB over that field is not.",
        "7. Re-run the mod-p emptiness at two more primes ≡ 1 mod 3, since the recorded case-(2) EMPTY used 32003 and 65537, both ≡ 2 mod 3, violating the campaign's own hygiene rule (READER open-queue pitfalls; canon/STATUS.md)."
      ],
      "agent_roles": ["Structurist (w-block decomposition, chart handling)", "Certificate-Engineer (the K-extension ring, lift extraction)", "Adversarial Verifier (independent re-derivation of the degree-5 eliminant in PARI/GP, held-out-prime re-checks, planted-root control on the 27/6 system)", "Ledger scribe"],
      "inputs": ["canon/HUNT_REPORT.md (Item 1, 1b, 1c)", "canon/wave1/edgeQ_eliminant.txt", "canon/wave1/edgeQ_param.out", "canon/wave1/H1F_FINDING.md", "pq/trackB_exactQ.py (marker-resumable; QELIM_TIMEOUT env, JCLEAF=1 for leaf selection)", "pq/trackB_Q_elim.sing", "pq/RESUME_STATE.md"],
      "outputs_with_labels": ["W4_ELIMINANT_EXACT.txt -> degree-5 eliminant [PROVED-exact] over Q (upgrade from CRT-reconstructed)", "cert_case2_w4.txt -> 'case (2) of (72,108), w=-4 block, EMPTY over Q-bar' [PROVED-exact]", "If all blocks close: 'case (2) of the last surviving sub-125 degree pair is EMPTY in characteristic zero' [PROVED-exact] — the campaign's single largest owed promotion", "Any block returning dim >= 0 over K -> [LIVE], escalate to L5"],
      "stop_rule": "Stop the w=-4 route if the K-extension std exceeds 24 h or 100 GB on the rented box without terminating, and switch to the certificate form (step 6) rather than buying more memory. Stop the whole lead — declaring NO VERDICT, not EMPTY — if neither the direct nor the certificate form terminates within 200 CPU-hours. Do not, under any circumstance, let three-prime agreement drift into a char-0 claim in the write-up.",
      "cost": "Steps 1-2: under an hour on the 4-core box (the char-0 bottom-edge analogue already solves in 316 s). Steps 3-5: hours to a few days; wants a persistent machine with >2.5 h uptime — the existing pipeline configures 6 h timeouts on fallback charts that the container's measured ~2.5 h process lifetime can never reach (pq/RESUME_STATE.md). Rented 64 GB box, ~20-60 machine-hours. Note trackB_exactQ.py is marker-resumable, so restarts cost a stage not a run.",
      "p_hit": 0.005,
      "why_not_already_closed": "Purely a uptime and sequencing failure. The route was written down as plan item P4 and never executed (MISS-4); the char-0 edge eliminant factorization was mid-run at the last checkpoint with logs trackB_exactQ_run2..runD.log on disk; the container's ~30-minute restarts and ~2.5 h ceiling made the configured 6 h budgets unreachable. Also, the campaign kept aiming at the degree-1144 field (the hard route) because the w=-4 block decomposition was discovered later, in the hunt branch."
    },

    {
      "id": "L4",
      "title": "B=16 ladder: char-0 verdicts at d=8 chart N, d=12 unsaturated, d=27 — via torus-graded certificates, not via direct Gröbner",
      "target": "(a) d=8 chart N, corrected system, 30 eq / 23 unk, exported and NEVER LAUNCHED (canon/OPEN_ITEMS.md §1, MISS-2); (b) d=12 unsaturated family — UNDECIDED after two kill attempts, and the campaign's own named biggest anomaly since the d=3 analogue solves instantly (canon/STATE_FULL.md §A, §G.1); (c) d=27, the next resonant cell, 114 eq / 85 unk, untouched (canon/OPEN_ITEMS.md §4).",
      "premise_with_citations": "This is the one genuinely open counterexample corridor found in the literature: GGV Pro Mathematica 27 (2013) Thm 1.2 is an IFF — B=16 iff a solution with mu_0 != 0 exists, which would BE a JC counterexample — and GGV stall at deg(q1)=5 (canon/ADJUDICATION.md §2-§3; the campaign killed d=5 in 22 s and d=6 in 135 s over Q where GGV's 2013 machine failed after an hour). d <= 7 chart N are EMPTY in characteristic zero, so d=8 is the frontier cell (canon/OPEN_ITEMS.md §1). The corrected export is 30 eq / 23 unk with a_16 left FREE so one run covers both roots of the row-0 quadratic, and F2 (mu0 = a2*mu2/3) proves chart Z contains no counterexample, so chart N is the ONLY chart. Two structural wins are recorded and must be used: seeding the row-0 root covers the whole cell because the relation is mu-free, so the Z/N split is unnecessary work; and the roots are sqrt(12d)-irrational, so mod-p work needs primes where 12d is a square (canon/MORNING_SUMMARY.md 'Two structural wins'). Prime SIZE is irrelevant — 16-bit bought nothing, the bottleneck is Gröbner structure, not coefficient arithmetic (same source). Direct GB scales ~32x per ladder level (d=6: 42 s, d=7: 1345 s), which is exactly why d>=8 needs a different engine rather than a bigger box. d=12 is resonant: the row-0 quadratic 90a^2 + 3a - 3/8 has roots exactly -1/12 and 1/20 and 12*12 = 144 = 12^2; d=27 likewise, 12*27 = 324 = 18^2, roots -1/20 and 1/28 (canon/OPEN_ITEMS.md §2, §4). The corrected UNSATURATED system (mu0 free, mu2 gauged to 1) is EMPTY at d=3,4,5,6 — strictly stronger than 'no solution with mu0 != 0' — and was never attempted at d=12 (canon/OPEN_ITEMS.md §3).",
      "method": [
        "1. VOIDNESS GATE: confirm every input is a corrected-system (m16_*) artefact. Everything built on the printed GGV (1.2) is void, including the earlier d=12 kills, the b16_d8_*.ms exports and the d=12 'two rational seeds' bookkeeping (canon/OPEN_ITEMS.md header). Regenerate rather than reuse: python3 wave6/w6_seed_d8.py.",
        "2. Parse-trap gate: scan every generated .ms for a constant generator and for any coefficient with |c| >= p or c ≡ 0 mod p before feeding msolve. This is the exact condition that made msolve write '[-1]' and exit 0, producing a fake 20-second d=8 EMPTY (canon/CATCHES.md 06:00Z). Reduce every coefficient mod p and drop vanishing terms at export time.",
        "3. d=8 chart N, char 0 directly first — it is only 23 unknowns and has literally never been launched on the corrected export. `msolve -g 2 -f m16_d8_N_q.ms -o d8N_q.gb` with characteristic 0 in the file, 1 thread, 8 h budget, RSS watched. -g 2 (Gröbner-only) is MANDATORY: msolve's solve mode needs zero-dimensional input, so solve-mode runs on non-rigid systems are NO VERDICT by construction (READER bottom-seed pitfalls).",
        "4. In parallel on a second core: Singular over Q with the degBound ladder (degBound = 5,6,7,8; a unit at any rung is a genuine 1 ∈ I), then `lift(I, ideal(1))` for cofactors, then exact verification per L1 step 5. This route is restart-tolerant: each rung is a separate process under 25 minutes.",
        "5. In parallel on a third core: L1's graded certificate machine on the same 30/23 system, using the ladder's own weight grading. Three disjoint routes to one verdict.",
        "6. Mod-p controls only as sanity, at primes ≡ 1 mod 3 where 12d is a quadratic residue: for d=8 that means 96 must be a QR mod p — screen candidates from {1000003, 1000033, 1000039, 999983, 1000081, 100153} with a Legendre-symbol test and use only those that pass. Two agreeing primes minimum before quoting anything, and even then it is mod-p only.",
        "7. d=12 unsaturated: export the corrected unsaturated form (mu0 free, mu2 gauged to 1) — the form that is EMPTY at d=3..6 and never tried at 12 — and run the same three routes. Leave a_{2d} unseeded so one run covers both rational roots -1/12 and 1/20.",
        "8. d=27 (114 eq / 85 unk) LAST, and only via the graded certificate route: it is explicitly out of reach of direct Gröbner, and the rank criterion is a proven can't-fail certifier that cannot decide it (canon/OPEN_ITEMS.md §4).",
        "9. If any cell returns NON-EMPTY at a screened prime: that is the closest thing on the board to a counterexample, because GGV Thm 1.2 is an iff. Freeze, Hensel-lift via canon/lift/lift_pipeline.py, exact-substitute over Q, and run the full HIT protocol H1-H6 (canon/reruns2/w3_hit_protocol.py). Label CANDIDATE-UNVERIFIED until H1-H6 all pass."
      ],
      "agent_roles": ["Exporter (regenerates corrected m16_* systems; owns the constant-generator and coefficient-reduction guard)", "Prime-Farmer (Legendre screening, one prime per job)", "Certificate-Engineer", "Adversarial Verifier (planted-root control at 23 unknowns — note multi-start Newton is PROVEN blind at 25 unknowns, so numerical controls are inadmissible here; use planted algebraic controls only)", "Witness-Auditor (owns HIT H1-H6 if step 9 fires)"],
      "inputs": ["wave6/w6_seed_d8.py", "wave5/ms/m16_d8_*.ms", "canon/OPEN_ITEMS.md §1-§4", "canon/MORNING_SUMMARY.md", "canon/lift/lift_pipeline.py", "canon/reruns2/w3_hit_protocol.py", "msolve 0.10.1 (build from source; the apt 0.6.5 candidate was rejected by the campaign), Singular 4.3.2"],
      "outputs_with_labels": ["d8N_q.gb + cert_m16d8N.txt -> 'B=16 ladder d=8 chart N EMPTY in characteristic zero' [PROVED-exact] — extends the char-0 frontier from d<=6 to d<=8 and is the campaign's cheapest genuinely new result", "d12_unsat verdict -> [PROVED-exact] or [NO VERDICT], never EMPTY-by-timeout", "d27 certificate or explicit [NO VERDICT]", "any NON-EMPTY -> [CANDIDATE-UNVERIFIED] -> HIT protocol -> possibly the counterexample"],
      "stop_rule": "d=8: 8 h per route; if all three routes fail, report NO VERDICT and do not proceed to d=9,10,11 (cost scales ~32x per level, so they are unreachable without a structural win). d=12 unsaturated: 24 h. d=27: certificate route only, 40 CPU-hours, then stop. Any run producing '[-1]' with a non-clean stderr is a FAILURE and is logged as such, never as a verdict.",
      "cost": "d=8: hours on the existing 4-core/15 GB box if the three routes are staged as sub-25-minute processes; a rented 64 GB box removes the staging burden for ~$20-60 total. d=12 unsaturated and d=27: rented 128 GB, budget 50-150 machine-hours.",
      "p_hit": 0.02,
      "why_not_already_closed": "d=8 chart N was exported and simply never launched (MISS-2) — the four prior d=8 attacks were all on the now-VOID printed GGV (1.2) system, and all four TIMEOUT with clean stderr. The corrected system is smaller (30/23 vs 39/30) and nobody ran it before the record ended. d=12 unsaturated has never been attempted in the corrected unsaturated form at all. Both are pure sequencing losses to container death, not mathematical resistance."
    },

    {
      "id": "L5",
      "title": "The witness lane — the only lane whose success mode is a counterexample, not a proof",
      "target": "The seed-pinned pentagon twin: wave6/pentseed/seed0_p1000003.ms (267 eq / 148 unk unreduced) and its sound linear fixed point seed0_p1000003_lin.ms (241 eq / 123 unk, degree profile {2:72, 3:54, 4:115}); plus the pentagon level-16 branches 1 and 2 that survive with an explicit char-0 witness (l16/breakthrough/PENTAGON_LEVEL16.md, PENTAGON_LEVEL15_BRANCH2.md); plus any NON-EMPTY handed over by L2/L4.",
      "premise_with_citations": "Certificates prove EMPTY; they never find a counterexample. Only a search can. Three recorded facts make this lane non-vacuous. First, the admissible bottom-edge seed 'survives every purely linear consequence (fixed point 241 eq / 123 unknowns, no inconsistent linear block, no equation collapsing to a nonzero constant). This cannot prove survival, only absence of a linear obstruction' (p11/CATCHES.md:1622-1650) — a weak positive signal, and one of the very few positive signals anywhere in the archive. Second, the seed run's fate was exit 124 at 5400.63 s with 9.53 GB RSS and a 0-byte .out — a TIMEOUT, explicitly not EMPTY (p11/wave6/pentseed/seed0.log); nothing was ever decided about it. Third, pentagon level 16 branch 2 survives level 15 generically with kappa and d0 determined, and branch 1 is alive with an explicit characteristic-zero witness, with the a0=0 and F3=0 exceptional subbranches undecomposed (l16/breakthrough/PENTAGON_LEVEL16.md). Crucially, this lane must NOT use numerics: multi-start Newton is PROVEN blind at these sizes — planted roots at residual exactly 0 were not found at 25 unknowns (ladder d=8) or 165 unknowns (pentagon) — so all 'empty floor' numerical evidence is retracted and a numerical MISS says nothing (canon/CATCHES.md 05:45Z, wave6/w6_plantctl.py). And any detector must fix all three gauges and use an ABSOLUTE normalization: both historical false-positive 'hits' were gauge artefacts (canon/STATUS.md §6.6). Also unresolved and load-bearing here: the four invisible bottom-edge seeds — the Galois orbit structure of the degree-9 bottom-edge eliminant is NOT established (the single-orbit claim was retracted 15 minutes after it was made, when p=1000081 gave admissible counts 1,1,0,2,3), so testing one admissible seed may decide only its own orbit (p11/CATCHES.md:2037-2065).",
      "method": [
        "1. Close the orbit gap cheaply and exactly first: FACTOR the degree-9 bottom-edge eliminant over Q directly (char-0 factorization in Singular/PARI; the char-0 bottom edge already solves in 316 s), instead of inferring orbit structure from Chebotarev averages over 13 primes. This tells you how many independent admissible seeds must be tested — the difference between one run and five.",
        "2. Continue the exact coupled level ladder downward from level 16 to levels 14 and below on branches 1 and 2, retaining ALL kernel constants, keeping the W-variable coupling (W9 = g9 - (3c1/2c0)z^4 h5 and W8 = g8 - (3c1/2c0)z^4 h4 — dropping the coupling and using g9/g8 manufactures a false level-16 resonant obstruction; that is exactly how the sigma^6|h7 wall arose), and always applying the bounded-support end check (deg W8 <= 11, z^12 coefficient must vanish — omitting it yields spurious solutions that pass every divisibility and resonance test).",
        "3. Decompose the a0=0 and F3=0 exceptional strata from the four emitted polynomials. These are the places a solution can hide from a generic-branch argument.",
        "4. On the reduced 241/123 system: after L2's bilinear projection, the d-block is small enough for exhaustive or branch-and-bound enumeration over a small field F_q; do that instead of Newton. Every candidate is then Hensel-lifted (canon/lift/lift_pipeline.py: p^(2^K) + half-extended-Euclid rational reconstruction + exact substitution over Q) and labelled CANDIDATE-UNVERIFIED.",
        "5. Every candidate passes the full HIT protocol before any claim: H1 exact rational coefficients only, no floats anywhere on the decision path; H2 Keller; H3 non-injectivity; H4 an independent resultant route; H5 gauge invariance; H6 non-vacuity (canon/reruns2/w3_hit_protocol.py). Additionally require, per the false-positive post-mortem, an ABSOLUTE normalization and 'allowed coefficients are O(1)' as an ACCEPTANCE CONDITION, not a post-hoc check.",
        "6. Bijectivity cross-check on any survivor via wave6/w6_bijcheck.py, then exact substitution back into the original (72,108) polygon data."
      ],
      "agent_roles": ["Structurist (level ladder continuation, coupling and bounded-support invariants — this agent's single job is to not repeat the sigma^6|h7 mistake)", "Search-Engineer (branch-and-bound over the reduced d-block; strictly exact, no floating point permitted in the tool allowlist)", "Witness-Auditor (owns H1-H6 and has unilateral veto; must fix all three gauges before evaluating anything)", "Adversarial Verifier (plants a known solution into the search and requires it be FOUND before any negative result is even recorded as NO VERDICT)"],
      "inputs": ["p11/wave6/pentseed/seed0_p1000003.ms", "p11/wave6/pentseed/seed0_p1000003_lin.ms", "p11/wave6/w6_pent_lineloop.py", "canon/wave6/bottomedge/be_c2is1_q.ms", "canon/wave6/bottomedge/elim_roots.json", "canon/wave6/bottomedge/analyse.py", "l16/breakthrough/pentagon_level16.py", "l16/breakthrough/pentagon_level15_branch2.py", "canon/lift/lift_pipeline.py", "canon/reruns2/w3_hit_protocol.py", "wave6/w6_bijcheck.py"],
      "outputs_with_labels": ["ORBIT_STRUCTURE.md -> exact factorization of the degree-9 eliminant over Q [PROVED-exact], closing a gap the archive explicitly left OPEN", "LEVEL14/13 branch reports -> [PROVED-exact] per level, or a surviving stratum [LIVE]", "any candidate -> [CANDIDATE-UNVERIFIED] until H1-H6; only after all six -> [COUNTEREXAMPLE, PROVED-exact]", "search exhausted with planted control passing -> [EMPTY over F_q on the reduced d-block], evidence only, never char-0"],
      "stop_rule": "Absolutely no numerical multi-start, at any size, for any purpose. Kill the level-ladder branch when a level's exceptional strata exceed 8 branches without any dying, or after 30 agent-hours. Kill the enumeration when the reduced d-block exceeds 10^9 field points. A search that finds nothing is [NO VERDICT] about emptiness and must be reported that way — a MISS says nothing at these sizes.",
      "cost": "Step 1: minutes. Steps 2-3: symbolic sympy, minutes per level, days of agent attention — this is thinking time, not machine time. Step 4: needs L2's projection first; then hours on the 4-core box. Total: ~5 agent-days, negligible compute.",
      "p_hit": 0.015,
      "why_not_already_closed": "The seed run died at a timeout and was never restarted; the linearly reduced twin's .out is 0 bytes and no verdict was ever recorded. The level ladder was still descending when the record ends. The orbit-structure question was 'closed' and then retracted 15 minutes later and left open. And the campaign's most natural search tool — multi-start Newton — was correctly retracted, which removed its only search lane and left it with nothing but emptiness-proving engines."
    },

    {
      "id": "L6",
      "title": "Kill the bad-prime objection outright: a certified-lucky-prime pipeline (why a mod-p unit GB can be an artifact, and the exact procedure that rules it out)",
      "target": "Every mod-p EMPTY in the inheritance: case (2) at 65521/32003/65537 and the fresh re-runs; the whole B=16 chart-Z ladder d=8..12 at 1-2 primes; the 20 above-125 shapes EMPTY at the single prime 65521; the F3 x2 single-prime 65521 result (MISS-6); and above all the README's claimed 'independently reproduced unit Groebner basis at p=1000003' on the bottom-seed target, which NO local artifact reproduces.",
      "premise_with_citations": "WHY A MOD-P UNIT GB CAN BE AN ARTIFACT, precisely. A unit basis mod p says 1 lies in the ideal generated by the REDUCED generators over F_p. It does not say 1 lies in I over Q. The clean witness: I = (p*x - 1) over Q has the perfectly good solution x = 1/p, but reduces mod p to the constant -1, i.e. a unit — EMPTY mod p, non-empty over Q. Generally, a Q-bar solution vanishes mod p exactly when p divides a denominator of that solution or degenerates the leading-term structure of the ideal (an unlucky prime, in the Arnold/Pauer sense: the leading-term ideal of the reduction strictly contains the reduction of the leading-term ideal). The archive already knows the rule empirically — 'Modular emptiness is unsound for contradictions' and 'three agreeing primes is strong evidence, never a char-0 proof' (canon/OPEN_ITEMS.md §5) — and it knows the specific unfixed hole: 'Good-prime selection is unjustified. Route-2 primes 32003/65537 are ≡ 2 (mod 3) and this is still UNCHECKED, and no script certifies that the chosen primes avoid degeneration of the ideal's leading terms' (canon/MANIFEST.md §D item 2, H1f). Multi-prime agreement narrows but never closes the hole, because the bad set is finite but with no effective bound absent a char-0 computation. There are exactly two rigorous exits and both are implementable: (A) an a-posteriori luckiness certificate — lift the modular GB by CRT + rational reconstruction and then CHECK over Q that the lifted basis is a Gröbner basis of I (all S-polynomials reduce to 0) and that every input generator reduces to 0 modulo it; if both hold, p was lucky and the result is a char-0 theorem. (B) for the unit case specifically, exit (A) degenerates into the cofactor identity 1 = sum h_i f_i over Q — which is L1. So the bad-prime problem and the char-0 problem are the SAME problem, and the certificate is the common solution. There is also a real cheap partial: a lucky-prime screen by majority leading-term signature across a batch, which detects (though does not disprove) degeneration.",
      "method": [
        "1. Implement luckyscreen.py: for a fixed system, compute the GB mod each of >=12 primes ≡ 1 mod 3 spread across bit-sizes (16-bit: 65521, 65539, 65599; 20-bit: 1000003, 1000033, 1000039, 999983, 1000081, 100153; plus 3 fresh 30-bit primes), record the leading-term ideal signature per prime, take the majority, and DISCARD minority primes as unlucky. Record the batch and the majority signature alongside every EMPTY. This is the fix the archive itself specifies and never wrote.",
        "2. Implement liftcheck.py: CRT + rational reconstruction of the majority-signature basis, then exact verification over Q (all S-polys reduce to 0 modulo the lifted basis; every input generator reduces to 0). On success, emit [PROVED-exact] with the verification transcript; on failure, emit [NO VERDICT] and raise the prime count.",
        "3. For unit outputs, route straight to L1's cofactor extraction — that IS the a-posteriori check and it is far cheaper than a full GB verification.",
        "4. Structure it as a prime FARM for the restart-hostile box: one prime = one independent process sized to finish under 25 minutes, writing its result and signature to a per-prime file, so a container restart costs one prime and never the run. Push after every result (the tree rolled back twice on restarts). This is the correct architecture for a 4-core / 15 GB / 30-min-restart box; a rented 64-128 GB box just runs more primes at once.",
        "5. Re-run the specific offenders: case (2) at two more primes ≡ 1 mod 3 (its recorded verdict used two primes ≡ 2 mod 3, violating the campaign's own hygiene rule); the F3 x2 single-prime 65521 result; the d=9,10,11 chart-Z single-prime results; the 20 above-125 shapes at 65521.",
        "6. Reproduce or void the README's 'unit GB at p=1000003' on the bottom-seed target. If it reproduces, hand it to step 3 for the cofactor lift — that would turn the strongest inherited asset into a char-0 theorem in one step. If it does not reproduce, the README claim is void and must be struck.",
        "7. Standing guards on every job: capture stderr always; treat '[-1]' with any parse/read error as a FAILURE, never a verdict; reject any input with a constant generator; verify no coefficient has |c| >= p or c ≡ 0 mod p; poll disk and inode headroom, not just RSS (a 98%-full disk once killed the W=10 eliminator with ENOSPC and no verdict); and never pgrep -f / pkill -f on your own script name (it killed the invoking shell four times) — use a pgrep -x loop."
      ],
      "agent_roles": ["Prime-Farmer (owns the farm, the queue, the per-prime checkpointing and the push-after-every-result discipline)", "Certificate-Engineer (liftcheck.py, cofactor route)", "Adversarial Verifier (constructs a deliberately unlucky prime for a toy system with a known 1/p-denominator solution and confirms luckyscreen.py flags it — a positive control for the screen itself, since the archive already shipped three can't-fail certifiers)", "Ledger scribe (every EMPTY in the final write-up carries its prime batch, majority signature, and either a certificate hash or the words 'mod-p only')"],
      "inputs": ["canon/MANIFEST.md §D item 2 (the H1f gap this lead closes)", "canon/campaign/audit_tracks/CASE2_VERDICT.md", "canon/campaign/audit_tracks/ABOVE_125_STATUS.md", "canon/STATE_FULL.md §A and §C", "/home/user/jacobian_planar/README.md:19-20 (the unreproduced unit-GB claim)", "msolve 0.10.1 built from source; Singular 4.3.2 (both absent on a fresh container — apt-get first)"],
      "outputs_with_labels": ["LUCKY_PRIME_LEDGER.tsv -> per system: prime batch, majority leading-term signature, discarded minority primes [audit artifact]", "Any system passing liftcheck -> promotion from [EMPTY mod p] to [PROVED-exact]", "Any system failing -> explicit demotion notice; the mod-p verdict stands as evidence only", "UNIT_GB_1000003_REPRODUCTION.md -> reproduces the README claim [CERTIFIED] or voids it [audit finding]"],
      "stop_rule": "A system gets at most 12 primes and one lift attempt. If liftcheck fails at 12 primes, stop and report the mod-p result as evidence only — do NOT keep adding primes hoping to reach a proof, because no finite number of primes ever constitutes one. Discard from the batch any prime whose signature is in the minority and say so in the ledger.",
      "cost": "Build: ~1.5 agent-days. Compute: the farm is exactly the workload the current 4-core / 15 GB / 30-min-restart box CAN host — the whole point of the architecture. Budget ~40 core-hours for the full re-run of every single-prime and hygiene-violating result. Rented box optional, would compress wall time roughly 8x for ~$30.",
      "p_hit": 0.005,
      "why_not_already_closed": "The gap is named verbatim in the campaign's own audit as unfixed (MANIFEST §D item 2: 'no script certifies that the chosen primes avoid degeneration of the ideal's leading terms') with the fix already specified. It was never implemented because the campaign's compute box could not host long GB runs at all and every hour went to attempting verdicts rather than certifying them. The prime-farm architecture is also the specific thing the box is GOOD at and the campaign kept scheduling monolithic runs instead — its own retrospective says so: 'Do not schedule monolithic Groebner runs on this container.'"
    },

    {
      "id": "L7",
      "title": "The (9,27) slivers and the GGHV Cor 5.7 repair — the one place where paper work can beat compute",
      "target": "The two p108 (9,27) sliver systems (rank-5 torus, sliced; smallest is p108_525122 at 25 params / 140 conditions, of whose five leaves three are already EMPTY and two remain), and the literature step they exist to test: GGHV Corollary 5.7.",
      "premise_with_citations": "The (9,27) orientation is killed in the literature ONLY by GGHV Cor 5.7, which nobody has ever re-derived (canon/STATE_FULL.md §B). The campaign then found the step BROKEN, precisely and by hand: 'GGHV Cor 5.7 is UNPROVEN: the proof of (5.12) applies [1, Cor 7.2] (standing hypothesis [P,Q] in K^x) to (psi phi P, psi phi Q), whose bracket is 1/2 + (lambda/2)x^{-1/2}, not in K^x, with lambda != 0 forced by (0,18) in N(P). Of the 66 coefficient conditions (5.12) asserts, the proven claim delivers 15. Failure localized to gghv.txt:1430-1433' — a [PROVED-exact] hand verification of a literature gap (canon/WEEKEND_PLAN.md §P3). The independent literature reader confirms the field is silent: no published erratum, follow-up, or re-derivation of Cor 5.7 exists, so 'the campaign's reopening of the (9,27) orientation is defensible'. The compute side is a timeout, not a verdict: p108_525122 timed out at 1800 s on a worker. And the systems have exploitable structure — rank-5 torus, already sliced, with three of five leaves closed. This is the highest ratio of consequence to compute anywhere on the board: if Cor 5.7 cannot be repaired, an entire orientation of the last surviving sub-125 degree pair re-enters the search space, and if the slivers turn out NON-EMPTY that is a counterexample candidate on the pair everyone believes is closed.",
      "method": [
        "1. Paper lane, zero compute: attempt the repair of (5.12). Either (a) establish [psi phi P, psi phi Q] in K^x by a different route so [1, Cor 7.2] applies, or (b) derive the remaining 51 of 66 coefficient conditions by other means, or (c) construct a counterexample to (5.12) as stated, which would be a publishable erratum in its own right and would fully reopen (9,27). Read canon/papers/2204.14178.pdf §5 in full; the failure is localized to gghv.txt:1430-1433.",
        "2. Note that GGHV is UNREFEREED (arXiv:2204.14178, v1 only) and that the refereed floor is Nguyen's 104 (Quaestiones Mathematicae 48(2) 2025, arXiv:1902.05923) — with a caveat that one independent reader could not locate the Nguyen paper and recommends citing GGHV's 108 until it is. Everything in [105,124] rests on unrefereed work regardless.",
        "3. Compute lane: exploit the rank-5 torus. Quotient by the torus action to eliminate 5 parameters exactly before any elimination, taking care that the quotient does not lose the saturation conditions. 25 params minus a rank-5 torus is 20 — a size where msolve -g 2 is genuinely comfortable.",
        "4. Run the two surviving leaves of p108_525122 with `msolve -g 2` at 3 primes ≡ 1 mod 3, on a persistent box with a 6-8 h budget rather than the 1800 s that produced the timeout, and in parallel with Singular facstd (factorizing GB, which suits the sliced/leaf structure).",
        "5. Then the second sliver shape, which was only ever queued and never run.",
        "6. Promote to char 0 with L1's certificate machine and L6's lift check. A mod-p EMPTY here would be the first independent evidence for Cor 5.7's conclusion, but only a certificate makes it a theorem.",
        "7. Non-negotiable orientation check: J(Q,P) = -J(P,Q) makes (72,108) and (108,72) ONE territory [PROVED-exact, wave0/w0_h1a_swap_and_G.py], and the reproduction shows corner A0=[8,1,28] appears twice — with (m,n)=(3,2) giving (108,72), max 108, the sub-125 survivor, and with (m,n)=(3,4) giving (108,144), max 144, which is ABOVE 125. Confirm which row every (8,28) artifact belongs to before spending a core; the independent literature reader flags this as a medium-severity conflation that could misroute effort."
      ],
      "agent_roles": ["Literature-Prover (owns step 1; reads §5 of the paper end to end, writes the repair or the counterexample — no compute, highest leverage per hour on the board)", "Structurist (torus quotient, leaf handling)", "Prime-Farmer", "Certificate-Engineer", "Adversarial Verifier (checks the torus quotient preserves the solution set, with a planted point)"],
      "inputs": ["canon/papers/2204.14178.pdf §5 and gghv.txt:1430-1433", "canon/WEEKEND_PLAN.md §P3", "canon/MORNING_SUMMARY.md", "the p108_* systems (p108_525122 and the second sliver shape)", "canon/gghv_audit/all_cases_max_le_150.json and gghv_audit/ggv_algorithms.py (the independent re-implementation reproducing 34/34 published cases and 10/10 GGHV rows)"],
      "outputs_with_labels": ["COR_5_7_REPAIR.md -> either 'Cor 5.7 repaired, (9,27) legitimately closed' [PROVED-exact], or 'Cor 5.7 refuted as stated' [PROVED-exact] — the latter reopens an entire orientation and is a genuine literature result", "p108 leaf verdicts -> [EMPTY mod p, k primes] then, with a certificate, [PROVED-exact]", "Any leaf NON-EMPTY -> [CANDIDATE-UNVERIFIED] -> L5's HIT pipeline", "ORIENTATION_LEDGER.md -> which (8,28) row every artifact belongs to [audit finding]"],
      "stop_rule": "Paper lane: 4 agent-days, then write up whatever state the repair is in (partial progress on a literature gap is itself a deliverable). Compute lane: 8 h per leaf per prime, 3 primes; then NO VERDICT. Do not extend to the full 140-condition unsliced system — the slicing is what makes this tractable.",
      "cost": "Paper lane: ~4 agent-days, zero compute, and it is the single cheapest high-consequence item in this whole plan. Compute lane: ~50-100 core-hours; comfortable on a rented 64 GB box (~$30-80), impossible on the 30-minute-restart container without the per-leaf checkpointing from L6.",
      "p_hit": 0.02,
      "why_not_already_closed": "The break in Cor 5.7 was found late, in the weekend-plan window, and promoted (9,27) to first-class only at the very end of the record. Shape 1 was still in msolve at an 1800 s budget and shape 2 was merely queued when the campaign stopped. No repair attempt on the paper was ever started, and no torus quotient was ever applied to the slivers."
    }
  ],

  "audit_gates_before_compute": [
    "G1 — LOCATE OR VOID THE HEADLINE ASSET. The README's '164 variables / 288 quadratic equations / 6,821 terms over Q(alpha), degree five' does not exist in main, docs/, archives/transfer/state_transfer.tgz, or any of the seven local worktrees; greps for those figures return nothing. The nearest real object is wave6/frontier/trackB1_sat_Q.ms at 166 vars / 284 eqs / degree 5 / 8,774 coefficients / max integer coefficient 468. The numbers disagree, 'quadratic' contradicts 'degree 5', and 'degree five' is probably a conflation of total polynomial degree with a field degree. Fetch branch claude/opus-5-counterexample-plan-sep6yk (head 24a06fc), count the file header directly, and publish a reconciliation before a single core is spent. No compute on an object whose dimensions nobody can state.",
    "G2 — THIRD-PRIME LIFT CHECK. trackB1_sat_Q.ms was reconstructed by a common integer lift from exactly two primes (1000003, 65521) and verified by re-reducing to those same two. That is not independent verification — it is the definition of the construction. Regenerate the export at a third prime (100153 or 999983) from the generating script and compare monomial-for-monomial against the reduction of the char-0 file. Until this passes, every char-0 statement about this object is about an unverified object.",
    "G3 — REPRODUCE OR STRIKE THE UNIT GB. The README asserts an 'independently reproduced unit Groebner basis' at p=1000003 for the headline asset. Nothing in any local worktree records a unit GB at that prime for that system; the closest local p=1000003 runs are a 5400 s timeout with a 0-byte output and a four-entry ledger of NO VERDICTs (one OOM at 13.9 GB, three timeouts including a 3 h run). Reproduce it under L6's guards or strike the claim. A plan built on an unreproduced unit GB is a plan built on nothing.",
    "G4 — VOIDNESS SWEEP. Everything built on the misprinted GGV (1.2) is void as a statement about B=16: the earlier d=12 kills, the b16_d8_*.ms exports, the d=12 'two rational seeds' bookkeeping. Only corrected-system (m16_*) artefacts count. Enumerate every input file for every lead and mark it corrected or void before use.",
    "G5 — PARSE-TRAP SWEEP on every .ms about to be run: reject any file containing a constant generator; assert every coefficient satisfies |c| < p and c != 0 mod p (sympy substitution does NOT reduce mod P — 3,921 of 8,264 coefficients once exceeded P and 8 were exactly 0 mod P). Always capture stderr. '[-1]' plus any parse or read error is a FAILURE, never a verdict.",
    "G6 — CONTROLS BEFORE VERDICTS, per engine, per formulation, no exceptions: a planted solution must be FOUND, and a provably empty input of the same shape must return a unit. The archive shipped three can't-fail certifiers and carries 45 hardcoded-True checks (42 previously unrecorded, 19 inside inherited certifiers); a [PASS] inside a docstring is prose, not a computation. Note also that planting a generic constant shift into a RAW dim-1 system destroys sparsity and never finishes — plant into the pinned zero-dimensional chart instead.",
    "G7 — CLOSURE LEMMA RE-DERIVATION. Before any truncation verdict is allowed to kill pentagon case (1), independently re-derive that truncate(W) builds a closed subsystem (every full solution restricts to a truncation solution). The whole value of the smallest live object rests on this one lemma.",
    "G8 — ORIENTATION LEDGER. J(Q,P) = -J(P,Q) makes (72,108)/(108,72) one territory, but corner A0=[8,1,28] appears in the enumeration twice: (m,n)=(3,2) -> (108,72), max 108, the sub-125 survivor; (m,n)=(3,4) -> (108,144), max 144, above the frontier. Tag every (8,28) artifact with its row before spending compute, or effort will be misrouted between two different regions.",
    "G9 — CITATION HYGIENE for the write-up: GGHV arXiv:2204.14178 is UNREFEREED (v1 only) and the 'Compositio Math 160 (2024)' citation for it is wrong, so every closure resting on GGHV is CONDITIONAL; the window [105,124] rests entirely on unrefereed work. One reader could not locate the Nguyen 104 reference and recommends citing GGHV's 108 until it is found — though a prior campaign retraction says Nguyen is real and refereed and that three web searches simply missed it. Resolve before publishing a floor. Do not cite Zoladek (not located). Do not cite any TRUST_MAP ABSENT verdict (ruled WRONG-TREE and void).",
    "G10 — ENVIRONMENT. A fresh container has NO Singular and NO msolve and every campaign script assumes Singular on PATH: apt-get install singular (4.3.2) first, and BUILD msolve 0.10.1 from source (the apt 0.6.5 candidate was rejected). Watch disk and inode headroom as well as RSS — a 98%-full disk once killed the W=10 eliminator with ENOSPC and no verdict. Never pgrep -f / pkill -f on your own script name; use a pgrep -x loop. ps -o rss reports KiB — divide by 1048576, an earlier unit slip nearly produced a false OOM alarm. Push after every commit: the git tree rolled back twice on restarts and 37+ local commits were unpushed when the record ended."
  ],

  "risks": [
    "The dominant risk is that all of this is true and empty. Every exact structural result points to EMPTY: the rank obstruction holds at d=3..15,18,20,27; conditions exceed unknowns by a growing ~d margin; T2 killed 378 Keller branches over 230 weight cells exactly over Q with zero non-automorphisms; T3 killed 1140/1140 mu_n cells. The plan's realistic product is proofs, not a counterexample, and the write-up must be honest about that from the first line.",
    "The headline asset may not exist. If G1 shows the README object was a handoff-time conflation of trackB1_sat_Q.ms with the pentagon-seed narrative, then L2's target shrinks to a system that four Gröbner attacks already failed on, and the strongest inherited asset is weaker than advertised.",
    "The Nullstellensatz degree can be astronomically large. Effective Nullstellensatz bounds are doubly exponential in the worst case; if the true D exceeds ~12 for any of these systems, the graded linear system explodes and the certificate route silently fails to terminate — producing NO VERDICT, which must not be reported as anything else. The grading is the only thing standing between this method and combinatorial death, and if the L-grading turns out not to separate the blocks as cleanly as the archive's census suggests, the whole certificate lane degrades.",
    "The bilinear projection in L2 can be invalid. If even a handful of the 284 equations are not bilinear in the c/d split, the minor-ideal reformulation is not equivalent and any EMPTY it produces is about the wrong variety. The 'bilinearity of every monomial' claim is verified in the archive but was verified about a specific formulation; re-verify on the exact file used.",
    "The torus quotient in L7 and every gauge fix everywhere can silently change the problem. The campaign's own auditor's chart gauge-fixing was found UNSOUND — the claimed scaling symmetry does not exist, because the 2*mu3*q1''(0) term in (1.2) row 3 is inhomogeneous under every continuous scaling, and both published GGV controls have b2 = 0 so neither exercised it. Verdicts were demoted to slice results as a result. Any symmetry reduction in this plan must be proved, not assumed.",
    "Agent-fleet risk. Certificate verification is the only step that is genuinely trustworthy, and it is trustworthy only if no agent is allowed to report a verdict without it. The archive's failure mode was not bad math, it was drift: 45 hardcoded-True checks, three can't-fail certifiers, and 'four primes read as a pattern only because a pattern was wanted'. Give the Adversarial Verifier a unilateral veto and never let the agent that computed a result be the agent that certifies it.",
    "Infrastructure risk is quantified and severe on the current box: ~13.3 GiB cgroup cap, restarts roughly every 30 minutes, a measured ~2.5 h hard process-lifetime ceiling, no swap, and no solver preinstalled. Nothing needing more than ~20 minutes or ~12 GB can finish. L1/L3/L6 are designed around this; L2/L4/L7 at full size are not, and pretending otherwise will just regenerate the archive's ledger of timeouts.",
    "Duplicate-effort risk: dedup-by-hash across registers is still pending, and the tail-closure predictor suggests reduced systems depend only on the chain TAIL, so several 'distinct' targets may be the same system. Hash every exported system before queueing it."
  ],

  "what_would_change_my_mind": [
    "A verified cofactor certificate at any of the four small objects would raise my confidence in the whole method from ~50% to ~90% and would justify immediately buying the 128 GB box for L2 and L4. Conversely, if the graded linear system at W=19 (21 eq / 27 unk — the smallest live object in the entire campaign) is already intractable, the certificate thesis is wrong and every lead that depends on it should be abandoned in one move.",
    "If G1 finds the 164/288/6,821 object and it is genuinely a different, smaller system than trackB1_sat_Q.ms, the priority order inverts: that object becomes lead #1 and L2's bilinear projection may be unnecessary.",
    "If G3 reproduces the p=1000003 unit GB on the headline asset, the entire plan collapses to one step — extract the cofactors and lift them — and I would drop L2's projection work entirely and put every agent on L1 + L6 against that single object.",
    "A NON-EMPTY at any screened prime on the B=16 ladder (d=8 chart N, or d=12 unsaturated) would be the strongest signal on the board, because GGV Thm 1.2 is an IFF: such a solution with mu_0 != 0 would BE a counterexample. That would move P(counterexample) from ~2% to well above 30% and I would stop every emptiness lane and put the whole fleet on lifting and the HIT protocol.",
    "A refutation of GGHV Cor 5.7 as stated — not merely 'unproven' but false — would reopen a whole orientation of the last surviving sub-125 pair and make L7 the top lead outright, ahead of everything else here.",
    "If the L-grading turns out not to block-diagonalize the certificate system (i.e. the constant 1 receives contributions from many weight blocks because the generators are not weight-homogeneous), the thesis loses its main computational lever and I would fall back to L6's certified-lucky-prime pipeline as the primary — which yields weaker, batch-of-primes-conditional statements, and I would say so plainly.",
    "If the truncate(W) closure lemma fails re-derivation at G7, W=19 stops being a kill of pentagon case (1) and the cheapest high-value target on the board evaporates; L1's application list would then start at the d=8 chart-N system instead."
  ]
}
```