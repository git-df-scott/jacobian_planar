```json
{
  "angle": "compute engineering and fleet operations",

  "thesis": "Nothing on this board died to mathematics. The open queue died to a 4-core/15 GB container that restarts every 30-50 min with no Singular and no msolve installed (toolchain reader gaps #1; open-queue 'Infrastructure'; MANIFEST sec C), to msolve modes that write '[-1]' and exit 0 on a parse error (CATCHES 'NEW msolve SILENT-LIE MODE'), to three rational-function cascades that failed their own self-tests (CATCHES 'CASCADE ATTEMPT #3'), and to a ledger that never carried prime lists or content hashes, so 49 TIMEOUTs collapsed to 16 unique systems only after the fact and p108_821326/p108_843700 were md5-identical and double-counted (CATCHES Sec 1/2.3). The correct move is therefore not more mathematics first: it is to build a provisioned, persistent, restart-resilient compute plant with a machine-readable verdict schema and automated gates, then re-run the whole undecided set through it smallest-cell-first. Two structural facts make this cheap and high-yield. (1) The single biggest evidential upgrade available anywhere is mechanical: EMPTY-mod-p never promotes to char 0 by campaign rule (MANIFEST proof-standard table), and 46 EMPTY verdicts are permanently stuck there — but a certified modular-GB + CRT + rational-reconstruction + exact-ideal-membership pipeline turns each into a checkable char-0 proof (1 in the reconstructed ideal), and that pipeline is ~300 lines and needs no new mathematics (toolchain gap #7). (2) The largest genuinely unsearched region, the 429 NO-CHAIN records, is blocked by a compiler, not by cores, and its cost may collapse by two orders of magnitude if the tail-closure predictor saturates (34 chains -> 26 distinct tails, zero predictor violations; CROSSDOOR §5) — a test that is 20 sample compilations, not a sweep. A counterexample, if it exists in reach, comes out of exactly two doors: the pentagon trackB1 system (a NONEMPTY there IS an admissible point of case (1); TARGET_QUEUE 'nothing else here is meaningful'), and the B=16 ladder chart-N cells at d >= 8. Both are memory-bound, never mathematics-bound: W=19 died to Singular 'no more memory' and to msolve at the 13.3 GiB cap; pent_L23 exit 137 at 13.9 GB; the 166-var char-0 system OOM'd at 13.9 GB in 18 min. A 128 GB host with a serial one-heavy queue is the difference between a NO-VERDICT ledger and a verdict.",

  "leads": [
    {
      "id": "ENV-1",
      "title": "Provision a pinned CAS image plus a persistent 128 GB heavy host, and make the job runner restart-resilient by construction",
      "target": "Make any run over 20 minutes or 12 GB finish at all. Nothing in the queue below is executable until this exists.",
      "premise_with_citations": "A fresh container comes up with NO Singular and NO msolve; every campaign script assumes Singular on PATH (RESUME_STATE 'Environment gotcha'; toolchain gap #1 confirms `which Singular msolve` returns nothing, only python3 + gp). The box is 4 cores / 15 GB / no swap (MANIFEST sec C, confirmed live: nproc=4, free -g 15). Container drops every ~30-50 min, six occurrences, twice rolling the git tree back to an older commit (WEEKEND_PLAN 'Infrastructure'; CATCHES 'INFRASTRUCTURE NOTE'); measured hard process ceiling ~2.5 h wall (RESUME_STATE). Consequence: the exact-Q fallback charts carry a 6 h timeout they can never reach, and 37+ local commits were UNPUSHED at credential death so some results exist only in chat logs (status-map pitfalls). Every heavy failure is a memory failure at this cap: pent_L23 exit 137 at 13.9 GB with 0-byte output; L18_g3 SIGKILL at 6.2 GB; trackB1_sat 60-var eliminated form OOM at 13.9 GB in 18 min; pentagon seed job exit 124 at 5400 s / 9.53 GB (AGENT_MAILBOX Groebner ledger; pent/RUNLOG.tsv; p11 wave6/pentseed/seed0.log). Version pinning matters and drifted before: MANIFEST sec D item 6 pins msolve 0.10.1-from-source over an apt 0.6.5 candidate that was never installed, so older docs quoting 0.6.5 are wrong. The working pattern already exists in-tree and must be generalized, not invented: wave6/bottomedge/sweep.sh exports, runs `timeout 600 msolve`, analyses, then git add/commit/push BEFORE the next prime, so a drop costs one prime (~96 s) rather than the sweep — git history is the checkpoint file.",
      "method": [
        "1. Write provision.sh, idempotent and re-runnable on every container wake: install Singular 4.3.2p16 (apt or pinned build), build msolve 0.10.1 from source (NOT apt 0.6.5), pip-pin sympy 1.14.0, python-flint 0.9.0, gmpy2 2.3.1, PARI/GP 2.15.4. Emit an engine manifest (binary path, --version string, sha256) to engines.json.",
        "2. Add engine_guard.py imported by every runner: if a required engine is absent or its version string does not match engines.json, exit with status MISSING-ENGINE and write a FAILURE verdict record — never a generic nonzero rc, never a silent skip (toolchain fix #1).",
        "3. Stand up ONE persistent heavy host, 16 cores / 128 GB RAM / 500 GB disk, no restart policy, with the same image. Its only job is the four heavy cells (HEAVY-1). Cost sizing below.",
        "4. Write runner.sh generalizing sweep.sh to arbitrary jobs: for each job in the queue, (a) skip if content_hash already has a verdict in ledger/, (b) run under `timeout N` with `/usr/bin/time -v`, stdout and stderr tee'd to separate files, (c) classify via verdict.py, (d) write ONE verdict JSON, (e) git add ledger/ + logs, commit with the content hash in the subject, push. One verdict per commit, exactly as sweep.sh does per prime.",
        "5. Serialize the heavy lane structurally: a single-slot FIFO queue file, not a policy. The concurrency tripwire was policy-only and two OOM kills followed including the original chart-N run at 83 min (CATCHES Sec 2.1). Make cgroup limits explicit per job (MemoryMax) so an OOM is a clean labelled OOM rather than a random SIGKILL of a neighbour.",
        "6. Sweep DISK and inodes alongside RSS in the watcher. Disk hit 98% / 1.1 GB free mid-run and killed the W=10 eliminator with ENOSPC and no verdict; neither job monitor watched it, both runs were healthy on memory and CPU (CATCHES 'SWEEP, 18:47'; toolchain gap #10). Write verdicts to a separate small volume so an intermediate-data blowup cannot corrupt a decided result.",
        "7. Ban `pkill -f` in a pre-commit hook. It killed the invoking shell four times, once losing an uncommitted document; the safe idiom is a pgrep -x loop or stop-by-PID-handle (MANIFEST sec G.7, two witnesses; status-map pitfalls).",
        "8. Ship a smoke suite the runner must pass on every wake: a known-EMPTY system must return EMPTY in < 1 s in both engines, a known-NONEMPTY must return non-empty, a deliberately malformed .ms must produce FAILURE (not EMPTY), and a 3-second job must survive a simulated container restart mid-run and resume from the ledger."
      ],
      "agent_roles": ["librarian", "auditor/skeptic"],
      "inputs": ["wt/canon/wave6/bottomedge/sweep.sh", "wt/canon/run_all.sh", "wt/canon/MANIFEST.md sec C and sec D item 6", "wt/pq/RESUME_STATE.md", "wt/canon/WEEKEND_PLAN.md 'Infrastructure'"],
      "outputs_with_labels": [
        "provision.sh + engines.json — infrastructure artifact, no math label",
        "runner.sh + verdict.py — infrastructure artifact",
        "smoke-suite result — PROVED-exact only in the trivial sense that it certifies the plant, recorded as CERTIFIED(plant)"
      ],
      "stop_rule": "Stop when the smoke suite passes twice across a deliberate container restart and one real job (a known-EMPTY reproduction) lands a committed+pushed verdict without human intervention. If the persistent 128 GB host cannot be obtained within 3 days, STOP building for it and re-scope every heavy lead to degree-bounded staged probes under 12 GB (see HEAVY-1 step 6) rather than pretending the runs will finish.",
      "cost": "2 agent-days of build. Hardware: 1 persistent host, 16 vCPU / 128 GB / 500 GB SSD, ~14 days ≈ 350-500 USD at commodity cloud rates; plus the existing ephemeral 4-core box for light lanes at no marginal cost.",
      "p_hit": 0.00,
      "why_not_already_closed": "The campaign never had a persistent host, and BUILD.md was written onto a branch (claude/opus-support-toolchain-62st0d) that is not in any local worktree, so provisioning knowledge was lost at every container death. Nobody owned the plant; each session re-discovered that Singular was missing."
    },

    {
      "id": "LEDGER-1",
      "title": "One verdict schema with content hash, prime list and stderr digest — and the guardrail suite as blocking CI gates rather than prose",
      "target": "Make every future verdict machine-checkable, deduplicated by content, and impossible to fabricate from an empty output file. Retroactively re-key the existing 429/464/804/474 counts to one register.",
      "premise_with_citations": "The archive's own catches reader enumerates 27 error classes, of which at least nine are pure ledger failures. Verdicts were double-counted because p108_821326 and p108_843700 are md5-identical; 49 TIMEOUT records reduce to 16 unique systems by tail-hash; the paused pair108 sweep was re-running its own 4-minutes-earlier TIMEOUT under a different tag (621292 == 671059) (CATCHES Sec 1, Sec 2.3). Verdict records carry no prime list, so 'exactly one admissible bottom-edge seed' was quoted as a statement about the problem when it was a statement about p=1000003 (CATCHES 'THE SEED WORK IS THREE JOBS'). Prime hygiene was violated: the recorded case-(2) EMPTY used 32003 and 65537, both = 2 mod 3, against the campaign's own p = 1 mod 3 rule, forcing re-runs (pentagon reader pitfalls; MANIFEST sec D item 2 flags this still UNCHECKED). 45 hardcoded-True checks exist in the tree, 42 previously unrecorded, 19 in inherited certifiers, including the literal `check(\"k >= 1 forces c = 0...\", True, ...)` at w1_h1c_endgame_closed_form.py:89 (ADJUDICATION §1, §4.3; errors/STATUS_CORRECTION items 2-3). msolve exits 0 on timeout, crash AND parse error alike, so exit codes must be ignored entirely and a verdict read only from a non-empty output with clean stderr (CATCHES class 10, 11; toolchain pitfalls). sympy substitution does not reduce mod P: 3921 of 8264 coefficients exceeded P and 8 were exactly 0 mod P, and the SAME bug class produced the campaign's worst historical failure (commit 91f42f5; toolchain gap #5). Counts are unreconciled at head: frontier_151_300_map.json holds 432 records (429 NO-CHAIN), the coverage audit says 464 admissible in [125,300], Session 41 says 804, the independent gghv_audit re-derivation says 474 with max<=300, and FABLE-006 says the published enumeration has 34 total with 24 untouched — five different objects, never reconciled (tail-census; mailbox pitfalls, which explicitly disavow 804 as coming from lost Sessions 19-38).",
      "method": [
        "1. Define job.json and verdict.json (schemas below) and make them the ONLY way a result enters the record. Every verdict is one file, named by content_hash.",
        "2. content_hash = sha256 of the canonicalized generator list (sorted monomials, reduced coefficients, variable list, characteristic) — not of the file text, so cosmetic re-exports do not create new hashes. Refuse to schedule a hash already carrying a terminal verdict; a TIMEOUT/OOM record is NOT terminal and may be re-scheduled at a larger budget on a larger host.",
        "3. Write gate.py implementing every gate in the `gates` block below, and wire it into (a) the exporter, before any file is written, (b) the runner, before any verdict is emitted, and (c) CI over the whole tree. A gate failure blocks the commit; there is no override flag.",
        "4. Re-key the entire historical record: hash every .ms and .sing artifact on disk, build unique-system -> verdicts map, and publish RECONCILIATION.md that states, per count (429 / 432 / 464 / 474 / 804 / 34-with-24-open), what object it counts, which file it came from, and whether it survives. Mark 804 WITHDRAWN with its reason (no supporting artifact, lost Sessions 19-38).",
        "5. Backfill prime lists and, where recoverable, stderr digests onto existing EMPTY records. Any EMPTY that cannot be shown to have had stderr captured is demoted to EMPTY-mod-p-UNAUDITED and re-queued, not deleted.",
        "6. Enforce orientation as part of the record key. L is not a function of the degree pair: (72,108) gives L=3 and (108,72) gives L=4 (CATCHES class 23), and the campaign used the wrong case label for weeks — (9,27)/(2,3) vs (8,28)/(3,2) vs (8,28)/(3,4) at max 144 are three different objects (mailbox pitfalls). Every record carries corner A0, (m,n), and orientation.",
        "7. Add the contradiction linter: two records under one key with incompatible labels is a hard error (w3_claim_ledger.py L1 pattern). STATUS carried 'First Framework PROVEN dead, unconditional' and 'conditional on unreproduced THEOREM 2/3' simultaneously in two files (errors/STATUS_CORRECTION item 11)."
      ],
      "agent_roles": ["librarian", "auditor/skeptic", "exporter/sanitizer"],
      "inputs": ["wt/canon/CATCHES.md (all 27 classes)", "wt/errors/FAILURE_ANALYSIS.md (M1-M3)", "wt/canon/wave4/w4_msformat.py", "wt/canon/wave2/w2_cantfail_audit.py", "wt/canon/wave6/frontier_151_300_map.json", "wt/canon/gghv_audit/all_cases_max_le_150.json"],
      "outputs_with_labels": [
        "ledger/*.json — one verdict per unique system, each carrying an explicit label from the vocabulary",
        "RECONCILIATION.md — reconciles 429/432/464/474/804/34; every surviving count CERTIFIED, every withdrawn count labelled WITHDRAWN with reason",
        "gate.py + CI — FAILURE on any violation"
      ],
      "stop_rule": "Stop when gate.py runs green over the whole tree, the AST scan reports zero literal-True check conditions (it currently finds 45), and RECONCILIATION.md accounts for every one of the five circulating counts. If re-keying shows that more than 20% of existing EMPTY records lack recoverable stderr, stop backfilling and simply re-run them — the plant makes re-running cheaper than archaeology.",
      "cost": "2 agent-days build, ~4 core-hours to hash and re-key the tree, ~20 core-hours to re-run any demoted EMPTYs on the light box.",
      "p_hit": 0.00,
      "why_not_already_closed": "Every guardrail in the archive exists as prose in CATCHES.md with no enforcer. The campaign named this exact failure itself as class 24, 'Policy without an enforcer': the concurrency tripwire was policy-only and two OOM kills followed."
    },

    {
      "id": "CERT-1",
      "title": "The certified char-0 promoter: modular GB at k primes + CRT + rational reconstruction + exact ideal-membership verification",
      "target": "Convert 46 stranded EMPTY-mod-p verdicts into PROVED-exact char-0 statements. First targets: (72,108) case (2) at d_3_3=1 and d_3_3=0; B=16 ladder d=7 (currently single-prime); B=16 chart Z d=8..12.",
      "premise_with_citations": "The campaign's rule forbids promoting mod-p to char 0 and it is right to: trackA_eliminator.py --mod p closes branches with genuine rational solutions — x-3y=0, x+4y-z=0 with nonzero=[y,z] has the solution (3,1,7) over Q but reports '1 closed, 0 open leaves' mod 7, and --verify replays it OK, exit 0 (CATCHES class 8). So modular emptiness is unsound for contradictions and three agreeing primes is evidence, never proof (OPEN_ITEMS §5). But the lift already half-exists: lift/lift_pipeline.py does Hensel to p^(2^K) plus half-extended-Euclid rational reconstruction plus exact substitution — for isolated POINTS only. There is no lift for ideal-level verdicts, which is precisely why 46 EMPTYs are permanently short of char 0 (toolchain gap #7, which also names the fix). The pieces to finish it are all present: python-flint 0.9.0 and gmpy2 2.3.1 are installed and, per the toolchain reader, NO archive script imports them — unexploited fast exact arithmetic sitting idle. Prime selection is currently unjustified: no script certifies the chosen primes avoid degeneration of leading terms, and 32003/65537 are ≡ 2 mod 3 in violation of the campaign's own hygiene rule (MANIFEST sec D item 2, still UNCHECKED). The case-(2) char-0 run exists as a marker-resumable script (trackB_exactQ.py, QELIM_TIMEOUT default 10800 s) whose 6 h fallback timeouts the 2.5 h container could never reach — it is blocked purely on uptime (open-queue item 'Characteristic-0 confirmation of case (2)'). Precedent that this works: the degree-5 case-(2) w=-4 eliminant over Q was already reconstructed by CRT from 41 of 96 primes and reproduces msolve's eliminant at 6 held-out primes (HUNT_REPORT item 1c); and the char-0 system trackB1_sat_Q.ms was reconstructed by common integer lift from two primes with 284/284 equations and 8,774 coefficients verified by reduction back to both (AGENT_MAILBOX:250-260). The method is proven on this data; only the ideal-level emptiness case was never built.",
      "method": [
        "1. Lucky-prime certifier first. For a batch of k >= 8 primes with p ≡ 1 mod 3, compute the mod-p reduced GB and compare leading-term ideals; discard minority-signature primes and record the batch plus the majority signature alongside every verdict (toolchain fix #8). Feed control primes 5, 11, 17 in and REQUIRE them rejected — a selector that accepts everything is a can't-fail certifier of exactly the kind the campaign shipped three of.",
        "2. Compute the reduced GB at each surviving prime. CRT the coefficients across primes, then rationally reconstruct with the |r|,|s| <= sqrt(m/2) bound already implemented in lift/lift_pipeline.py. Add primes and repeat until the reconstruction is STABLE across two successive prime additions — instability is a stop, not a retry-with-more-primes-until-it-agrees.",
        "3. VERIFY EXACTLY over Q, and this is the whole point: check that each original generator reduces to 0 modulo the reconstructed basis G, and that G is a GB by Buchberger's criterion on its own S-pairs. Both checks are exact rational arithmetic in python-flint. Success = the char-0 ideal contains what G says it contains.",
        "4. For EMPTY specifically the certificate is far cheaper than a full GB: emptiness means 1 is in the ideal, so it suffices to find an explicit Nullstellensatz certificate 1 = sum_i h_i f_i with deg(h_i) <= D, by exact linear algebra over Q on the coefficient matrix at increasing D. That is a rank/solve problem, not a Buchberger run, and it is verifiable by a third party by one polynomial multiplication. Use the mod-p GB only as an oracle for the right D (start at D = the mod-p degree bound, walk up).",
        "5. Two-sided self-test before any real data: a planted system with a known rational solution must FAIL to produce a certificate at every D up to the bound, and a planted contradiction must produce one. Ship the tool failing rather than delete it — this is exactly the discipline that caught cascade attempt #3, which reported 57 dead branches on a system with the obvious solution (a=3,b=4,c=2) (CATCHES class 7).",
        "6. Run on case (2) both charts first (it is the campaign's central verdict and it is marker-resumable, so restarts cost a stage), then d=7, then chart Z d=8..12.",
        "7. Every output records: prime batch, majority signature, reconstruction stability step, D, and the certificate itself as a file a referee can multiply out."
      ],
      "agent_roles": ["prover", "certifier", "lifter", "auditor/skeptic"],
      "inputs": ["wt/canon/lift/lift_pipeline.py", "wt/pq/trackB_exactQ.py", "wt/pq/trackB_Q_elim.sing", "wt/canon/campaign/audit_tracks/CASE2_VERDICT.md", "wt/canon/STATE_FULL.md §A (ladder chart Z rows)", "python-flint 0.9.0, gmpy2 2.3.1"],
      "outputs_with_labels": [
        "case (2) both charts: PROVED-exact (char-0 Nullstellensatz certificate) if it lands, else EMPTY-mod-p(p1,...,pk) unchanged plus a recorded D-ceiling",
        "B=16 d=7: PROVED-exact, or EMPTY-mod-p(p1,p2) with the second and third primes that MISS-1 lost",
        "chart Z d=8..12: EMPTY-mod-p upgraded from 1 prime to k primes, or PROVED-exact",
        "certificate files — independently checkable by polynomial multiplication"
      ],
      "stop_rule": "Per target: stop when the exact certificate verifies (PROVED-exact) or when D exceeds the point where the linear system no longer fits in 128 GB — record the D-ceiling reached and label EMPTY-mod-p(k primes) with that ceiling attached. Do NOT let a failed reconstruction become 'probably empty' prose. If the reconstruction is unstable across primes on case (2), STOP and treat that as a signal the mod-p EMPTYs may be reduction artifacts — that is a finding, not a failure.",
      "cost": "3 agent-days build. Compute: case (2) ~200 core-hours on the heavy host; d=7 ~50; chart Z d=8..12 ~150. Total ~400 core-hours, i.e. ~1.5 days of the 16-core host.",
      "p_hit": 0.01,
      "why_not_already_closed": "The lift pipeline was built for points, not ideals, and the campaign correctly refused to promote mod-p EMPTY without one — so it simply left 46 verdicts stranded rather than building the 300 lines that would promote them. The char-0 case-(2) run was blocked purely on the 2.5 h container ceiling against its own 6 h timeouts."
    },

    {
      "id": "HEAVY-1",
      "title": "The four memory-bound cells on the 128 GB host, with staged degree-bounded probes so a cap yields partial certified information",
      "target": "Get an actual verdict on: (a) pentagon trackB1_sat (166 vars / 284 eqs) — a NONEMPTY here IS a counterexample candidate; (b) B=16 d=8 chart N on the corrected export (30 eq / 23 unk); (c) p108_525122 and p108_192622 surviving leaves — the first independent test of GGHV Cor 5.7; (d) d=12 chart N and d=12 unsaturated.",
      "premise_with_citations": "These are the only cells in the archive where a NONEMPTY would be immediately meaningful. TARGET_QUEUE states it flatly: 'A NONEMPTY on trackB1 is immediately meaningful and nothing else here is.' Every failure on them was memory: W=19 died once in Singular with 'no more memory' and once OOM-killed in msolve at the 13.3 GiB cap — 'every pentagon failure to date has been memory, never mathematics' (WEEKEND_PLAN §P0). The Groebner ledger on the 166-var system is four attempts, four NO VERDICT: 60 vars deg 22 eliminated form OOM at 13.9 GB in 18 min; 186 vars deg 2 all-vertex-sat timeout at 2.3 GB after 40 min then again at 3 h; 148 vars deg 2 timeout at 1.5 GB after 50 min (AGENT_MAILBOX:1419-1432). d=8 chart N on the CORRECTED export has never been launched at all — MISS-2 — and the four prior attacks all TIMED OUT with clean stderr on the now-VOID printed system (OPEN_ITEMS §1; MORNING_SUMMARY). p108_525122 has 2 unresolved of 5 leaves and p108_192622 has 1 leaf of 139 eqs / 38 vars, both TIMEOUT at 1800 s, and they are first-class because GGHV Cor 5.7's proof has an invalid step at gghv.txt:1430-1433 — the bracket after translation is 1/2 + (lambda/2)x^{-1/2}, not in K^x, so 51 of 66 required conditions are unsupported (CATCHES 'GGHV COROLLARY 5.7 IS UNPROVEN'; P108_RESULTS.md). Critical constraints on how to run them: msolve is structurally excluded above ~180 variables (hard 2^25 hash-table ceiling, segfaults) (AGENT_MAILBOX:1053,1233), so the 166-var system goes to Singular and only the reduced forms to msolve. msolve's default solve mode requires dimension 0 — prior solve-mode runs on the positive-dimensional pentagon are NO VERDICT BY CONSTRUCTION; only -g 2 (Groebner-only) decides emptiness at any dimension (bottom-seed pitfalls). And p108_192622/525122 have grading-torus rank 5, hence are positive-dimensional, so their 1800 s solve-mode timeouts were STRUCTURAL, not budget (AGENT_MAILBOX:1610-1625; mailbox pitfalls: 'do not requeue a NO VERDICT at a longer budget without first measuring the grading-torus rank'). Finally, W=19 is UNDERDETERMINED by 6 (21 eqs / 27 unks) and trackB1_pentagon.py:432 witness() already certifies every truncation W=12..19 NON-EMPTY over Q — so the P0 truncation plan is futile and withdrawn (CATCHES class 15, 16 'P0 IS FUTILE'). Do not re-run it.",
      "method": [
        "1. Pre-flight EVERY cell before it is scheduled: compute eqs - vars excess (must be > 0), measure the grading-torus rank (if > 0 the system is positive-dimensional: use -g 2 or Singular, never msolve solve mode), and check the variable count against the msolve ~180 ceiling. Route accordingly. This pre-flight alone retires several 'timeouts' as mis-targeted rather than hard.",
        "2. Run the corrected d=8 chart N FIRST — it is the smallest genuinely-new object at 30 eq / 23 unk, already exported (wave5/ms/m16_d8_*.ms, regenerable via wave6/w6_seed_d8.py), a_16 left free so one run covers both roots of the row-0 quadratic, and chart Z is eliminated analytically by F2 (mu0 = a2*mu2/3). Pick primes where 12d = 96 is a square. Two primes before quoting any EMPTY. Note prime SIZE buys nothing here — the bottleneck is Groebner structure, not coefficient arithmetic (MORNING_SUMMARY) — so do not spend effort on 16- vs 20-bit.",
        "3. Run the pentagon trackB1_sat system in Singular on the 128 GB host with an 18 h budget and periodic checkpointing. Use the all-vertex-saturated deg-2 form (186 vars) rather than the eliminated deg-22 form (60 vars) — the ledger shows the eliminated form OOMs at 13.9 GB while the deg-2 forms merely time out at 1.5-2.3 GB, i.e. they are time-bound and time is exactly what the persistent host buys.",
        "4. Branch the pentagon cheaply before the big run: c_1_0*d_0_1 = 0 and c_1_0*d_1_1 = 0 with c_1_0 saturated nonzero kills one branch instantly and forces d_0_1 = d_1_1 = 0 in the other (TARGET_QUEUE). Apply the sound constant-coefficient linear fixed point first — it takes 267/148 to 241/123 and is strictly better solver input (CATCHES; p11 wave6/w6_pent_lineloop.py) — and reduce every coefficient mod P before export or msolve rejects the file.",
        "5. Re-run p108_525122's 2 unresolved leaves and p108_192622's single 139-eq/38-var leaf with the torus rank measured first and -g 2 selected accordingly, 8 h budget each. Pre-register the verdict standard BOTH ways, as the campaign already did: EMPTY at one prime is replication-grade only; NON-EMPTY needs the full prime tower plus a char-0 lift before the word 'refutation' is used (OPUS43-012 standard).",
        "6. STAGED DEGREE-BOUNDED PROBES so a cap still yields information (toolchain fix #4): run dim before vdim, and run degree-bounded std at increasing D. Two mandatory guards, both learned the hard way — `option(degBound,D)` is NOT Singular syntax and errors silently, so the first such runs were unbounded and 'passed' two trivial controls; and a degree-bounded result is meaningful ONLY when D exceeds the max degree of the input generators, with gbsize == number-of-input-equations being a no-op sentinel, not evidence (CATCHES class 17). Report D and max input degree together, always.",
        "7. Any NONEMPTY goes straight to the HIT protocol H1-H6 (exact rationals only, Keller constancy, non-injectivity by exact witness, independent resultant route, gauge invariance under random affine changes on BOTH source and target, non-vacuity) with no step skipped. Both prior 'hits' were gauge artifacts: v1 used an absolute stopping test on a system homogeneous of degree -1 in P's scale and Newton drove ||x|| to 1e10 with Q collapsing to zero; v2's 1.70e-09 outlier inflated the denominator via an unfixed coordinate-scale gauge (STATUS §6.6). H5 is the step both would have failed."
      ],
      "agent_roles": ["prover", "certifier", "auditor/skeptic", "lifter"],
      "inputs": ["wave6/frontier/trackB1_sat_p1000003.ms and trackB1_sat_Q.ms", "wave5/ms/m16_d8_*.ms + wave6/w6_seed_d8.py", "wave6/frontier/P108_RESULTS.md", "wt/p11/wave6/w6_pent_lineloop.py", "wt/p11/reruns2/w3_hit_protocol.py"],
      "outputs_with_labels": [
        "d=8 chart N: EMPTY-mod-p(p1,p2) or CANDIDATE-UNVERIFIED -> HIT",
        "pentagon trackB1: EMPTY-mod-p(p1,p2), or TIMEOUT/OOM with the D-ceiling reached recorded, or CANDIDATE-UNVERIFIED -> HIT protocol",
        "p108 leaves: EMPTY-mod-p(p1,p2) (replication-grade only) or CANDIDATE-UNVERIFIED; a non-empty here means GGHV §5 has an error and (9,27) reopens",
        "every cap-terminated run: TIMEOUT or OOM with peak RSS, wall time, D reached, stderr digest — explicitly NOT EMPTY"
      ],
      "stop_rule": "Per cell: 18 h wall and 100 GB RSS. On termination by cap, record TIMEOUT/OOM with the staged partial information (dim if reached, largest D at which the bound bit) and DO NOT re-queue at a longer budget until the torus rank and eqs-vars excess have been re-measured — a structural non-termination does not care about the budget. Stop the pentagon lane entirely if the deg-2 form shows no degree progress across two successive 18 h runs; that is a shape problem and belongs to CASC-1.",
      "cost": "~1,400 core-hours across the four cells (d=8: ~50; pentagon: ~700; p108 leaves: ~250; d=12: ~400). About 4-5 days of the 16-core host.",
      "p_hit": 0.02,
      "why_not_already_closed": "d=8 chart N on the corrected system was exported and never launched (MISS-2). The pentagon and p108 cells were never given a machine that could hold them: every single termination was a 13.3 GiB cap or a container restart, and half the p108 timeouts were msolve solve-mode on positive-dimensional input, which cannot terminate by construction."
    },

    {
      "id": "CASC-1",
      "title": "Rebuild the rational-function cascade as saturation/regular-chain branching, gated behind a two-sided self-test",
      "target": "The single shared blocker. Unblocks d >= 8 in char 0, d=12, d=27, and pentagon propagation past level 3 — none of which direct Groebner can reach.",
      "premise_with_citations": "Direct Groebner scales ~32x per ladder level, measured: d=6 took 42 s, d=7 took 1345 s (OPEN_ITEMS 'THE PLAN, P2'). That puts d >= 8 in char 0 and d=12, d=27 out of reach by that route on any hardware — d=27 is 114 eqs / 85 unknowns and explicitly out of reach. Three cascade attempts all failed their own self-tests, and the failure modes are documented precisely, which is what makes a fourth attempt tractable rather than reckless: (i) w6_pent_cascade2.py solved each level but never wrote solved values back into `known`, so every equation classified as nonlinear and it reported '0 conditions, 0 contradictions' at every level against a deficit of 118 — a can't-fail tool (CATCHES class 6); (ii) w6_pent_levelcascade.py reported 'LEVEL 2 IS INCONSISTENT -> THIS SEED DIES' by silently adding monomials of degree 0 in the fresh unknowns but containing an undetermined non-fresh variable to the constant column, i.e. setting them to 1 — retracted (CATCHES class 7); (iii) w6_ratcascade.py reported 57 dead branches on a system with the obvious solution (a=3,b=4,c=2) and was caught by its own two-sided self-test BEFORE use. The pentagon reader is blunt about the stakes: 'attempt #3 would have falsely killed the campaign's best lead had it been run on the pentagon.' The named root cause in all three is division by symbolic pivots and non-propagating substitution (toolchain gap #3), and the named fix is standard and known: saturation / regular-chain style branching. There is also a real mathematical obstruction recorded for the closed-form route, so the cascade must not pretend to route around it: for x-degree <= 1, u = P is a first integral and Q is quadratic in u giving three x-slots, but at x-degree 2, x is algebraic of degree 2 over u so the trick does not transfer (AGENT_MAILBOX:1455-1470). And the pentagon level ladder has its own coupling trap: the true level-17 unknown is W9 = g9 - (3c1/2c0)z^4 h5 (and W8 = g8 - (3c1/2c0)z^4 h4 at level 16); using g9 alone manufactures a false level-16 resonant obstruction — this is exactly how the sigma^6|h7 wall arose — and omitting the bounded-support end check (deg W8 <= 11, z^12 term must vanish) yields spurious solutions that pass every divisibility and resonance test (pentagon reader pitfalls; l16/breakthrough/PENTAGON_LEVEL16.md).",
      "method": [
        "1. DESIGN ON PAPER FIRST, as the archive itself demands ('must be designed on paper before a fourth attempt', p11 CATCHES:1930-1936). Write the branching rule as a specification with a proof that the branches COVER: at each vanishing pivot emit exactly two ideals — pivot = 0 added and propagated, and pivot inverted via Rabinowitsch saturation — and prove the union is the original variety.",
        "2. Implement over F_p first with python-flint (installed, unused). No symbolic division anywhere: every division is either by a saturated-nonzero variable or it branches.",
        "3. Build the two-sided self-test BEFORE any real data and make it a hard gate: a planted-solution system must stay OPEN at every level, a planted contradiction must CLOSE, and the specific historical regressions must be in the suite — the (a=3,b=4,c=2) system that attempt #3 killed, and an overdetermined system where a non-propagating implementation would report 0 conditions.",
        "4. Add the propagation assertion the campaign named: assert that solved values are actually READ downstream, and treat 'a cascade reporting no constraints at every level on an overdetermined system' as an automatic FAILURE, not a result.",
        "5. Add the pentagon-specific guards to the ladder driver: use the coupled variables W8, W9 (never bare g8, g9) and enforce the bounded-support end condition [z^19]K16 = 3c1(a4^2-4c0b8)^2/(4c0^3). A cascade that omits either will manufacture a kill on the campaign's best lead.",
        "6. Only after all self-tests pass, run on: d=8 chart N (cross-check against HEAVY-1's direct Groebner result — agreement is the acceptance test), then d=12 chart N and d=12 unsaturated, then d=27, then pentagon levels past 3.",
        "7. Any branch this tool CLOSES on real data must be independently reproduced by a direct GB run on the same branch before the closure enters the ledger. A replay sharing the engine's arithmetic is not an independent check (CATCHES class 27)."
      ],
      "agent_roles": ["prover", "certifier", "auditor/skeptic"],
      "inputs": ["wt/canon/wave6/w6_ratcascade.py, w6_pent_cascade2.py, w6_pent_levelcascade.py (all committed as FAILING with their self-tests)", "wt/l16/breakthrough/PENTAGON_LEVEL16.md", "wt/canon/OPEN_ITEMS.md P2", "python-flint"],
      "outputs_with_labels": [
        "cascade tool + self-test suite — CERTIFIED only if both sides of the self-test fire",
        "d=12, d=27 cells: EMPTY-mod-p(p1,p2) or CANDIDATE-UNVERIFIED",
        "pentagon levels 4+: per-level conditions, with any inconsistency requiring direct-GB reproduction before it is recorded"
      ],
      "stop_rule": "Hard stop: if the fourth attempt fails its two-sided self-test, COMMIT IT AS FAILING and stop the lane for this two-week window. Do not run it on real data 'to see'. Three prior attempts establish the base rate; a fourth failure means the design is wrong, not the implementation.",
      "cost": "4 agent-days build (the paper design is most of it). Compute after it lands: ~200 core-hours across d=12, d=27, pentagon levels.",
      "p_hit": 0.01,
      "why_not_already_closed": "Three attempts, three self-test failures, all from the same root cause (symbolic pivot division plus non-propagating substitution), and none was preceded by a paper design. The campaign has the diagnosis written down and never spent the design day."
    },

    {
      "id": "TAIL-1",
      "title": "Enumerate the 41 timeout shapes, run the tail-closure saturation test, and extend the chain compiler only if tails saturate",
      "target": "Turn the two largest 'unsearched' regions from folklore into lists: (a) the 41 timeout shapes, which exist nowhere as an enumeration; (b) the 429 NO-CHAIN frontier records, whose cost may collapse by orders of magnitude.",
      "premise_with_citations": "The '41 undecided TIMEOUT shapes' are referenced as an aggregate in OPEN_ITEMS §7, STATE_FULL §G item 3 and wave6/CERTIFICATE_ROUTE.md, but NO FILE IN THE WORKTREE LISTS THE 41 INDIVIDUALLY (tail-census: 'NOT ENUMERABLE FROM THIS ARCHIVE'). Even the arithmetic is inferred: 33 virgin + 5 orphan + 3 above-125 = 41 is the reader's inference, not the archive's, and STATE_FULL quotes the same virgin block as 36 rather than 33 — so 33 vs 36 is itself inconsistent in the record. Meanwhile the 429 figure has an exact meaning: frontier_151_300_map.json is 432 records of which 429 are status 'NO-CHAIN', meaning the chain library stops around max 150, so no A0-chain, hence no reduced Newton-polygon pair, hence no system exists to hand any solver (tail-census). Extending the compiler is genuinely hard: above-125 Newton polygons are published NOWHERE — 1708.07936 §6 gives chain data only, GGHV §4 gives polygons only for (9,27),(9,24),(8,28),(7,21), hand-derived with no general recipe (RESUME_STATE P3) — and a naive pattern-fit provably fails, because (9,27) has A0 = n*(3,9) and A1 = n*(3,8) exactly while (8,28) is not n*(lattice point) yet its reduced polygons have base pair (4,7),(4,8): two examples with incompatible A0-to-base relations, and fitting a rule to them is the exact failure mode that produced three retracted results (ABOVE_125_STATUS). BUT the sub-150 version of the map is RESOLVED: TRACKD_CHAIN_MAP.md derives it from GGHV Sec 4 + [C] Thm 2.20, reproduces all six published reduced pairs plus the bracket exponent, and all 34 shapes have explicit coordinates. And the cheap lever is the tail-closure conjecture: dedup found that reduced systems depend only on the chain TAIL, the predictor (last-2-segments + shape index) -> system hash has ZERO violations across 16 groups, and the current library is 34 chains -> 26 distinct tails (CROSSDOOR §5). If tails saturate, the 429-case frontier collapses to finitely many tail-systems. The proposed test is exactly 'extend the compiler on 20 sample cases across (150,300] and count new tails vs reused', and it is UNRUN — no results file, no tail-hash counts. Note also the mailbox's competing and better-sourced framing: FABLE-006 says the published enumeration is 34 total with 24 untouched above 125, and explicitly disavows 804 as coming from lost Sessions 19-38 with no supporting artifact.",
      "method": [
        "1. Build the 41-shape list mechanically, not by reading prose: scan every log and RUNLOG for exit 124 / 0-byte-output / TIMEOUT records, hash each associated system by content, and emit TIMEOUT_SHAPES.json with eqs, vars, torus rank, orientation, corner, (m,n), budget used, engine, stderr digest. Publish the true count and reconcile 33 vs 36 vs 41 explicitly. Expect the count to SHRINK — 49 TIMEOUT records previously reduced to 16 unique systems by tail-hash (CATCHES Sec 2.3).",
        "2. Sweep the deduplicated list smallest-first on the light box: sort by (vars, eqs), pre-flight each for eqs-vars excess > 0 and torus rank, and give each an 1800 s budget with two compliant primes. Each shape is an independent lottery ticket and the small end is cheap.",
        "3. Run the tail-closure saturation test as specified: extend the chain construction to 20 sample cases across (150,300], compute the tail hash for each, and count new tails vs reused against the 34 -> 26 baseline. This is 20 compilations, not a sweep.",
        "4. Decide on the result. If reuse is high and rising (say >= 15 of 20 reuse an existing tail), the frontier collapses to finitely many tail-systems and extending the compiler is worth 3 agent-days. If new tails keep appearing, STOP — the compiler extension is a multi-week project with unpublished polygon data and does not fit this window; record that finding and move the effort to HEAVY-1.",
        "5. Independently of the above, discharge the A'_t assumption, which is cheap and is the one census case resting on an unprinted step of a source: re-derive the admissible A'_t for the (10,40) chain inside gghv_audit/ggv_algorithms.py — which already reproduces 34/34 published cases and 10/10 GGHV rows with four negative controls — enumerating A'_t from the algorithm's own filters rather than importing (1,0). Same pattern already used for the F6 gcd discrepancy.",
        "6. Also cheap and owed: reconcile against the published enumeration. FABLE-006's 24 untouched cases come from a PDF text extraction whose own author flagged it as needing verification, and the campaign's reproduction all_cases_max_le_150.json (34 cases, 10 with max<125) has never been checked row-for-row against §6 of the published PDF — the literature reader calls this 'the single highest-value remaining check'. Install poppler-utils in the provisioning script (it was absent, which is why the check failed) and do it."
      ],
      "agent_roles": ["generator", "librarian", "exporter/sanitizer", "auditor/skeptic"],
      "inputs": ["wt/canon/wave6/frontier_151_300_map.json", "wt/canon/CROSSDOOR.md §5", "wt/canon/campaign/audit_tracks/TRACKD_CHAIN_MAP.md", "wt/canon/gghv_audit/ggv_algorithms.py + all_cases_max_le_150.json", "wt/canon/papers/1708.07936.pdf", "wt/mailbox/AGENT_MAILBOX.md:3909-3985 (FABLE-006)"],
      "outputs_with_labels": [
        "TIMEOUT_SHAPES.json — the 41 (or fewer) shapes enumerated, each TIMEOUT, none EMPTY",
        "sweep verdicts: EMPTY-mod-p(p1,p2) per shape, or TIMEOUT with resource record",
        "TAIL_SATURATION.json — new-vs-reused tail counts over 20 samples; empirical, explicitly not a proof of saturation",
        "A'_t: PROVED-exact (re-derived) or a new census row",
        "§6 table cross-check: CERTIFIED or a discrepancy list"
      ],
      "stop_rule": "The saturation test decides the compiler question and nothing else does. If < 15 of 20 samples reuse an existing tail, the compiler extension is OUT OF SCOPE for this window — say so and stop, rather than starting a multi-week build against unpublished polygon data. On the timeout sweep, stop each shape at 1800 s and record TIMEOUT; do not escalate a shape whose torus rank shows it is positive-dimensional.",
      "cost": "1.5 agent-days for enumeration + saturation test + A'_t + PDF check. Compute: ~120 core-hours for the smallest-first sweep on the light box. Compiler extension, only if the saturation test says yes: +3 agent-days.",
      "p_hit": 0.005,
      "why_not_already_closed": "The 41 shapes were never enumerated because no machine-readable NO-VERDICT ledger existed — they are folklore in three files. The saturation test was assigned (OPUS43-012 to Codex) and never run. The PDF check failed only because poppler-utils and pypdf were absent from the container."
    },

    {
      "id": "SCHED-1",
      "title": "A scheduler that ranks by expected information per core-hour and always runs the smallest undecided cell first",
      "target": "Make the fleet's allocation a computed decision rather than a session-by-session preference, and make wasted compute structurally impossible (vacuous ansatz, positive-dimensional solve mode, re-running a known hash).",
      "premise_with_citations": "The campaign burned compute on runs that could not carry information, and it caught each only afterward. The plane-sweep shape search ran 1728 shapes producing maps of total degree <= ~32 when Moh proved JC2 for degree <= 100, so a negative result carried exactly zero information — killed mid-run (CATCHES class 16). The entire P0 truncation plan ran for two weeks at W=19, which is underdetermined by 6 and whose non-emptiness was ALREADY CERTIFIED by trackB1_pentagon.py:432 witness(), a docstring in the repo saying so (CATCHES 'P0 IS FUTILE', 'FOUND IN PLAIN SIGHT'). The rank criterion was extended and proposed for further extension to d=48 and d=75 as a priority when it is a can't-fail certifier that cannot decide anything (OPEN_ITEMS §4 vs WEEKEND_PLAN §P2 — the readers flag this as a direct tension, with OPEN_ITEMS governing). msolve solve-mode runs on positive-dimensional p108 systems timed out structurally at 1800 s and were requeued at longer budgets (mailbox pitfalls). And the paused pair108 sweep re-ran its own 4-minutes-earlier TIMEOUT under a different tag (CATCHES Sec 1). Meanwhile the ranking that DOES exist is sound and should be encoded: the open-queue reader ranks pentagon W=19 / trackB1 first on HIT-chance per compute-hour and d=8 chart N second on cost per unit of new information, and ranks Path D last with 'A counterexample from Path D this session: effectively zero. This path builds the road; it does not walk it' (41.md).",
      "method": [
        "1. Encode a pre-flight refusal list in the scheduler. A job is REFUSED, not queued, if any holds: max total degree the ansatz can produce <= 100 (restates Moh); gcd(deg P, deg Q) not in {16} union (20, inf); below max 125 and not (72,108) in one of its two orientations; eqs - vars excess <= 0 for an emptiness run; msolve solve mode requested on a system with torus rank > 0; content_hash already carries a terminal verdict; the criterion applied is on the known-vacuous list (rank/bifurcation).",
        "2. Score each admissible job as expected_information / expected_core_hours. Expected information: 1.0 for a cell with NO verdict by any method (pentagon case (1), d=8 chart N corrected, p108 leaves), 0.6 for upgrading single-prime to multi-prime, 0.5 for promoting EMPTY-mod-p to PROVED-exact, 0.1 for re-confirming an already multi-prime EMPTY, 0.0 for a can't-fail criterion. Expected core-hours: from the measured scaling law (32x per ladder level: d=6 42 s, d=7 1345 s) and from the recorded RSS/wall of the nearest sibling run.",
        "3. Break ties by SIZE, ascending. Smallest undecided cell first, always. This is the rule the weekend plan named and never implemented, and it is what makes the 41-shape sweep and the d=8 cell come before the pentagon in the light lane.",
        "4. Two lanes with different rules. LIGHT lane on the ephemeral 4-core box: only jobs whose sibling runs completed in < 15 min and < 8 GB, run under the one-verdict-per-commit runner so a restart costs one job. HEAVY lane on the persistent host: single-slot FIFO, 18 h and 100 GB caps, checkpointed.",
        "5. Re-cost after every verdict. A TIMEOUT updates the cost model for its whole family upward; an OOM at a known RSS updates the memory model. The scheduler re-ranks nightly.",
        "6. Publish the queue and its scores as a committed file so the ranking is auditable and a later session cannot silently re-prioritize by preference. Include the REFUSED list with reasons — that file is the record of compute NOT spent, which is as valuable as the verdicts."
      ],
      "agent_roles": ["librarian", "auditor/skeptic", "generator"],
      "inputs": ["wt/canon/OPEN_ITEMS.md 'THE PLAN'", "wt/canon/WEEKEND_PLAN.md", "wt/mailbox/wave6/frontier/TARGET_QUEUE.md", "wt/canon/pent/RUNLOG.tsv (cost model seed)", "ledger/ from LEDGER-1"],
      "outputs_with_labels": [
        "queue.json with per-job score, expected cost, lane — infrastructure artifact",
        "REFUSED.json with per-job refusal reason — the record of compute not spent",
        "cost_model.json updated per verdict"
      ],
      "stop_rule": "Stop building when the scheduler reproduces the open-queue reader's hand ranking (pentagon 1, d=8 2, p108 3, char-0 case (2) 4, 41-shape sweep 5, cascade 6) from the scores alone. If it does not, either the scoring is wrong or the hand ranking was — investigate before running anything, because that disagreement is itself information.",
      "cost": "1 agent-day. Negligible compute.",
      "p_hit": 0.00,
      "why_not_already_closed": "Priorities lived in prose across WEEKEND_PLAN, OPEN_ITEMS, MORNING_SUMMARY and TARGET_QUEUE, which contradict each other (the rank-criterion extension is a priority in one and worthless in another), and each container restart re-litigated them from whichever file was read first."
    }
  ],

  "job_schema": {
    "job_id": "string, stable, = content_hash[:16]",
    "content_hash": "sha256 of the canonicalized generator list: sorted monomials, coefficients reduced into [0,p), variable list, characteristic. NOT a hash of the file text.",
    "system_size": {"n_vars": "int", "n_eqs": "int", "max_total_degree": "int", "n_terms": "int", "eqs_minus_vars_excess": "int, must be > 0 for an emptiness run", "grading_torus_rank": "int, > 0 means positive-dimensional: msolve solve mode is invalid"},
    "characteristic": "0 or p",
    "prime_list": "array of primes used; each must satisfy p = 1 mod 3 and, for ladder cells, 12d a square mod p",
    "engine": "singular-4.3.2p16 | msolve-0.10.1 | pari-2.15.4 | flint-0.9.0",
    "engine_sha256": "binary hash from engines.json",
    "flags": "exact command line, e.g. 'msolve -g 2 -f X.ms -o Y.out'; -g 2 is REQUIRED for any positive-dimensional emptiness question",
    "budget": {"wall_seconds": "int", "mem_bytes": "int, enforced by cgroup", "disk_bytes_free_required": "int"},
    "orientation_key": {"degree_pair": "[dP, dQ] with order significant", "corner_A0": "[a,1,b]", "m_n": "[m,n]", "chart": "string, e.g. d_3_3=1 | chart N | c2=1", "gauge": "string + a proof reference that every orbit meets the slice"},
    "provenance": {"generator_script": "path", "source_system": "corrected (m16_*) | printed (VOID)", "parent_hash": "hash of the system this was derived from, if any"},
    "lane": "light | heavy",
    "score": {"expected_information": "float 0-1", "expected_core_hours": "float", "ratio": "float"}
  },

  "verdict_labels": [
    "PROVED-exact — char-0, exact arithmetic end to end, with a certificate a third party can re-check (ideal membership, or an explicit Nullstellensatz 1 = sum h_i f_i)",
    "CERTIFIED — engine input + captured output + an independent Python certifier that carries a negative control observed to FAIL",
    "EMPTY-mod-p(p1,p2,...) — modular only, prime list mandatory, at least two compliant primes, NEVER promotable to char 0 without CERT-1",
    "TIMEOUT — cap reached; records wall time, peak RSS, largest D at which a degree bound bit, dim if reached. Not a verdict.",
    "OOM — memory cap reached; records peak RSS and cgroup limit. Not a verdict.",
    "FAILURE — parse error, missing engine, ENOSPC, segfault, empty output, or '[-1]' co-occurring with any parse/read error. Never an EMPTY.",
    "CANDIDATE-UNVERIFIED — a point or solution found and exactly lifted, but the HIT protocol has not been passed",
    "HIT — H1 through H6 of w3_hit_protocol.py all passed with H6 shown to have fired (rejects the known negatives, accepts the positive control)"
  ],

  "gates": [
    {"name": "no-verdict-from-empty-output", "test": "output file size > 0 bytes AND the classification string matches a documented msolve/Singular output exactly ('[-1]:' EMPTY, '[1,n,-1,[]]' POSITIVE-DIMENSIONAL, '[0,...' ZERO-DIM). Exit codes are ignored entirely: msolve exits 0 on timeout, crash and parse error alike.", "action_on_fail": "emit FAILURE (or TIMEOUT/OOM per /usr/bin/time), never EMPTY; block the commit"},
    {"name": "stderr-captured-and-clean", "test": "stdout and stderr tee'd to separate files; grep for 'Error when parsing', 'Error when reading', 'cannot be 0 modulo'. Any '[-1]' co-occurring with such a line is a FAILURE.", "action_on_fail": "FAILURE record with the stderr digest; block the commit"},
    {"name": "export-sanitizer", "test": "every coefficient satisfies 0 <= c < p and c != 0 mod p; no vanishing terms; no nonzero constant generator; the written file round-trips by re-parse back to the sympy system.", "action_on_fail": "refuse to write the file; do not invoke the solver (this is the exact bug that caused the msolve silent-lie EMPTY and 3921/8264 rejected coefficients)"},
    {"name": "no-constant-generators", "test": "no generator is a nonzero constant, in particular not a nonzero multiple of the characteristic.", "action_on_fail": "reject the export; the campaign's fake 20-second d=8 EMPTY came from exactly this"},
    {"name": "overdetermination-pre-flight", "test": "eqs - vars excess > 0 before any emptiness or parametrization run.", "action_on_fail": "refuse to schedule; a non-positive excess makes emptiness unreachable (the W=19 two-week error)"},
    {"name": "torus-rank-routing", "test": "grading torus rank measured; if > 0, msolve solve mode is forbidden and -g 2 or Singular is required.", "action_on_fail": "refuse to schedule; prior p108 timeouts were structural non-termination, not budget"},
    {"name": "degree-bound-bites", "test": "for any degree-bounded run, D > max total degree of the input generators, and gbsize != number of input equations.", "action_on_fail": "mark the result a no-op sentinel, not evidence; also assert option(degBound,D) is valid Singular syntax, since the invalid form errors silently"},
    {"name": "prime-hygiene", "test": "every prime satisfies p = 1 mod 3; for ladder cells 12d is a square mod p; control primes 5, 11, 17 are fed to the selector and must be REJECTED.", "action_on_fail": "reject the prime; a selector that accepts the controls is itself a can't-fail certifier"},
    {"name": "lucky-prime-majority", "test": "mod-p GB leading-term ideal matches the majority signature across the prime batch.", "action_on_fail": "discard the minority prime and record it; never average over it"},
    {"name": "modular-verdicts-never-stand-alone", "test": "any contradiction/emptiness claim has characteristic == 0, or >= 2 compliant primes AND a pending CERT-1 job.", "action_on_fail": "label EMPTY-mod-p(prime list) and refuse the PROVED-exact label"},
    {"name": "two-sided-self-test-on-reducers", "test": "every branch-and-reduce, cascade or eliminator passes a planted-solution system (must stay OPEN) and a planted contradiction (must CLOSE), including the (a=3,b=4,c=2) regression; and solved values are asserted to be read downstream.", "action_on_fail": "quarantine the tool, commit it as FAILING, run nothing real with it"},
    {"name": "planted-root-control-for-numerical-lanes", "test": "a root planted at residual exactly 0.0 at the SAME unknown count is recovered by the search.", "action_on_fail": "the lane is a finder only: a HIT goes to the HIT protocol, a MISS may not be cited at all (multi-start Newton is blind at >= 25 unknowns: pentagon best 1.9e5 of 3 starts, ladder d=8 best 1.7e3 of 25 starts)"},
    {"name": "ast-scan-for-literal-True-checks", "test": "AST walk of the tree rejects any check() whose condition is a compile-time constant; the scanner is itself self-tested on a synthetic rigged/honest pair.", "action_on_fail": "CI red; 45 such checks currently exist, 19 in inherited certifiers"},
    {"name": "certifier-negative-control-observed-to-fail", "test": "run_all.sh reports ok for a certifier only if its negative control was executed and observed to FAIL.", "action_on_fail": "report the certifier as UNCHECKED, not passing"},
    {"name": "content-hash-dedup", "test": "content_hash not already carrying a terminal verdict in ledger/.", "action_on_fail": "refuse to schedule; transfer the existing verdict by hash identity (p108_821326 == p108_843700; 49 TIMEOUTs -> 16 unique systems)"},
    {"name": "ansatz-degree-gate", "test": "max total degree the ansatz can produce > 100; gcd(dP,dQ) in {16} union (20,inf); below max 125 only (72,108) in one of its two orientations.", "action_on_fail": "refuse; a negative result would carry zero information (Moh)"},
    {"name": "engine-present-and-pinned", "test": "engine binary exists and its --version matches engines.json.", "action_on_fail": "MISSING-ENGINE status and a FAILURE record — never a silent skip or a generic nonzero rc"},
    {"name": "disk-and-inode-headroom", "test": "free disk and inodes above threshold before and during every long run.", "action_on_fail": "abort cleanly with a FAILURE record; ENOSPC silently killed the W=10 eliminator mid json.dump"},
    {"name": "ps-verify-claimed-running", "test": "every 'running' claim carries a live PID check; the watchdog logs a value (/proc cmdline, RSS) that would visibly differ if it watched the wrong process.", "action_on_fail": "mark the job NOT RUNNING and re-queue; enforcer v1 matched the 3 MB bash wrapper instead of the 7.4 GB msolve"},
    {"name": "no-pkill-f", "test": "pre-commit hook rejects `pkill -f` and `pgrep -f` on self-matching patterns.", "action_on_fail": "block the commit; this killed the invoking shell four times"},
    {"name": "orientation-in-the-key", "test": "every degree-pair record names its orientation, corner and (m,n).", "action_on_fail": "reject the record; (72,108) gives L=3 and (108,72) gives L=4, and (8,28)/(3,2) vs (8,28)/(3,4) are different objects"},
    {"name": "contradiction-linter", "test": "no two records under one key carry incompatible labels.", "action_on_fail": "hard error; STATUS carried 'PROVEN dead, unconditional' and 'conditional' simultaneously"},
    {"name": "hit-protocol-complete", "test": "H1-H6 all pass with H6 shown to have fired.", "action_on_fail": "CANDIDATE-UNVERIFIED, and report which step failed; claim nothing (both prior 'hits' were gauge artifacts that H5 would have caught)"}
  ],

  "timeline": [
    "Day 1 — ENV-1 steps 1-2: provision.sh, engines.json, engine_guard.py; request the persistent 16-core/128 GB host. LEDGER-1 steps 1-2: job/verdict schema and content hashing. In parallel, TAIL-1 step 1: mechanical scan for every timeout record.",
    "Day 2 — ENV-1 steps 4-8: runner.sh, serial heavy queue, disk+RSS watcher, pkill ban, smoke suite passing across a deliberate restart. LEDGER-1 step 3: gate.py wired into exporter, runner and CI. First real verdict lands committed+pushed unattended.",
    "Day 3 — LEDGER-1 steps 4-6: re-key the tree by content hash, publish RECONCILIATION.md (429/432/464/474/804/34), backfill prime lists, demote un-audited EMPTYs. SCHED-1: scheduler, refusal list, cost model; check it reproduces the hand ranking. AST scan run: expect ~45 findings, triage them.",
    "Day 4 — Heavy host live. HEAVY-1 step 2: corrected d=8 chart N launched (smallest genuinely-new cell, two primes with 96 a square). TAIL-1 step 2: smallest-first sweep of the deduplicated timeout list starts on the light box. CASC-1 step 1: paper design of the cascade begins (design only, no code).",
    "Day 5 — CERT-1 steps 1-2: lucky-prime certifier with controls 5/11/17 required to be rejected; multi-prime GB batches on case (2) both charts. TAIL-1 steps 3-4: tail-closure saturation test on 20 samples across (150,300] — the go/no-go for the chain compiler. TAIL-1 step 5: A'_t re-derivation inside ggv_algorithms.py.",
    "Day 6 — CERT-1 steps 3-5: CRT + rational reconstruction with the stability rule, exact ideal-membership verification, Nullstellensatz linear-algebra route, two-sided self-test. HEAVY-1 step 3: pentagon trackB1_sat all-vertex-saturated form launched in Singular, 18 h budget, checkpointed. TAIL-1 step 6: §6 PDF cross-check now that poppler is installed.",
    "Day 7 — Midpoint audit (see audit_gates_before_compute). Read the verdicts, not the plan. Re-cost every family from measured data. CASC-1 steps 2-3: implementation plus the self-test suite including the (a=3,b=4,c=2) regression.",
    "Day 8-9 — CERT-1 step 6 on case (2) to completion; if it lands, that is the campaign's first char-0 promotion and the single largest evidential upgrade available. HEAVY-1 step 5: p108_525122's 2 leaves and p108_192622's 1 leaf, torus rank measured first, -g 2, 8 h each, verdict standard pre-registered both ways.",
    "Day 10 — CASC-1 self-test verdict. If it fails, commit as FAILING and stop the lane — three prior attempts set the base rate. If it passes, cross-check it against HEAVY-1's direct d=8 result; agreement is the acceptance test.",
    "Day 11-12 — CERT-1 on B=16 d=7 (recovering the confirming primes MISS-1 lost) and chart Z d=8..12. HEAVY-1 step 6: staged degree-bounded probes on whatever capped out, so a cap yields a recorded D-ceiling rather than nothing. Chain-compiler extension starts only if the saturation test said yes.",
    "Day 13 — CASC-1 (if alive) on d=12 chart N, d=12 unsaturated, d=27. Everything else finishes or is capped and labelled.",
    "Day 14 — Freeze. Every job in the ledger carries a label from the vocabulary. Publish: the reconciled counts, the enumerated timeout list, the saturation verdict, every char-0 promotion with its certificate, and — most importantly — the honest list of what is still TIMEOUT/OOM/NOT RUN with its measured resource wall. Undecided stays undecided."
  ],

  "audit_gates_before_compute": [
    "Before ANY solver run: gate.py green on the export (sanitizer, no constant generators, coefficients reduced mod p, round-trip re-parse). The msolve silent-lie EMPTY and 3921 rejected coefficients both came from skipping this.",
    "Before ANY emptiness run: eqs - vars excess computed and > 0, and grading torus rank measured to route the engine. Two weeks went into W=19, underdetermined by 6, whose non-emptiness was already certified by a witness() in the repo.",
    "Before ANY heavy launch: read the target artifact's docstring, meta and declared side conditions. The P0 futility and the eliminator mis-targeting were both settled by files already on disk.",
    "Before ANY reducer or cascade touches real data: two-sided self-test passing, including the historical regressions. Attempt #3 would have falsely killed the campaign's best lead.",
    "Before ANY numerical lane output is cited: planted-root control at the same unknown count recovered. Otherwise a MISS carries zero information.",
    "Before ANY prime batch: the lucky-prime selector rejects controls 5, 11, 17, and every chosen prime is 1 mod 3.",
    "Before ANY scheduling: content_hash checked against the ledger. Before any re-queue at a longer budget: torus rank re-measured, because structural non-termination does not care about the budget.",
    "Before ANY [PROVED-exact] label: the certificate exists as a file a third party can re-check by multiplication, and the certifier's negative control was observed to fail.",
    "Day-7 midpoint audit, mandatory: re-read the ledger rather than the plan; confirm no TIMEOUT or OOM has drifted into prose as 'probably empty'; confirm the AST scan is at zero; confirm no verdict lacks a prime list or stderr digest.",
    "Before ANY counterexample claim: HIT protocol H1-H6 complete with H6 shown to have fired. Both prior 'hits' were gauge artifacts and H5 is the step they would have failed."
  ],

  "risks": [
    "The persistent 128 GB host is the load-bearing assumption of HEAVY-1 and CERT-1. If it cannot be obtained, both lanes reduce to staged degree-bounded probes under 12 GB, and the honest expected output of the fortnight drops to: a working plant, a reconciled ledger, the enumerated timeout list, and the saturation verdict — real deliverables, but no new verdict on the pentagon.",
    "CERT-1's Nullstellensatz linear algebra can blow up in D. The coefficient matrix at the D the mod-p GB suggests may not fit in 128 GB. Mitigation: record the D-ceiling reached as a real result and do not let a failed reconstruction become 'probably empty'.",
    "The cascade is a fourth attempt after three failures with the same root cause. Base rate says it fails. The hard stop (commit as FAILING, do not run on real data) is the mitigation, and it must be honoured even under schedule pressure — this is precisely the tool whose predecessor would have falsely killed the best lead.",
    "The chain compiler may be a multi-week project against polygon data published nowhere, with a proven failure mode for naive pattern-fitting (three retracted results). The saturation test is the only thing licensed to authorize starting it.",
    "Re-keying by content hash may reveal that the surviving verdict count is materially smaller than the record claims — 49 TIMEOUTs already collapsed to 16 unique systems. That is a correct finding, not a regression, but it will look like the campaign going backwards and must be framed as such in the write-up.",
    "Msolve's ~180-variable ceiling and 2^25 hash table are hard: the 166-var and 186-var pentagon forms sit right at the edge, so the second-engine cross-check may be unavailable exactly where it is most wanted. Singular becomes single-engine there, which weakens every verdict it produces.",
    "Everything built on GGV's misprinted (1.2) is VOID. If any corrected-vs-printed provenance is mis-recorded in the re-keying, a void result could be resurrected as live. The provenance field in job.json must be populated from the generator script, never inferred from a filename.",
    "Agent-fleet risk: seven roles with contracts is coordination overhead that can exceed its value at this scale. The mitigation is that the contracts ARE the gates — an agent's output is accepted only if gate.py passes it, so a mis-behaving agent produces a blocked commit rather than a corrupt record."
  ],

  "what_would_change_my_mind": [
    "If the persistent host runs the pentagon deg-2 saturated form for 18 h at 100 GB and shows no degree progress across two successive runs, then memory was never the binding constraint on that cell and the whole 'every pentagon failure was memory, never mathematics' premise is wrong. Compute engineering would then be the wrong angle entirely and the effort belongs in the level-ladder mathematics (CASC-1 step 5) or in reformulation.",
    "If the tail-closure saturation test shows new tails appearing at a steady rate across the 20 samples, the 429-case frontier does NOT collapse and is a genuinely infinite-effort region — I would drop it from the two-week plan entirely and reallocate to CERT-1 and HEAVY-1.",
    "If CERT-1's reconstruction is UNSTABLE across primes on case (2), that is evidence the mod-p EMPTYs may be reduction artifacts rather than truth, which would mean the campaign's most-cited verdict is weaker than recorded — and would make re-auditing the existing EMPTY set, not searching new cells, the highest-value work.",
    "If re-keying by content hash shows the 41 timeout shapes are really ~12 unique systems and most are positive-dimensional (torus rank > 0), then they were never lottery tickets, just msolve solve-mode misuse repeated under different tags, and TAIL-1's sweep is worth almost nothing.",
    "If the corrected d=8 chart N cell — 30 eqs / 23 unknowns, the smallest genuinely-new object on the board — still does not finish on 16 cores and 128 GB in 18 h, then the ~32x-per-level scaling law is optimistic and every estimate above for d=9..27 is off by orders of magnitude; the entire ladder would then be unreachable by any amount of hardware and only the cascade or a reformulation matters.",
    "If the published §6 cross-check shows the campaign's reproduction all_cases_max_le_150.json disagrees with the paper row-for-row, the enumeration underlying every 'unsearched region' count is wrong, and the first two weeks should go to re-deriving the enumeration rather than to running anything."
  ]
}
```