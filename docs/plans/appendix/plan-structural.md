```json
{
  "angle": "Structural, non-brute-force routes to a JC2 counterexample: mechanism-first (what KIND of map can be etale and non-injective) rather than cell-by-cell elimination on Newton-polygon systems.",

  "thesis": "The archive's own record says the elimination program is compute-bound, not idea-bound: every hard cell ends in TIMEOUT/OOM, and its two genuinely NEW structural results of the final days — the deck-group theorem (a plane counterexample must be a non-Galois covering of geometric degree mu>=3) and the sweep dichotomy (det J(S) = det(Delta,X') + gamma*det(Delta,Delta')) — were produced in hours of paper work, not weeks of Groebner. Both came from asking about mechanism. The thesis of this plan is that the same move has one more round left in it, because the sweep dichotomy as recorded closes a NARROWER family than its headline claims: it is proved for S polynomial and AFFINE-LINEAR in gamma, and the divisional-twist rebuttal is proved only for the single twist shape w = gamma*u with C = gamma*x^s. The archive itself wrote down the general twist identity C*{P,Q} - j*Q*{P,C} + i*P*{Q,C} = kappa*C^(i+j+1) (CATCHES.md, 'THE PLANE SIDE CONDITION (the object nobody has written down)'), launched exactly one search on it, killed that search as vacuous by Moh (max total degree ~32 <= 100), and NEVER RE-LAUNCHED IT ABOVE THE MOH GATE. That is a live, executable, mechanism-shaped search with a headline 'closed' sign hanging on it. Around it I place four more leads that share one property the elimination queue lacks: a positive outcome is a counterexample or an explicit candidate, not merely a cell that fails to be empty. The honest overall probability that this plan produces a verified JC2 counterexample is low single digits; its expected value is dominated by (i) the twisted-sweep side-condition system above Moh, (ii) collision-first search, where non-injectivity is an INPUT so any solution passes HIT gate H3 by construction, and (iii) a cheap adversarial-prime audit that could expose a mod-p EMPTY verdict as a bad-reduction artefact — the one failure mode the archive proved real (z=7y dies mod 7) and then never systematically hunted for.",

  "leads": [
    {
      "id": "L1",
      "title": "The twisted plane sweep above Moh: solve the general division-twist side-condition system, including Laurent/monomial twists in K[x,x^-1,y]",
      "target": "A plane Keller map F = (P/C^i, Q/C^j) of tangent-sweep type with total degree > 100, or a proof that the general twist family is empty (which would upgrade the archive's dichotomy to its headline).",
      "premise_with_citations": "The dimension-3 refutation's mechanism is the tangent sweep S(gamma,w) = (p(w)+2gamma, q(w)+gamma*w) with q' = (w/2)p', which is ALREADY A PLANE MAP; its only defect is det J(S) = 2*gamma, the coordinate rather than a constant, and dimension 3 spends its spare variable cancelling that gamma via C = gamma*x and division by C, C^2 (canon/CATCHES.md, 'THE MECHANISM, AND WHY IT IS OUR BUSINESS'; papers/2608.00222.pdf sec 3.1, 3.4). Gao gives NO reason the plane fails beyond Wang's degree-2 theorem and 'known constructions produce degree >= 3' (2608.00222 p.2 lines 125-127), and the archive says so explicitly. The archive PROVED that no POLYNOMIAL conjugation repairs the plane sweep (det J(F) factors as a product of three polynomials), and concluded that any plane sweep-type counterexample must use a DIVISION twist with divisibility side conditions. It then derived the exact identity det J(F) = C^(-i-j-1)[C*{P,Q} - j*Q*{P,C} + i*P*{Q,C}], verified it as a can-fail control (wave6/w6_plane_sweep.verify_identity), and wrote 'the object nobody has written down'. The closure argument (canon/CATCHES.md, 'THE SWEEP DICHOTOMY', branch (a)) rebuts the twist only in the specialised form 'with w = gamma*u the twisted Jacobian is u^2*Psi(gamma,u)*{gamma,u}' — one twist shape, not the general (C,i,j). The one search that WAS launched (wave6/w6_plane_sweep_search.py, gamma = c0 + a x^al y^be, w = gamma(1+b x^mu y^nu), C = gamma*x^s, (i,j) in {(1,2),(1,1),(0,1),(2,3)}, deg p <= 3) was self-caught as VACUOUS BY MOH: it produces maps of total degree at most ~32 and Moh 1983 proves JC2 for degree <= 100 (canon/CATCHES.md, 'TWO CATCHES ON MY OWN PLANE-SWEEP SEARCH'; literature reader: Moh, J. reine angew. Math. 340 (1983) 140-212, accepted classical). It was killed mid-run and NO VERDICT was ever recorded for the general system. Separately, FABLE-005 (mailbox/AGENT_MAILBOX.md:3802-3907, commit 4c6018e) asked for exactly this — 'look for a plane analogue of the tangent sweep via a monomial twist in K[x,x^-1,y]' — and the message is recorded as UNANSWERED, as is the whole FABLE thread from 4c6018e to HEAD 156ba7a.",
      "method": [
        "1. AUDIT FIRST (gate A1 below): read wave6/w6_plane_sweep.py and w6_plane_sweep_search.py in full and write down, verbatim, the exact hypothesis set under which 'THE SWEEP DICHOTOMY' is proved. Record explicitly whether S is assumed polynomial in gamma of degree exactly 1, whether (gamma,w) are assumed to be polynomial coordinates on the source, and whether branch (a)'s twist rebuttal is stated for general (C,i,j) or only for w = gamma*u, C = gamma*x^s. Publish the answer as a labelled scope statement. If the dichotomy IS general, stop and report the lead closed — that is a valid, cheap outcome.",
        "2. Generalise the sweep ansatz along the two axes the recorded proof does not cover: (i) degree >= 2 in the sweep parameter, S = X(w) + gamma*Delta(w) + gamma^2*E(w); (ii) a source reparametrisation that is not polynomial-invertible, i.e. (gamma,w) related to (x,y) by a monomial map, so the natural home is K[x,x^-1,y]. Re-derive det J(S) symbolically in each and record whether the branch-(b) collapse (Delta parallel to a constant vector => triangular => injective) still forces triangularity. This is exact sympy work, minutes.",
        "3. Write the GENERAL twist system: unknown C (arbitrary polynomial of prescribed Newton support, not gamma*x^s), unknown (i,j) over a finite grid, unknown sweep data (X, Delta, E), and impose C*{P,Q} - j*Q*{P,C} + i*P*{Q,C} - kappa*C^(i+j+1) == 0 identically in (x,y), plus the divisibilities C^i | P, C^j | Q realised as exact polynomial cofactor unknowns (P = C^i * Ptilde) rather than as a post-hoc check.",
        "4. PRE-FLIGHT DEGREE GATE, MANDATORY (this is the exact error that killed the last attempt): compute the maximum total degree the ansatz can realise; ABORT any shape whose max total degree <= 100 (Moh) and additionally require gcd(deg P, deg Q) = 16 or > 20 (GGV Thm 1.2 gate) and, if max < 125, that the pair be (72,108) in one of its two orientations (GGHV as amended). Emit the surviving shape list with its degree bounds BEFORE any solver is invoked, and log it.",
        "5. Solve the surviving shapes exactly: saturate kappa != 0 and the sweep non-degeneracy (Delta not identically parallel to a constant vector), export via the msolve sanitiser, run mod p at two compliant primes first, then char 0 on anything that is not clean-EMPTY.",
        "6. Any solution goes straight to the lift pipeline (lift/lift_pipeline.py: Hensel to p^(2^K), half-extended-Euclid rational reconstruction, exact substitution) and then to the full HIT protocol H1-H6 in reruns2/w3_hit_protocol.py, with H6 shown to have fired.",
        "7. Report EMPTY only with the shape family's degree bounds attached, so the negative is a scoped theorem ('no sweep-type plane Keller map with twist support S and degree <= N') rather than another vacuous run."
      ],
      "agent_roles": ["scope-auditor (reads w6_plane_sweep*.py and fixes the dichotomy's exact hypotheses)", "symbolic algebraist (re-derives det J for the generalised ansatz in sympy with can-fail controls)", "shape enumerator + Moh/gcd/GGHV pre-flight gate keeper", "CAS runner (msolve/Singular, two primes then char 0, stderr captured)", "HIT-gate agent (lift + H1-H6)", "adjudicator (labels the negative with its scope)"],
      "inputs": ["wt/canon/wave6/w6_plane_sweep.py", "wt/canon/wave6/w6_plane_sweep_search.py", "wt/canon/CATCHES.md sections 'A NEW DOOR: THE PLANE TANGENT SWEEP', 'THE SWEEP MECHANISM IS DEAD IN THE PLANE', 'TWO CATCHES ON MY OWN PLANE-SWEEP SEARCH'", "wt/canon/papers/2608.00222.pdf sec 3.1-3.4", "wt/canon/wave4/w4_msformat.py (export sanitiser)", "wt/canon/lift/lift_pipeline.py", "wt/canon/reruns2/w3_hit_protocol.py", "mailbox/AGENT_MAILBOX.md:3802-3907 (FABLE-005 ask 3)"],
      "outputs_with_labels": ["SCOPE STATEMENT of the sweep dichotomy [LIT-READ-of-own-artifact, verbatim-anchored]", "generalised det J identities [PROVED-exact, with can-fail control]", "shape list surviving the Moh/gcd/GGHV pre-flight [CERTIFIED gate output]", "per-shape verdicts [EMPTY-mod-p(p1,p2) or PROVED-exact char-0 EMPTY, never promoted]", "any solution [CANDIDATE-UNVERIFIED until H1-H6 pass, then HIT]"],
      "stop_rule": "Stop at step 1 and declare the lead closed if the recorded dichotomy proof is genuinely general in (C,i,j) and in gamma-degree. Otherwise stop when every shape passing the pre-flight gate has a verdict at two compliant primes, or after 40 CPU-hours with the resisters logged to the NO-VERDICT ledger. Never requeue a TIMEOUT at a longer budget without first measuring the grading-torus rank (the archive's p108 timeouts were structural, not budgetary).",
      "cost": "Low. Steps 1-4 are days of reading and sympy, near-zero CPU. Step 5 is tens of CPU-hours on shapes deliberately kept small in unknown-count; the systems are identity-in-(x,y) coefficient systems, not the 186/302 pentagon.",
      "p_hit": "3% for a genuine sweep-type plane counterexample. ~35% that step 1 alone materially narrows the archive's 'sweep is dead in the plane' headline to a scoped statement, which is a real deliverable either way.",
      "why_not_already_closed": "The dichotomy is recorded as ESTABLISHED but its twist rebuttal is written for one twist shape (w = gamma*u, C = gamma*x^s); the general side-condition system is described in the same file as 'the object nobody has written down'. The one search that touched it was withdrawn as vacuous by Moh before completing, so there is no verdict, and the follow-up ask (monomial twist in K[x,x^-1,y], FABLE-005) is the last unanswered message in the mailbox.",
      "executable_or_research": "executable (step 1 is a two-hour audit that decides whether steps 2-7 are worth launching)"
    },

    {
      "id": "L2",
      "title": "Collision-first search: put the non-injectivity witness INTO the system instead of deriving it",
      "target": "A point of a Keller-plus-collision variety: coefficients of (P,Q) on a pinned Newton polygon TOGETHER with two distinct source points p != q satisfying F(p) = F(q). Any such point is a counterexample candidate that already satisfies HIT gate H3 by construction.",
      "premise_with_citations": "Every system this campaign ever solved encodes Keller plus a Newton-polygon shape; non-injectivity is never a variable, only a downstream consequence. The HIT protocol makes this explicit: H3 NON-INJECTIVITY demands 'two distinct points of C^2 with the same image, given exactly and substituted back' and H4 demands an INDEPENDENT resultant route (p11/reruns2/w3_hit_protocol.py docstring, 'THE GATE'). The deck-group result says exactly where such a collision must live: a plane Keller counterexample cannot have geometric degree 2 and cannot be a Galois covering, so it is a NON-GALOIS covering of degree mu >= 3 (canon/CATCHES.md, 'A COMPLETELY DIFFERENT HUNT: CLASSIFY BY THE DECK GROUP'), and every known counterexample-producing mechanism sits at mu >= 3 (Alpoge's map has geometric degree exactly 3; Gallagher's family realizes every degree >= 3). Note the honest caveat the archive itself records: that theorem RELIES ON UNRE-DERIVED INPUTS (Bayle-Beauville's classification of birational involutions of P^2; Ramanujam/Morrow rationality of boundary components of a smooth compactification of C^2). Adding the collision as unknowns is cheap in structure and expensive in variables (+4 unknowns, +2 equations, +1 saturation for p != q), but it changes what a solution MEANS: a mod-p solution of the collision-augmented system is a direct input to lift/lift_pipeline.py, whereas a mod-p solution of the plain system is only a shape point. The archive's own ranking already says the analogous thing about its best target: 'A NONEMPTY on trackB1 is immediately meaningful and nothing else here is' (mailbox/wave6/frontier/TARGET_QUEUE.md).",
      "method": [
        "1. Pick the two smallest live carriers, in this order: (a) the B=16 corrected ladder cells d=8..12 chart N (d=8 is 30 equations / 23 unknowns on the corrected export, canon/OPEN_ITEMS.md sec 1) — these are the smallest live objects with an explicit degree map deg P = 16(3d-2), deg Q = 16(2d-1), all above 125 and covered by no published elimination (canon/CROSSDOOR.md sec 1); (b) the (8,28)/(3,2) pentagon polygons, after FABLE-003's reduction (Q is linear and redundant; 57-variable rank-drop on a 303x124 structured matrix with exactly ONE inhomogeneous equation — mailbox/AGENT_MAILBOX.md:3562-3675), NOT the 186-variable form.",
        "2. Augment: introduce p = (x1,y1), q = (x2,y2) as unknowns; add P(p) - P(q) = 0, Q(p) - Q(q) = 0; saturate distinctness with a Rabinowitsch variable t*((x1-x2)^2 + (y1-y2)^2) - 1 = 0 (or two charts t*(x1-x2)=1, t*(y1-y2)=1 to avoid the isotropic-line degeneracy over F_p, which the sum-of-squares form silently admits). Keep the existing Keller and shape equations unchanged and keep every existing gauge with its admissibility proof (a forced-nonzero variable of nonzero weight).",
        "3. TWO-SIDED SELF-TEST BEFORE ANY REAL DATA (the archive quarantined three cascades on this rule): run the augmentation on a KNOWN non-injective etale-off-a-curve map to confirm the augmented system is SATISFIABLE and the collision is found; run it on a known polynomial automorphism (triangular, e.g. (x, y+x^m)) to confirm the augmented system is EMPTY. A tool that cannot produce both outcomes is quarantined.",
        "4. Solve mod p at two compliant primes (for the ladder cells pick primes where 12d is a square, since the row-0 roots are sqrt(12d)-irrational — canon/MORNING_SUMMARY.md 'Two structural wins'), with stderr captured and the '[-1]-with-parse-error is a FAILURE' rule enforced.",
        "5. Any mod-p solution: Hensel-lift and rationally reconstruct BOTH the coefficients and the two collision points; substitute exactly; then run H1-H6. A collision point that reconstructs is worth more than the coefficient vector alone because it independently corroborates H3 and gives H4 a target for the resultant route.",
        "6. If the augmented systems resist, run the cheap discriminator FIRST rather than escalating budget: compute the grading-torus rank of each augmented export (exponent-vector nullspace), since a positive-dimensional input makes msolve solve-mode non-terminating by construction and its timeout structural, not budgetary (mailbox/AGENT_MAILBOX.md:1610-1625, OPUS43-012)."
      ],
      "agent_roles": ["system-augmenter (writes the collision block and the saturation charts)", "control agent (builds and runs the two-sided self-test; owns the quarantine decision)", "CAS runner", "torus-rank prober (runs before any budget escalation)", "lift + HIT-gate agent", "adjudicator"],
      "inputs": ["wt/canon/wave5/ms/m16_d8_*.ms and wave6/w6_seed_d8.py (corrected d=8 export; path to be re-confirmed on disk — OPEN_ITEMS and WEEKEND_PLAN disagree)", "wt/pq/trackB1_pentagon.py and trackB1_msolve_export.py", "mailbox/AGENT_MAILBOX.md:3562-3675 (FABLE-003 reduction)", "wt/canon/reruns2/w3_hit_protocol.py", "wt/canon/lift/lift_pipeline.py", "wt/canon/wave4/w4_msformat.py"],
      "outputs_with_labels": ["augmented system exports, content-hashed [CERTIFIED export, sanitiser-gated]", "two-sided self-test log [CERTIFIED or QUARANTINED]", "per-cell verdicts [EMPTY-mod-p(p1,p2) — and note that EMPTY here is WEAKER than the plain system's EMPTY, since it only says no collision of this shape]", "any solution [CANDIDATE-UNVERIFIED -> HIT after H1-H6]"],
      "stop_rule": "Quarantine and stop if the self-test fails either side. Otherwise stop per cell at two primes with agreeing verdicts, or at the first structural (positive-dimensional) timeout diagnosis, whichever comes first. Do NOT report an augmented EMPTY as a cell verdict — it is strictly weaker than the unaugmented one and must be labelled so.",
      "cost": "Moderate. The d=8 augmented system is ~27 unknowns / 32 equations plus saturation — still the smallest live object in the campaign. The pentagon augmented form is only worth attempting on FABLE-003's 57-variable reduction, never on 186 variables.",
      "p_hit": "4%. This is the highest-p_hit lead here, not because a counterexample is likely but because it is the only formulation where a positive is unambiguous and immediately gate-ready. Note the asymmetry: a negative from this lead is nearly worthless (it constrains only collisions of the assumed shape).",
      "why_not_already_closed": "Nothing in the archive ever made non-injectivity a variable. The HIT protocol demands a collision witness at H3, and every run to date would have had to construct one after the fact. The formulation is new; it is also the formulation in which the deck-group theorem (mu >= 3, non-Galois) becomes actionable rather than merely orienting.",
      "executable_or_research": "executable"
    },

    {
      "id": "L3",
      "title": "Adversarial-prime audit: hunt the bad reduction that would make a recorded mod-p EMPTY a false negative",
      "target": "A cell recorded EMPTY mod p that returns NON-EMPTY at a different prime — which points directly at a rational point the campaign eliminated by accident.",
      "premise_with_citations": "The campaign PROVED that modular elimination is unsound for contradictions with a minimal witness: x - 3y = 0, x + 4y - z = 0 with nonzero=[y,z] has the genuine rational solution (3,1,7) but reports '1 closed, 0 open leaves' mod 7, and --verify replays it OK, exit 0 (canon/CATCHES.md, 'MODULAR ELIMINATION IS UNSOUND FOR CONTRADICTIONS'). It then made 'mod-p EMPTY is never a char-0 verdict' a standing rule and applied it as a brake — but never as a SEARCH TOOL. Yet a large fraction of the board is exactly one prime deep: B=16 ladder d=7 EMPTY at p=1000003 ONLY (confirming primes died in container restarts, MISS-1); chart Z d=9,10,11 EMPTY at p=1000033 single prime; the 20 above-125 certified shapes EMPTY at F_65521 ONLY (campaign/audit_tracks/ABOVE_125_STATUS.md); F3 x2 EMPTY at 65521 single prime (MISS-6); and case (2)'s three primes include 32003 and 65537, BOTH ≡ 2 (mod 3), violating the campaign's own p ≡ 1 mod 3 hygiene rule. The archive also records that good-prime selection is UNJUSTIFIED: 'no script certifies that the chosen primes avoid degeneration of the ideal's leading terms' (toolchain reader, gap 8, MANIFEST.md sec D item 2). And it has a live precedent for a five-prime sample overturning a four-prime conclusion (the bottom-edge Galois-orbit retraction, commit 7a747c8, admissible counts 1,1,0,2,3).",
      "method": [
        "1. Build the single-prime register: enumerate every EMPTY verdict in the archive that rests on one prime, or on primes violating p ≡ 1 mod 3, or on primes with no recorded leading-term check. Include the 20 above-125 F_65521 shapes, ladder d=7, chart Z d=9/10/11, F3 x2, and case (2)'s non-compliant pair. Dedup by CONTENT HASH first (p108_821326 and p108_843700 are md5-identical and were double-counted).",
        "2. Build the lucky-prime certifier the archive says is missing: for each system, compute the mod-p Groebner basis at a batch of 8-12 primes and compare LEADING-TERM IDEALS. A prime whose leading-term ideal is in the minority is UNLUCKY by definition; record the batch and the majority signature alongside the verdict. This is a pure re-run, cheap, and it is exactly the mechanism by which a false EMPTY would announce itself.",
        "3. Choose primes ADVERSARIALLY rather than by convenience: primes dividing the numerators/denominators of the system's structure constants, primes dividing discriminants of the known eliminants (degree 9 bottom-edge, degree 1144 case-(2), degree-5 w=-4 edge), and small primes deliberately (5, 11, 17 are the archive's own control primes and are REQUIRED to be rejected by the hygiene check — use them to confirm the checker bites).",
        "4. Any disagreement across primes on a system recorded EMPTY is escalated immediately: re-solve at that prime, extract the point, and hand it to lift/lift_pipeline.py. A coordinate that will not rationally reconstruct is reported NOT RECONSTRUCTIBLE (the pipeline's own honest label) and the flag stands as a targeting hint rather than a candidate.",
        "5. Every run through wave4/w4_msformat.py as a hard gate: assert 0 <= c < p, no zero terms, no nonzero constant generator, and a round-trip re-parse — this is what stops msolve's silent-lie mode, which already produced one fake 20-second EMPTY.",
        "6. Report the audit as a ledger: per system, the prime batch, the majority leading-term signature, the minority primes, and whether the verdict survives. Systems whose verdict rests on a minority prime are RETRACTED to NO VERDICT."
      ],
      "agent_roles": ["register builder (content-hash dedup across all verdict registers)", "lucky-prime certifier author (with the 5/11/17 negative control that must be rejected)", "CAS runner (parallel, one prime per commit, restart-resilient in the style of wave6/bottomedge/sweep.sh)", "lift agent", "adjudicator (owns the RETRACTED-to-NO-VERDICT relabelling)"],
      "inputs": ["wt/canon/campaign/audit_tracks/ABOVE_125_STATUS.md (the 20 F_65521 shapes)", "wt/canon/STATE_FULL.md sec A (ladder single-prime rows)", "wt/canon/campaign/audit_tracks/CASE2_VERDICT.md", "wt/canon/wave6/bottomedge/sweep.sh (the restart-resilient sweep pattern to reuse)", "wt/canon/wave4/w4_msformat.py", "wt/canon/lift/lift_pipeline.py", "wt/canon/MANIFEST.md sec D item 2"],
      "outputs_with_labels": ["single-prime register, deduped by content hash [CERTIFIED bookkeeping]", "lucky-prime certifier with a firing negative control [CERTIFIED, or NO CERTIFIER if the control does not fail]", "per-system prime batches and majority signatures [measured]", "any prime disagreement [FLAG -> CANDIDATE-UNVERIFIED if it reconstructs]", "relabelled verdicts [RETRACTED to NO VERDICT where the majority test fails]"],
      "stop_rule": "Stop when every single-prime EMPTY in the register carries either a second agreeing compliant prime or an explicit NO-VERDICT relabel. Stop early and escalate on the first cross-prime disagreement. Do not let a passing majority test drift into a char-0 claim — the promotion ban stands regardless of how many primes agree.",
      "cost": "Very low per system (the bottom-edge sweep measured ~96 s wall per prime end-to-end including commit), embarrassingly parallel, and restart-resilient by construction. This is the cheapest lead on the board.",
      "p_hit": "3% that any prime disagreement surfaces; conditional on one surfacing, maybe 20-30% that it points at a real rational point rather than a coefficient artefact — so ~1% for a counterexample. But ~60% that the audit retracts at least one currently-quoted EMPTY to NO VERDICT, which reopens territory at negligible cost.",
      "why_not_already_closed": "The archive proved the failure mode and then used it only defensively. Its own toolchain audit lists 'good-prime selection is unjustified' as an open gap with no certifier. Several headline EMPTYs are single-prime or use primes that violate the campaign's own hygiene rule, and were flagged by the campaign itself as MISSes.",
      "executable_or_research": "executable"
    },

    {
      "id": "L4",
      "title": "Refute the GGV conjecture symbolically in d, instead of climbing the ladder cell by cell",
      "target": "An integer d for which the corrected B=16 system admits a solution with mu0 != 0 (equivalently, with mu1, mu2 not both zero). By GGV Thm 1.2 — an IFF — such a solution IS a constructive JC2 counterexample, of degrees (16(3d-2), 16(2d-1)).",
      "premise_with_citations": "GGV Pro Mathematica 27 (2013) Thm 1.2 is an iff: B = 16 holds iff a solution with mu0 != 0 exists, and such a solution is a counterexample. GGV discard NOTHING at B=16 and stall at deg(q1)=5 (canon/ADJUDICATION.md sec 2, sec 3 last bullet); GGHV's 'in [4] this case has been discarded' is adjudicated a sloppy citation. The campaign's own summary calls GGV's conjecture (all solutions have mu1 = mu2 = 0) 'the sharpest theorem-shaped target on the board' and notes that a REFUTATION AT SOME d WOULD BE A CONSTRUCTIVE COUNTEREXAMPLE (canon/STATE_FULL.md sec A last row, sec G.6). The cell-by-cell route is dead by arithmetic: direct Groebner scales ~32x per ladder level (d=6: 42 s; d=7: 1345 s, 6.67 GB), so d >= 8 in char 0 and d = 12, 27 are out of reach, and the shared blocker P2 (rational-function cascade) has failed three times, each caught by its own self-test (canon/OPEN_ITEMS.md THE PLAN P2). But the symbolic-in-d ingredients EXIST and are exact: the corrected system is weighted-homogeneous with exact TORUS RANK 1 and the grading is explicit — wt(a_i) = 2d-i, wt(b_j) = d-j, wt(mu3) = d, wt(mu2) = 2d-1, wt(mu1) = 3d-2, wt(mu0) = 4d-3 (canon/CATCHES.md, 'TORUS RANK (computed, not argued)', which is also the fourth independent confirmation of the (1.2) correction). Structural identities are already d-INDEPENDENT: (F2) mu0 = a2*mu2/3 and (F3) mu0*mu3 + mu1*mu2 = 0 hold for all d >= 4 because the y^3 row of (1.3) caps every index at 3, so H(d=5) == H(d=7) == H(d=9) AS EXPRESSIONS — the campaign's own BLINDSPOT 1 catch, which downgrades 'verified for eight values of d' to one identity. That catch is the lead: d-independence of the hard rows is not a bookkeeping embarrassment, it is the handle for treating d as a symbol. (1.3) itself is CORRECT and reduces exactly to the Wronskian condition p1*q0' - p0'*q1 = mu0 (canon/CATCHES.md, 'WE AUDITED (1.2) AND NEVER AUDITED (1.3)'). And there is a recorded resonance law: singular descent occurs only at d = 3k^2, making d = 12 and d = 27 the distinguished cells (12*12 = 144 = 12^2, roots exactly -1/12 and 1/20; 12*27 = 324 = 18^2, roots -1/20 and 1/28 — canon/OPEN_ITEMS.md sec 2, sec 4).",
      "method": [
        "1. Re-derive the corrected system with d SYMBOLIC from the Poisson bracket (the archive's rederivation.py already starts there and checks all five bracket ODEs; do not transcribe from print — the printed (1.2) row 3 misprint voided two months of work and every artefact built on it is VOID as a statement about B=16).",
        "2. For each row family, decide by expression comparison at two values of d whether it is d-independent (as (F2), (F3) provably are) or genuinely d-dependent. Report the d-independent block as ONE identity with its structural reason, never as N confirmations (the archive's own d-independence lint rule).",
        "3. Use the rank-1 torus grading with symbolic weights to write the general solution of the d-dependent rows as recurrences in the index, and solve those recurrences in closed form in d. This is where the leverage is: the campaign already has 'closed forms + resonance law in hand' and records the proof as unattempted (STATE_FULL sec A).",
        "4. Substitute the closed forms back and reduce the whole ladder to a FINITE set of polynomial conditions in the single symbol d (plus the row-0 quadratic root, which is sqrt(12d)-irrational — carry it as an algebraic element, do not seed it, since seeding the row-0 root covers the whole cell because the relation is mu-free).",
        "5. Now ask two questions of that finite system, both cheap: (i) does it force mu1 = mu2 = 0 for ALL d? — that is a PROOF of the GGV conjecture and closes B=16 outright; (ii) does it admit an integer root d >= 8 with mu0 != 0? — that is a CONSTRUCTIVE COUNTEREXAMPLE. Check the distinguished resonant values d = 12, 27, 48, 75 (d = 3k^2) explicitly and separately, since the resonance law predicts degeneration there.",
        "6. Cross-check any symbolic-d conclusion against the char-0 cell verdicts already proved: d = 3,4,5,6 EMPTY over Q, d = 7 EMPTY in char 0 (26 eq / 20 unk, 1345 s, [-1], clean stderr, mod p agrees). A symbolic-d derivation that does not reproduce those five EMPTYs is WRONG and must be discarded, not patched. This is the lead's own can-fail control.",
        "7. Any candidate d: instantiate, solve the single cell exactly, lift, and run H1-H6."
      ],
      "agent_roles": ["bracket re-deriver (from the Poisson bracket, with the term-by-term control map: every term of every transcribed equation needs a named control that dies if that term is deleted)", "symbolic-d algebraist (recurrences and closed forms)", "resonance specialist (d = 3k^2 cells)", "control agent (owns the d=3..7 reproduction gate)", "CAS runner for instantiated candidates", "HIT-gate agent"],
      "inputs": ["wt/canon/wave6/w6_ggv12_rederivation.py", "wt/canon/wave5/w5_b16_abel.py (the transcription that carried the misprint — read as a cautionary artifact, not a source)", "wt/canon/CATCHES.md sections 'TORUS RANK', 'BLINDSPOT 1', 'WE AUDITED (1.2) AND NEVER AUDITED (1.3)', 'B=16 LADDER: d = 7 IS EMPTY'", "wt/canon/OPEN_ITEMS.md sec 1-4", "wt/canon/papers/ GGV Pro Mathematica 27 (2013) and arXiv:1310.8249v3 eq (3.9)"],
      "outputs_with_labels": ["symbolic-d system with per-term control map [PROVED-exact or UNVERIFIED per term]", "d-independence classification of each row family [PROVED-exact, one identity each]", "closed forms in d [PROVED-exact]", "the finite condition set in d [PROVED-exact]", "either 'mu1 = mu2 = 0 forced for all d' [THEOREM — closes B=16] or a candidate integer d [CANDIDATE-UNVERIFIED -> HIT after instantiation and H1-H6]", "reproduction of d=3..7 EMPTY [mandatory can-fail control; failure voids the derivation]"],
      "stop_rule": "Abort and discard the derivation if step 6's control fails (it must reproduce d = 3,4,5,6,7 EMPTY). Stop on either a proof of the conjecture or an exhausted closed-form attempt; do not fall back to launching more ladder cells, which is what the compute-bound queue already does badly.",
      "cost": "Low CPU, high skilled-agent time. Days to weeks of symbolic work; the instantiated check of a candidate d is a single small solve.",
      "p_hit": "2% for a refutation (a counterexample). Perhaps 15-20% for a proof of the GGV conjecture, which is the opposite outcome and closes the whole B=16 door — a valuable result but not the one this angle wants. Be honest that the exact evidence to date points at rigidity: d = 3..7 are EMPTY in char 0.",
      "why_not_already_closed": "The archive says in its own words that the closed forms and the resonance law are IN HAND and the proof is UNATTEMPTED. The ladder was climbed cell by cell into a 32x-per-level wall instead. The d-independence of the hard rows — recorded as a catch about over-counting confirmations — is precisely the structural fact that makes symbolic d tractable, and nobody turned it around.",
      "executable_or_research": "research direction with an executable core (steps 4-6 produce a decidable finite system either way)"
    },

    {
      "id": "L5",
      "title": "Dixmier / Weyl-algebra route: run the deformation cascade in hbar, not in y",
      "target": "A pair (X,Y) in the first Weyl algebra A_1(C) with [X,Y] = 1 generating a PROPER subalgebra — equivalently, by Tsuchimoto / Belov-Kanel-Kontsevich (JC_2n <=> DC_n), a JC2 counterexample. Operationally: solve the archive's pinned Newton-polygon systems in their hbar-deformed (noncommutative) form, order by order.",
      "premise_with_citations": "The campaign's entire apparatus already lives in the quasi-classical limit: every system is a Poisson-bracket condition [P,Q] = x^r on a pinned Newton polygon (mailbox/campaign notes; the (8,28) pentagon is [P,Q] = x^2, the (9,27) shape is [P,Q] = x). CATCHES.md records that the trustworthy derivations 'start from the Poisson bracket, not from any transcription' (CATCHES.md:640, :1127, :1250). The Weyl-algebra formulation is the hbar-deformation of exactly that bracket, and by the stable equivalence JC2 <=> DC1 a solution of the deformed system is a counterexample — the two problems are the same problem in different clothes. What differs, and is the entire point of the lead, is the FILTRATION available for a cascade. The archive's cascade attempts all propagate in the y-adic / level grading, and all three failed at the same point (division by symbolic pivots, non-propagating substitution, manufactured contradictions), each caught by a self-test; attempt #3 reported 57 dead branches of 61 on a system with an obvious solution (canon/CATCHES.md, 'CASCADE ATTEMPT #3 ALSO FAILS'; OPEN_ITEMS P2). The hbar-adic filtration is a genuinely different one, and it is graded by ORDER OF DEFORMATION rather than by y-degree, so each rung is a LINEAR problem over the previous rung's solution set with an explicit obstruction class — objects far smaller than the 302-equation pentagon. This also makes the lead a natural home for the archive's most reliable machinery: the level-16/15 exact ladder work (wt/l16/breakthrough/) already shows the campaign can descend a rung-by-rung symbolic ladder correctly ONCE the coupling is retained (the sigma^6 | h7 wall was a false obstruction manufactured by dropping the coupling W9 = g9 - (3c1/2c0) z^4 h5 — PENTAGON_LEVEL16.md). Same discipline, new filtration.",
      "method": [
        "1. Fix the equivalence precisely and cite it: state which direction of JC2 <=> DC1 is used and re-derive the dictionary from the Newton-polygon data to the Bernstein filtration on A_1, so the pinned polygons transfer. Label the equivalence LIT-READ until re-derived; do NOT import it into a triage table as a kill or a licence without an anchor-by-exact-quotation record.",
        "2. Set up the hbar-deformation of one pinned system — start with the (8,28)/(3,2) pentagon in FABLE-003's reduced form (57-variable rank-drop, one inhomogeneous equation), NOT the 186-variable form — writing [X,Y] = 1 in A_1 as the commutative bracket plus explicit hbar-order corrections.",
        "3. Solve order by order in hbar. At each order the unknowns enter LINEARLY over the previous order's solution; the obstruction to lifting from order k to k+1 is a single explicit class. Compute it exactly. Retain every coupling term (the l16 lesson: dropping a coupling manufactures a false obstruction) and impose the bounded-support end conditions explicitly (the l16 lesson: omitting the end condition admits spurious solutions that pass every divisibility and resonance test).",
        "4. TWO-SIDED SELF-TEST BEFORE REAL DATA: run the cascade on a Weyl pair known to generate A_1 (must lift to all orders) and on a planted contradiction (must obstruct at the planted order). Quarantine the tool if either side fails. Additionally run the archive's own vacuity pre-check: ask what the obstruction reports when the tested quantity is a product of two functions vanishing at the base point; if that equals the expected answer, the test is vacuous (this is exactly how the rank criterion turned out to be a can't-fail certifier).",
        "5. Outcomes: an obstruction at some finite order KILLS the cell with a small, checkable certificate (much cheaper than a 13.9 GB Groebner run and it is a char-0 statement, not mod p). No obstruction through the full order range, with the tower solvable, gives an explicit (X,Y) — dequantize it and run H1-H6.",
        "6. Only after the pentagon: repeat on the B=16 corrected cells d=8..12, where the unknown counts are 23-40 and the hbar-tower is correspondingly short."
      ],
      "agent_roles": ["literature agent (JC_2n <=> DC_n dictionary, anchored by exact quotation; this is the load-bearing external input and must be labelled UNVERIFIED-HERE until re-derived)", "noncommutative algebraist (hbar-deformation, obstruction classes)", "cascade engineer (coupling retention + bounded-support end conditions, per the l16 discipline)", "control agent (two-sided self-test and the vacuity pre-check)", "HIT-gate agent"],
      "inputs": ["wt/l16/breakthrough/PENTAGON_LEVEL16.md, PENTAGON_LEVEL15_BRANCH2.md, pentagon_level16.py (the exemplar of a correctly-run symbolic ladder)", "mailbox/AGENT_MAILBOX.md:3562-3675 (FABLE-003's 57-variable reduction)", "wt/canon/CATCHES.md 'CASCADE ATTEMPT #3 ALSO FAILS' and 'TWO BUGS IN MY OWN PENTAGON CASCADE' (the failure modes to design against)", "wt/canon/reruns2/w3_hit_protocol.py"],
      "outputs_with_labels": ["JC2 <=> DC1 dictionary [LIT-READ, or PROVED-exact if re-derived]", "hbar-deformed system [PROVED-exact with can-fail control]", "per-order obstruction classes [PROVED-exact char-0 — note these are char-0 statements, unlike the mod-p ladder]", "cell verdict: OBSTRUCTED AT ORDER k [char-0 kill with certificate] or TOWER SOLVABLE [CANDIDATE-UNVERIFIED -> HIT]", "two-sided self-test log [CERTIFIED or QUARANTINED]"],
      "stop_rule": "Quarantine on self-test failure or on the vacuity pre-check flagging the obstruction as structurally forced. Otherwise stop at the first genuine obstruction (that is a result) or when the tower has been carried past the order at which the commutative system's known conditions are exhausted.",
      "cost": "Low CPU, high skilled-agent time. The per-order objects are small; the cost is getting the deformation and the obstruction theory right, which is exactly where three previous cascades died.",
      "p_hit": "1.5% for a counterexample. Higher (~20%) that it yields a char-0 kill certificate on a cell that Groebner cannot reach, which is itself a first for the pentagon — the pentagon has NO VERDICT of any kind by any method anywhere.",
      "why_not_already_closed": "The archive never left the quasi-classical limit. It has no Weyl-algebra or Dixmier artefact of any kind. Its three cascade failures were all in the y-adic filtration; the hbar filtration is untried, and it has the property the y-adic one lacks — each rung is linear with an explicit obstruction.",
      "executable_or_research": "research direction with an executable core (steps 2-4 produce a runnable cascade with a mandatory two-sided self-test)"
    },

    {
      "id": "L6",
      "title": "Riemann-Hurwitz WITHOUT properness, as a targeting device for where a counterexample must live",
      "target": "Not a counterexample directly: an exact Euler-characteristic ledger per Newton-polygon shape that says which of the 24 published-but-untouched open cases (max in [125,150]) are RH-consistent, and specifically which are consistent only with EQUALITY — the tightest shapes, where a counterexample must live if one exists at all in that region.",
      "premise_with_citations": "FABLE-002 (mailbox/AGENT_MAILBOX.md:3479-3561, commit 461df8d) derived chi(F_c) = D - 2*deg(a_0) on the generic fibre, forcing P Newton-degenerate and bounding D <= 17, and called it 'a solver-free emptiness test — the kill test I want run'. It was never run. FABLE-005 (4c6018e) then RETRACTED the unqualified bound, because the dimension-3 mechanism converts ramification into NON-PROPERNESS, and asked Sol to re-derive Riemann-Hurwitz WITHOUT properness, calling it 'the highest value item on the board now'. That message is UNANSWERED, as is everything from 4c6018e to mailbox HEAD 156ba7a. The non-properness point is exactly the crux of JC2: an etale map of C^2 that were also proper would be a covering of a simply connected space, hence injective. So the correct tool is not naive RH but Euler-characteristic additivity for a non-proper fibration, with the corrections at infinity read off the Newton polygon — data the campaign already has for the pinned shapes. Meanwhile the target region is now much better defined than the archive's own folklore: FABLE-006 (156ba7a, HEAD) reports that arXiv:1708.07936 sec 6 enumerates 34 possible counterexamples with max <= 150 and that only the 10 with max < 125 were ever discarded, leaving 24 UNTOUCHED — and says this list REPLACES the campaign's '804 pairs above 125', a figure from lost Sessions 19-38 that no artifact supports. The archive's other counts (429 NO-CHAIN records in wave6/frontier_151_300_map.json; 464 admissible in [125,300]; 474 from the independent gghv_audit re-derivation) are mutually unreconciled. 24 verified open cases is a tractable target list; 429 blocked on an unbuilt compiler is not.",
      "method": [
        "1. GATE FIRST (this is audit gate A2 and it blocks all solver time on this lead): verify the 24-case table against the PUBLISHED arXiv:1708.07936 PDF, not against a text extraction. Fable flagged his own extraction as needing verification and had already retracted three claims that session for that exact error class; and the literature reader confirms Section 6's body was never successfully extracted (arXiv PDF is FlateDecode-compressed; ar5iv truncates at the table of contents). Cross-check against the campaign's own reproduction canon/gghv_audit/all_cases_max_le_150.json (34 entries, 10 with max < 125), which the literature reader names as 'the single highest-value remaining check'.",
        "2. Resolve the (8,28)/(3,4) bookkeeping conflict at the same time: the reproduction file puts A0=[8,1,28] with (m,n)=(3,4), final=[7,4,3] at degrees (108,144), max 144 — ABOVE 125 — while parts of the campaign have treated it as frontier territory alongside the sub-125 survivor (8,28)/(3,2) -> (108,72). Carry (m,n) with every corner from here on; the campaign used the wrong case label for weeks (FABLE-004).",
        "3. Re-derive the Euler-characteristic identity for the generic fibre WITHOUT assuming properness: use additivity of chi over the fibration of C^2 by P = const, with chi(C^2) = 1, and account for the punctures/branches at infinity explicitly from the Newton polygon rather than assuming a proper degree-D cover. State the hypotheses in the SENTENCE, with a recorded domain probe — an input just outside the intended domain on which the identity is REQUIRED to fail (this is the archive's L3 linter rule, and quantifier-scope drift is failure mechanism M3, which has already voided two headline results including the original H1c).",
        "4. Instantiate the ledger on the 24 verified open cases plus both orientations of (72,108). For each: fiber genus, number of punctures, and the resulting consistency inequality.",
        "5. Classify: shapes where the ledger is INCONSISTENT are killed solver-free and char-0 (a real, cheap kill); shapes consistent with SLACK are ordinary; shapes consistent only at EQUALITY are the targeting output — hand those, and only those, to L2's collision-first machinery and to any remaining solver budget.",
        "6. Validate the ledger against known negatives before trusting it: it must be consistent for the polynomial automorphisms it is fed and must reproduce the known kills. A ledger that kills nothing, or kills everything, is a can't-fail certifier and is quarantined."
      ],
      "agent_roles": ["literature verifier (published PDF vs extraction vs the campaign's reproduction; page-image read if no PDF tooling)", "geometer (chi additivity without properness, with an explicit domain and domain probe)", "ledger implementer + control agent (known-negative validation)", "targeting adjudicator (hands the equality-tight shapes to L2)"],
      "inputs": ["arXiv:1708.07936 published PDF sec 5 (family definitions) and sec 6 (the 34-case table)", "wt/canon/gghv_audit/all_cases_max_le_150.json", "mailbox/AGENT_MAILBOX.md:3479-3561 (FABLE-002), :3802-3907 (FABLE-005), :3909-3985 (FABLE-006)", "wt/canon/campaign/audit_tracks/ABOVE_125_STATUS.md (the eps_P + eps_Q = (r+1,1) invariant, a usable sanity check on any candidate reduced pair)"],
      "outputs_with_labels": ["verified 24-case table [LIT-READ against the published PDF, or UNVERIFIED if only the extraction is available]", "properness-free chi identity with domain + domain probe [PROVED-exact or REFUTED-as-stated]", "per-shape ledger [PROVED-exact char-0 kill where inconsistent; NO VERDICT where consistent]", "the equality-tight shape list [targeting output, explicitly NOT a verdict]"],
      "stop_rule": "Do not spend one second of solver time on this lead's shape list before gate A2 passes. Quarantine the ledger if its known-negative validation fails. Stop once every one of the 24 verified cases plus (72,108) has a ledger row.",
      "cost": "Very low CPU. Days of reading and derivation. The gate (step 1) is hours.",
      "p_hit": "<1% directly — this lead does not find counterexamples, it says where to look, and it may also kill shapes. Its honest value is that it is the only cheap char-0 filter on the largest genuinely unsearched region, and that it converts a disputed frontier of 429/464/804 into a verified list of 24.",
      "why_not_already_closed": "Both the RH kill test and the properness-free re-derivation were explicitly assigned and are explicitly UNANSWERED — the mailbox thread ends with FABLE-006 and no reply from Sol, Codex or Opus 5 exists in the archive. The 24-case list is the last thing anyone wrote and nobody has verified it against the published paper.",
      "executable_or_research": "research direction with a hard, cheap, executable gate"
    }
  ],

  "audit_gates_before_compute": [
    "A1 (blocks L1). Read wave6/w6_plane_sweep.py and w6_plane_sweep_search.py in full and publish the sweep dichotomy's EXACT hypothesis set, anchored by exact substring quotation to the file on disk. Specifically: is S assumed affine-linear in gamma? Are (gamma,w) assumed polynomial coordinates? Is the branch-(a) twist rebuttal proved for general (C,i,j) or only for w = gamma*u, C = gamma*x^s? If general, L1 is closed and should be reported as such.",
    "A2 (blocks L6, and any solver time on the 24 open cases). Verify FABLE-006's 24-case table against the PUBLISHED arXiv:1708.07936 PDF sec 6 — not against a text extraction, which its own author flagged after retracting three claims that session for that error class, and which the literature reader confirms could not be decoded (compressed PDF, ar5iv truncated). Cross-check against canon/gghv_audit/all_cases_max_le_150.json. Reconcile or explicitly park the 24 vs 429 vs 464 vs 474 vs 804 count discrepancy.",
    "A3 (blocks any claim that rests on the deck-group theorem, including L2's targeting rationale). The theorem is recorded as ESTABLISHED but RELYING ON UNRE-DERIVED INPUTS: Bayle-Beauville's classification of birational involutions of P^2 and Ramanujam/Morrow rationality of boundary components of a smooth compactification of C^2. Label both UNVERIFIED-HERE until re-derived or anchored, per the archive's rule that no published kill enters a triage table without a re-derivation record. Note the de Jonquieres case IS computed locally and carries the most weight.",
    "A4 (blocks everything on the (9,27) orientation). GGHV Corollary 5.7 is UNPROVEN by local line-by-line audit: after translation the bracket is 1/2 + (lambda/2)x^(-1/2), not in K^x, so [1, Cor 7.2] does not apply; 51 of 66 required conditions are unsupported; failure localised to gghv.txt:1430-1433. No published erratum exists. Treat the (9,27) orientation as OPEN, and treat the two rank-5 sliced test systems (p108_192622, p108_525122) as the outstanding first independent test — currently 3 of 5 leaves EMPTY and 2 unresolved on 525122, 1 unresolved leaf (139 eq / 38 vars) on 192622.",
    "A5 (blocks every solver run). Install Singular 4.3.2 and msolve 0.10.1 — a fresh container has NEITHER, and every campaign script assumes Singular on PATH. Route every export through wave4/w4_msformat.py as a HARD gate: assert 0 <= c < p, no zero terms, no nonzero constant generator, and a round-trip re-parse. Capture stdout AND stderr for every solver call and grep for 'Error when parsing' / 'Error when reading' / 'cannot be 0 modulo'; any '[-1]' co-occurring with a parse error is a FAILURE record, never an EMPTY verdict.",
    "A6 (blocks every new tool). Two-sided self-test on any cascade, branch-and-reduce, augmentation or ledger tool BEFORE real data: a planted-solution system must stay OPEN and a planted contradiction must CLOSE. Plus the vacuity pre-check: ask what the test reports when the tested quantity is a product of two functions vanishing at the base point; if that equals the expected answer, the test is vacuous (this is exactly how the rank criterion was exposed as a can't-fail certifier that 'cannot decide d=27 or anything else'). Plus an AST scan rejecting any compile-time-constant check condition — 45 hardcoded-True checks exist in the tree.",
    "A7 (blocks every new ansatz). Pre-flight degree gate: compute the maximum total degree the ansatz can realise and ABORT if <= 100 (Moh). This single check would have saved the entire 1728-shape plane-sweep search, which was guaranteed to return nothing. Also assert gcd(deg P, deg Q) = 16 or > 20, and below max 125 that the pair is (72,108) in one of its two orientations.",
    "A8 (blocks every emptiness run). Compute equations-minus-variables excess before launching; refuse to schedule an emptiness or parametrisation run when the excess is non-positive, since the variety is then positive-dimensional and cannot be empty. The pentagon truncation ladder was run for two weeks at W=19, underdetermined by 6, which is why msolve OOM'd — and trackB1_pentagon.py:432 witness() had already certified W=12..19 NON-EMPTY over Q, in the repo, with its docstring saying so.",
    "A9 (blocks every requeue). Before extending any TIMEOUT's budget, measure the grading-torus rank of the export. The p108 1800 s timeouts were STRUCTURAL (rank 5, positive-dimensional, msolve solve-mode cannot terminate), not budgetary. Content-hash every export and refuse to schedule a hash already in the register (p108_821326 and p108_843700 are md5-identical and their EMPTY was double-counted).",
    "A10 (blocks every candidate). No candidate is called a counterexample without H1-H6 in reruns2/w3_hit_protocol.py, no step skipped, with H6 shown to have fired (rejects the known negatives, accepts the positive control). H5 gauge independence is the step both wave-1 false positives would have failed. Any detector must fix all three gauges and use an ABSOLUTE normalisation with 'allowed coefficients are O(1)' as an acceptance condition, not a post-hoc check."
  ],

  "risks": [
    "The single largest risk is that the closure I am probing in L1 is in fact general, and 'the sweep mechanism is dead in the plane' is exactly as strong as it reads — in which case L1 collapses at gate A1 for the cost of a two-hour audit. That is the correct price and the gate exists for it.",
    "The deck-group result and the sweep dichotomy are the two loveliest things in this archive and both were produced fast, by one agent, at the end of a long campaign, and neither has been externally refereed. The archive says so itself for the deck-group inputs. A fleet that builds on them without discharging A3 inherits their risk silently — and the archive's own worst failures (GGV (1.2), GGHV Cor 5.7) were exactly inherited-assumption failures.",
    "L2's negative is nearly worthless while its positive is decisive. There is a real risk of a fleet burning weeks producing augmented EMPTYs and mistaking them for cell verdicts. They must be labelled as strictly weaker than the unaugmented verdict, always.",
    "Both L4 and L5 are skilled-agent-time-bound, not compute-bound, and both live in the exact register where this campaign has failed repeatedly: symbolic ladders and cascades. Three cascade attempts died, each caught by its own self-test, and attempt #3 would have falsely killed the campaign's best lead. A4/A6 are not bureaucracy here; they are the difference between a result and a retraction.",
    "Everything built on GGV's printed (1.2) is VOID as a statement about B=16, and several OPEN_ITEMS entries are void for that reason rather than resolved. Any agent re-using a pre-2026-08-21 artefact without checking its provenance is building on sand. Only corrected-system (m16_*) artefacts count.",
    "Infrastructure remains the binding constraint on anything solver-shaped: ~13.3 GiB cgroup cap, container restarts roughly every 30 minutes, ~2.5 h measured process lifetime, and a working tree that has rolled back twice. Nothing needing >20 minutes or >12 GB finishes. L1, L3 and L6 are chosen partly because they survive that; L2's pentagon arm does not, and should be run only on FABLE-003's reduced form or on a persistent >=32 GB host. Push after every commit; 37+ local commits were unpushed when the record was written.",
    "Count drift is endemic and unreconciled (24 vs 429 vs 464 vs 474 vs 804 above-125 cases; 33 vs 36 virgin timeouts; the '41 timeout shapes' aggregate that is nowhere enumerated; '184 tail hashes' and '189 non-closing records' that match nothing in the archive at all). Any plan that quotes one of these numbers as an archive fact will propagate an error.",
    "Two false-positive 'hits' have already occurred, both gauge artefacts, both caught pre-commit. A fleet running many parallel opportunistic searches raises the false-positive rate proportionally. H5 is the specific defence and it must not be skipped for a candidate that 'obviously looks right'.",
    "Honest prior: nothing in this archive is evidence that a plane counterexample EXISTS. The archive's own framing is that the deliverable is not 'counterexample or bust', and its two strongest new structural results — no plane sweep, non-Galois mu >= 3 — jointly say a plane counterexample must use a mechanism unknown even in dimension 3. That is a sobering finding and this plan should be run with that as the prior, not against it."
  ],

  "what_would_change_my_mind": [
    "Gate A1 showing the sweep dichotomy is proved for general (C,i,j) and general gamma-degree. That closes L1 outright and I would reallocate its budget to L2 and L3.",
    "A cross-prime disagreement from L3 on any system currently recorded EMPTY. That would be the single most informative event available at this cost, and it would immediately promote that cell above everything else on the board — including the pentagon.",
    "A rational reconstruction from the lift pipeline that verifies by exact substitution, on any candidate from any lead. That flips the entire plan into HIT-protocol mode and everything else stops.",
    "L4 producing a proof that mu1 = mu2 = 0 for all d. That closes the B=16 door — the one genuinely open counterexample corridor found in the literature — and would mean the only remaining home for a counterexample is the above-125 region, which would make L6's gate A2 and the chain-compiler question the whole game.",
    "A re-derivation of Bayle-Beauville / Ramanujam-Morrow that FAILS to support the deck-group theorem. That would reopen geometric degree 2, which is the first thing anyone would try and is currently believed provably empty.",
    "Independent confirmation that GGHV Cor 5.7 is repairable after all. That re-closes the (9,27) orientation and shrinks the sub-125 board back to one shape, which would push more weight onto L4 and L6.",
    "A first EMPTY verdict on the pentagon by any method (it currently has NO VERDICT of any kind, by any method, anywhere). That would retire the campaign's central territory and, combined with a case-(2) char-0 confirmation, would raise the published bound 108 -> 125 — a real theorem, and a signal that the elimination program was worth more than this angle credits.",
    "Evidence that the hbar-adic filtration in L5 obstructs at low order on a cell the commutative system cannot decide. That would make L5 the cheapest kill engine on the board and reorder the whole plan around it."
  ]
}
```