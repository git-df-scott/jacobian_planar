```json
{
  "claims": [
    {
      "id": "C1",
      "refuted": true,
      "verdict": "SOUND_WITH_CORRECTION",
      "reasoning": "I could not verify GGV Pro Mathematica 27 (2013) Thm 1.2's exact wording from the artifacts available (only the campaign's own paraphrase in enumerated-corners.md L122-123, structural.md L81-82, skeptic.md L190), and the campaign has already been burned once by trusting a printed GGV equation (the (1.2) row-3 misprint), so the iff must be re-read from the page image before anything is built on it. Even granting the iff literally, three things the plan omits stand between 'solution with mu0 != 0' and 'counterexample'. (a) These theorems characterise EXISTENCE of a counterexample with the given invariant; the solution vector is leading/Puiseux-type data (a_i, b_j, mu_k) for a cell, not a pair (P,Q). Reconstructing polynomials of degrees (16(3d-2), 16(2d-1)) from it is a separate, non-trivial step, and it is exactly the step no artefact in this archive has ever performed. (b) Cell nondegeneracy: the degree pair is attached to the cell deg(q1)=d, so the solution must have the leading coefficient of q1 nonzero (and any other coefficient the cell decomposition assumes nonzero). A solution with that coefficient zero lies in a lower cell and carries the WRONG degree pair; mu0 != 0 alone does not pin the cell. (c) Field and gauge: the solution must be over a characteristic-zero field, exactly represented. A mod-p solution proves nothing (JC is FALSE in char p — x^p - x is Keller and non-injective), and every gauge normalisation used in the export must have a recorded admissibility proof; the campaign's two prior 'hits' were both gauge artifacts. Note also the internal caution the plan itself records: F2 (mu0 = a2*mu2/3) already forces mu0 into the ideal, so 'mu0 != 0' is not a free side condition but a saturation that must be applied to the corrected system and its admissibility checked, not assumed.",
      "correction_to_plan": "Downgrade the headline from 'a NON-EMPTY at d=8 IS a counterexample' to 'a NON-EMPTY at d=8 is the only CANDIDATE on the board whose promotion path is an iff rather than a necessary condition'. Add three mandatory preconditions before any such claim: (i) page-image re-read of Thm 1.2 recording its exact hypotheses and its constructive/reconstruction content, labelled LIT-READ; (ii) an explicit saturation pinning the cell (leading coefficient of q1 nonzero) alongside the mu0 saturation, with both charts of any resulting case split run; (iii) an explicit reconstruction gate H0 producing (P,Q) in Q[x,y] with verified degrees 16(3d-2), 16(2d-1) — H1-H6 cannot be entered without it. Forbid quoting a mod-p non-empty as anything but 'a lift target'."
    },
    {
      "id": "C2",
      "refuted": true,
      "verdict": "UNSOUND",
      "reasoning": "The core math is right but the plan's ranking is provably inverted and three degeneracies are unguarded. (1) DECISIVE RANKING ERROR: the augmented variety projects onto the base (Keller + polygon) variety by forgetting (p,q). Hence NONEMPTY(augmented) implies NONEMPTY(base) — the augmented system can never have a hit where the base system is empty. Its p_hit is therefore bounded above by the base cell's p_hit, so 4% (the plan's highest) cannot exceed the 0.5-3% assigned to the same cells' base systems. The augmentation changes only how INTERPRETABLE a hit is, never how likely. (2) COST: for the degrees at issue (>= 100 by Moh), P(x1,y1)-P(x2,y2) is a degree->=100 dense polynomial in the four new variables coupled to every coefficient unknown. This raises Groebner degree massively on systems that already OOM at 13.9 GB. So the lead is strictly harder AND no more likely to hit. (3) det J DEGENERACY: if Keller is encoded as det J = c with c a free unknown, the component c = 0 is a genuine solution locus on which P,Q are algebraically dependent, so collisions exist for free and the collision block is satisfied vacuously. That component will dominate. c must be normalised to 1 or Rabinowitsch-saturated. (4) DEGREE-DROP DEGENERACY: nothing forces the leading coefficients nonzero, so a solution can have deg P, deg Q below the pinned values, making the Newton-polygon conditions vacuous and the object outside the intended family. (5) CHAR-p: over F_p, Keller non-injective maps are abundant (Artin-Schreier x^p - x, and additive/Frobenius composites), so a mod-p NONEMPTY on the augmented system is close to expected noise and carries essentially no char-0 evidence — the negative-control suite must include a check that these do not populate the pinned shape. (6) ISOTROPIC LINES: the plan's framing treats (x1-x2)^2 + (y1-y2)^2 as a mod-p problem. It is not — over C the locus (y1-y2) = +-i(x1-x2) consists of genuinely distinct points, so t*((dx)^2+(dy)^2) = 1 SILENTLY DELETES real solutions in characteristic zero as well. It is an unsoundness for EMPTY verdicts, not a mod-p nuisance. The two-chart fix is correct — {dx != 0} union {dy != 0} = {p != q} exactly — but EMPTY then requires BOTH charts empty. If a solution over C is found and all of (3),(4) are guarded, then yes: a non-injective polynomial map C^2 -> C^2 with det J in C* is a genuine JC2 counterexample (injectivity is what JC reduces to, by Bialynicki-Birula–Rosenlicht), so H3 is satisfied by construction.",
      "correction_to_plan": "Keep the augmentation as a CANDIDATE-INTERPRETATION tool applied only to cells that have already returned NONEMPTY on the base system; never as a primary search. Drop p_hit from 4% to strictly below the corresponding base cell's figure (<=0.3%). Delete the sum-of-squares Rabinowitsch form from the spec entirely (it is unsound over C, not just over F_p) and mandate the two charts with EMPTY requiring both. Add: normalise det J = 1 (or saturate c), add leading-coefficient nonvanishing saturations pinning deg P and deg Q, and add a char-p negative control that Artin-Schreier/Frobenius-type non-injective Keller maps are excluded by the shape equations before any mod-p NONEMPTY is reported."
    },
    {
      "id": "C3",
      "refuted": true,
      "verdict": "UNSOUND",
      "reasoning": "The plan's stated verification check is in the WRONG DIRECTION for emptiness. If every original generator f_i reduces to 0 modulo the reconstructed G, that establishes I = <f> is contained in <G>; Buchberger's criterion on G establishes only that G is a Groebner basis of <G>. Together they give <G> CONTAINS I and nothing more. For an EMPTY verdict one needs 1 in I, i.e. the OTHER containment <G> contained in I — the check as specified is vacuous when G = {1}, since <1> contains everything. (Note the check is exactly the right one for the opposite verdict: if G is verified as above and 1 is NOT in <G>, then I is a proper ideal, so V(I) over C is NONEMPTY by the Nullstellensatz. The plan has the certifier pointed at the wrong verdict.) What emptiness actually requires is a cofactor certificate: explicit h_i in Q[x] with 1 = sum h_i f_i, verified by exact expansion over Q. That single object is a complete, self-contained, checker-verifiable char-0 proof — no Groebner verification, no lucky-prime argument, and no CRT correctness argument is needed, since the certificate is checked against the ORIGINAL generators. Both routes the claim proposes are therefore sound only in the form 'reconstruct candidate cofactors (from the traced modular computation, or by bounded-degree linear algebra over Q) and verify them exactly'. Two further points. (a) The linear-algebra route is one-sided: SUCCESS at degree bound D proves emptiness; FAILURE at D proves nothing unless D exceeds an effective-Nullstellensatz bound (Kollar-type, ~d^n), which is astronomically beyond reach here. Record a failure as 'no certificate at D', never as evidence of non-emptiness. (b) The ideal certified must be the SATURATED one — emptiness of V(I) is not the campaign's question; emptiness of V(I) minus the degeneracy loci is, so the Rabinowitsch/saturation variables must be inside the f_i the certificate is checked against. Finally: lucky-prime majority voting on leading-term ideals is a search heuristic and contributes ZERO to the proof; it should not be recorded as evidence.",
      "correction_to_plan": "Rewrite the CERT-1 acceptance criterion: an EMPTY is promoted to PROVED-exact if and only if explicit h_i in Q[x] are produced with 1 = sum h_i f_i, verified by exact expansion against the original (saturated) generator list, by an independent checker. Delete 'each generator reduces to 0 mod G plus Buchberger' as the emptiness criterion; retain it, correctly labelled, as a NONEMPTY certifier (proper ideal containing I). State explicitly that CRT/rational-reconstruction/lucky-prime steps are heuristics for FINDING the certificate and carry no evidential weight themselves, and that a bounded-D linear-algebra failure is 'no certificate', not evidence."
    },
    {
      "id": "C4",
      "refuted": true,
      "verdict": "SOUND_WITH_CORRECTION",
      "reasoning": "The DIRECTION IS RIGHT, contrary to what one might expect from the way the plan writes it. The known implications are: DC_n implies JC_n (Dixmier's conjecture is at least as strong), and JC_{2n} implies DC_n (Tsuchimoto 2005; Belov-Kanel–Kontsevich 2005-07); together these give stable equivalence of the two conjecture families. Taking n = 1 in the second: JC_2 implies DC_1, whose contrapositive is exactly the plan's target — a pair (X,Y) in A_1(C) with [X,Y] = 1 generating a proper subalgebra (i.e. a non-surjective endomorphism of A_1, a counterexample to DC_1) yields a counterexample to JC_2. So the plan's arrow is correct and should be stated as JC_2 => DC_1, used contrapositively. Two real corrections. (a) EFFECTIVITY: the JC_{2n} => DC_n passage runs through reduction mod p and p-curvature/Poisson-centre arguments; it is not known to be effective in the sense the plan needs. An explicit ¬DC_1 witness does not visibly hand you an explicit (P,Q) in Q[x,y]^2 of controlled degree, so the HIT protocol — which requires explicit polynomials — is not directly applicable to the output of this lead. The lead's deliverable would be 'JC2 is false', with the explicit plane map a further research problem. This must be stated, not glossed. (b) THE DEFORMATION DESCRIPTION IS WRONG AS WRITTEN. Two errors. First, the quasi-classical shadow of [X,Y] = 1 is a POISSON-COMMUTING pair: because [X,Y] = 1 has degree far below deg X + deg Y - 2, the symbols satisfy {sigma(X), sigma(Y)} = 0. The archive's pinned systems are {P,Q} = x^r with r >= 0, i.e. NOT Poisson-commuting — they arise from a different (Newton-polygon/leading-form) reduction. Identifying the two is an unproved step, and if it is false the entire hbar tower is being built over the wrong base point. Second, 'each rung linear with an explicit obstruction class' is over-simplified in exactly the way that killed cascade attempts #1-#3: the order-(k+1) equations are indeed linear in the order-(k+1) unknowns, but the obstruction class DEPENDS ON THE CHOICE MADE AT EVERY LOWER ORDER, so it is a function on the (positive-dimensional, branching) solution variety of the truncated tower, not a single computable class. A nonvanishing obstruction on one branch kills nothing. That is the same symbolic-pivot/branch-covering problem the y-adic cascades failed on; the hbar filtration does not escape it. Finiteness of the tower also needs an argument (it holds if set up as a filtered/Rees deformation of a fixed-degree pair, not as a formal hbar-adic one).",
      "correction_to_plan": "State the implication explicitly and correctly in the lead text: DC_n => JC_n and JC_{2n} => DC_n (Tsuchimoto; Belov-Kanel–Kontsevich); the lead uses the contrapositive of JC_2 => DC_1. Add a labelled caveat that this passage is not known to be effective, so a ¬DC_1 witness yields 'JC2 false' but not an explicit plane map, and H1-H6 cannot be run on it. Replace 'each rung is linear with an explicit obstruction class' with: 'rungs are linear over a BRANCHING lower-order solution variety; the branching-cover discipline and coverage proof demanded for the collision cascade applies here identically'. Add a prerequisite step: prove (or refute) that the archive's {P,Q} = x^r pinned systems are the associated-graded shadow of the Weyl problem — the tower's base point must be a Poisson-COMMUTING pair, and if the pinned systems are not, this lead is aimed at the wrong object. Set up the deformation as a filtered/Rees deformation so the tower is finite."
    },
    {
      "id": "C5",
      "refuted": true,
      "verdict": "SOUND_WITH_CORRECTION",
      "reasoning": "The claim's first half is correct and the plan is honest about it in places (enumerated-corners L27, engineering L94 quoting FABLE-004 'necessary-only'), but the protocol as specified does not close the gap. What a NONEMPTY point on a reduced-Newton-polygon coefficient system gives is an admissible configuration of LEADING/boundary data. Getting from there to a counterexample requires, in order: (H0a) an exact char-0 point — mod-p points carry no information, since JC is false in char p; (H0b) EXACT LIFT TO FULL POLYNOMIALS — the reduction discards all coefficients interior to / below the polygon, and the existence of any completion of the admissible leading data to a genuine Keller pair is an unsolved obstruction problem in its own right. This is the load-bearing missing gate: no artefact in the archive performs it, and H1-H6 as listed presuppose an explicit (P,Q). (H0c) verified degrees matching the claimed pair. Given an explicit (P,Q) in Q[x,y]^2, H1-H6 IS sufficient, but only under two sharpenings: H2 must be a GLOBAL SYMBOLIC IDENTITY — det J(P,Q) - c expanded to the zero polynomial with c in Q* — never a check at sample points; and H3's two points must be exact algebraic numbers in an exact representation with P(p) = P(q), Q(p) = Q(q) verified exactly in the number field, not numerically. With those two, H2 + H3 alone constitute a complete proof (a non-injective Keller map over C is a JC2 counterexample); H1, H4, H5, H6 are hygiene and independent corroboration, and H5 is correctly identified as the step both prior false 'hits' would have failed. One addition worth making: H3 has an equivalent and often cheaper exact form, [C(x,y) : C(P,Q)] >= 2 computed by exact resultants, which is what H4 should be — degree of the field extension, not a second numerical route.",
      "correction_to_plan": "Insert a gate H0 (exact reconstruction) ahead of H1 with three parts — exact char-0 point, completion of the leading data to full P,Q in Q[x,y] with verified degrees, and a recorded statement that failure to complete leaves the verdict at ADMISSIBLE-SHAPE, never CANDIDATE. Sharpen H2 to 'det J - c expands to the zero polynomial, c in Q*, symbolic, no sampling' and H3 to 'exact algebraic points with equality verified in an exact number-field representation'. Re-specify H4 as the exact field-degree computation [C(x,y):C(P,Q)] >= 2 by resultants. Add an explicit label ADMISSIBLE-SHAPE to the verdict schema so a NONEMPTY reduced-system point cannot be reported in the same vocabulary as a candidate."
    },
    {
      "id": "C6",
      "refuted": true,
      "verdict": "SOUND_WITH_CORRECTION",
      "reasoning": "The METHOD is legitimate: Euler characteristic is additive over constructible stratifications for complex algebraic varieties (chi = chi_c), and a polynomial P: C^2 -> C is a locally trivial C-infinity fibration over the complement of its finite bifurcation set B(P). So 1 = chi(C^2) = (1 - k)*chi(F_gen) + sum_i chi(F_{c_i}) with k = |B(P)| is a valid identity requiring NO properness, and it is a genuine constraint. FABLE-005 was right to retract the naive-RH version and right that the correct replacement is chi-additivity. Three adversarial points the plan omits. (1) THE LIKELY-VACUITY PROBLEM: the whole content is in the corrections at infinity — B(P) contains values where the topology jumps with no affine critical point (vanishing cycles at infinity; Broughton's x + x^2*y is the standard example). Reading those off the Newton polygon requires P to be convenient and NONDEGENERATE with respect to its Newton boundary (Kouchnirenko / Nemethi-Zaharia). But FABLE-002's own conclusion was that the constraint FORCES P NEWTON-DEGENERATE. So the read-off tool is valid precisely where the targets are not, and applying it to the pinned shapes without a degeneracy audit would produce exactly the quantifier-scope failure (M3) the plan says it is guarding against. (2) DIRECTION: even when the identity is consistent, that is NO VERDICT; only an INCONSISTENCY is a kill, and it is a kill only if the at-infinity term is bounded rigorously (an inequality, not an assumed equality) on the degenerate case too. The plan does label the consistent case NO VERDICT, which is correct. (3) NOVELTY RISK: chi/topology-of-fibres constraints on Keller pairs are a developed literature (Nguyen Van Chau and others on the non-proper value set / asymptotic variety of Keller maps, Orevkov). Deriving this fresh without a literature read risks re-deriving a published condition and mislabelling it as a new filter — the same failure that produced the 'GGHV discarded (80,112)' confusion.",
      "correction_to_plan": "Reframe the lead as 'chi-additivity over the P = const fibration' and drop the phrase Riemann-Hurwitz entirely (RH is the properness-dependent statement that was already retracted). Make the Newton-nondegeneracy hypothesis an explicit, checked precondition per shape: if P is Newton-degenerate on a shape, the polygon read-off of the at-infinity correction is INADMISSIBLE for that shape and the ledger entry is NO VERDICT, not a kill — expect this to void most targets. Require an inequality-form bound on the at-infinity term valid in the degenerate case if any kill is to be claimed. Add a mandatory literature step (Nguyen Van Chau, Orevkov, Suzuki-type chi formulas) before deriving, so the identity is labelled either LIT-READ or genuinely new."
    },
    {
      "id": "C7",
      "refuted": true,
      "verdict": "SOUND_WITH_CORRECTION",
      "reasoning": "There IS a plausible mathematical mechanism, and the plan does not name it, which is why it currently reads as numerology. The chain-to-reduced-polygon compiler applies a sequence of elementary corner transformations and then extracts a coefficient system from the TERMINAL polygon data; if that extraction depends only on the final polygon and its multiplicities, and those are determined by the last few chain entries, then the reduced system genuinely factors through the tail — a provable structural statement, not a statistical one. That is the right thing to establish. As currently specified the evidence is worthless in three ways. (a) 34 chains -> 26 distinct tails with 'zero predictor violations' is measured on the SAME data the predictor was read off; it is a fit, not a test. (b) The archive's own record says naive pattern-fitting from the known examples already produced three retracted results ((9,27) has A0 = n*(3,9) exactly, (8,28) does not), so this exact epistemic move has a documented failure history here. (c) Even if true, tail-closure only DEDUPLICATES compute; it does not enlarge the searched region, and a NONEMPTY on a shared tail system still requires per-chain lifting because the degree pairs differ — so its value to a counterexample hunt is near zero and it should not be load-bearing for any verdict. The 20-sample saturation count is also the wrong experiment: it measures a rate, which cannot distinguish 'the map factors through the tail' from 'the sample happened to reuse tails'.",
      "correction_to_plan": "Replace the 20-sample saturation test as the go/no-go with a FACTORIZATION AUDIT of the compiler itself: read the construction and determine, as a proof or a refutation, whether the reduced-system extraction depends on chain entries before the tail. That is cheaper than 20 compilations and settles the question. Keep the sample count only as a corroborating MEASURED number, and hold the saturation claim at CONJECTURE regardless of the rate. State explicitly that tail-closure is a compute-dedup result with no evidential effect on any EMPTY or NONEMPTY verdict, and that shared tail systems still require per-chain lifting because degree pairs differ."
    },
    {
      "id": "C8",
      "refuted": true,
      "verdict": "UNSOUND",
      "reasoning": "The per-lead figures are inflated by roughly an order of magnitude, and several are internally incoherent. Decompose honestly: P(hit) = P(JC2 is false) * P(a minimal counterexample lies in the region this campaign can reach) * P(this campaign gets a VERDICT rather than a TIMEOUT there) * P(the verdict is NONEMPTY and survives lifting and H1-H6). Charitable numbers: P(JC2 false) maybe 5-10% (Keller 1939; Moh's floor of 100; GGV/GGHV pushing the floor to 108-125; Nguyen ~104; the enumeration exists precisely because its authors expected every case to die; the dimension->=3 refutation explicitly leaves the plane untouched, which is evidence about method, not about the plane being false). P(in reach | false): the enumerated region is max <= 150 while the honest frontier extends to 300+ and beyond; call it 10-20%. P(verdict | in reach): the archive's own measurement is brutal — 49 TIMEOUTs, OOM at 13.9 GB on the live cells, 0 non-EMPTY across ~46 decided systems, both historical 'hits' gauge artifacts; call it 20-40% even with a 128 GB host. Product: ~1e-3, i.e. ~0.1% for the WHOLE campaign, not per lead. Every per-lead figure at or above 1% is inflated, and the four plans' figures must NOT be summed — the leads overwhelmingly re-search the same enumerated shape list and are strongly positively correlated through the single question 'is JC2 false at these degrees'. Specific inflations: (i) collision-first at 4% is provably the worst offender — its solution set projects into the base system's, so its p_hit is bounded ABOVE by the base cell's 0.5-3%; being the highest figure in its plan is a logical error, not a judgement call. (ii) sweep-type at 3% re-launches above the Moh gate a family whose only prior run was killed as vacuous; raising the degree gate makes the systems bigger, not the variety more likely nonempty. (iii) Dixmier at 1.5% requires solving a noncommutative strengthening of a commutative problem the campaign cannot solve, via a passage that is not known to be effective. (iv) p108 leaves at 2-3% cite '3 of 5 leaves already EMPTY' — that is evidence FOR emptiness of the rest, and the text says so, then prices it as the highest per-run figure anyway. The most defensible number on the board is the B=16 ladder, and only because it is the one iff; but d = 3..7 are all char-0 EMPTY, and the excess of conditions over unknowns grows with d, so conditional emptiness INCREASES up the ladder — 0.3-0.5% should be 0.1-0.2%. The plans' honest, well-supported deliverable is not a hit: it is the first characteristic-zero ideal-membership-certified EMPTY on real systems plus a defensible map of what is genuinely unsearched, and the enumerated-corners plan's own 1-2% overall (with 0.3% surviving H1-H6) is the closest to right and is still ~3-10x high.",
      "correction_to_plan": "Rewrite the probability section around a single decomposed campaign-level figure of ~0.1% (order 1e-3) for a verified plane counterexample, showing the four factors, and state explicitly that per-lead p_hits are strongly positively correlated and MUST NOT be summed. Cap collision-first strictly below its base cell's figure (<=0.3%) with the projection argument spelled out. Reduce sweep-type to <=0.5%, Dixmier to <=0.3% with the effectivity caveat, p108 leaves to <=0.5%, and B=16 d=8 to 0.1-0.2%. Promote the certified char-0 EMPTY pipeline and the closure/unsearched-region map to the plan's stated primary deliverables, with the counterexample framed as the low-probability tail of doing that work honestly."
    }
  ],
  "honest_probability_note": "Decomposing P(hit) = P(JC2 false, ~5-10%) * P(minimal counterexample in the reachable max<=150 enumerated region, ~10-20%) * P(a verdict rather than a TIMEOUT there, ~20-40% even on a 128 GB host) * P(NONEMPTY survives lifting and H1-H6, <=50%) gives ~1e-3 for the ENTIRE campaign, not per lead. Per-lead figures of 2-4% are 10-40x high and, being strongly correlated through the single question 'is JC2 false at these degrees', must never be summed. The one figure that is provably wrong rather than merely optimistic is collision-first's 4%: its solution set projects into the base system's, so it cannot exceed the base cell's own p_hit. The defensible deliverables are the certified characteristic-zero EMPTY (which requires a cofactor certificate 1 = sum h_i f_i, NOT the reduce-to-zero-plus-Buchberger check the plan specifies, which certifies the opposite verdict) and an honest map of the unsearched region."
}
```