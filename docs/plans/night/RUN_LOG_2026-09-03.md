# Night run log, 2026-09-03

Working branch: `claude/jc2-counterexample-hunt-handoff-w369mc`. Engines: msolve 0.10.1 (source build), Singular 4.3.2 (apt), PARI 2.15.4. Box: 4 cores, 15 GB, no swap.

Labels follow `docs/plans/CE_HUNT_PLAN.md` (handoff branch `claude/jc2-handoff-audit-hartnc`). A timeout, OOM, segfault or empty output is never a verdict.

## 05:05Z Plant

- Worktrees: canon 24a06fc, mailbox 156ba7a, p11 e4fa5ce, l16 1e3ac1f, handoff ae58bd3.
- Smoke tests passed: `be_c2is0_p1000003.ms` returns `[-1]` in 1 s; poisoned constant (2000006 at p=1000003) exits 1 with the parse error, no `[-1]`.

## 05:10Z Correction to the (9,27) record

`mailbox/wave6/frontier/P108_RESULTS.md` calls all four p108 systems "the (9,27) branch". The ledger `p11/wave6/pair108_results.json` says otherwise:

| system | shape tag | orientation |
|---|---|---|
| p108_192622 | (9,27)/9,24/11/3,8 (m,n)=2,3, c'=0, eps=((1,1),(1,0)) | (72,108), the (9,27) orientation |
| p108_584120 | (9,27)/9,24/11/3,8 (m,n)=2,3, c'=3 | (9,27) orientation, no committed .ms |
| p108_525122 | (8,28)/11/4,7 (m,n)=3,2, c'=0, eps=((1,0),(2,1)) | (108,72), the (8,28) orientation |
| p108_821326, 843700 | (8,28) and (8,32) c'=0 eps=((2,1),(1,0)) | EMPTY in 0.4 s (Singular facstd) |
| p108_867707, 638901, 286301, 671059, 621292 | (8,28)/(8,32) variants | TIMEOUT, no committed .ms |

So the only committed test of the (9,27) orientation is p108_192622. Ten shapes at (72,108) exist in the compiler output for these three chains; two are EMPTY, eight have no verdict.

## 05:10Z Structure of the two committed systems

| file | declared vars | unused vars | gens | p | torus rank (all rows) |
|---|---|---|---|---|---|
| p108_525122.ms | 28 | c_1, x | 140 | 65521 | 10 (8 on used variables) |
| p108_192622.ms | 40 | c_1, x | 139 | 65521 | 18 (16 on used variables) |

The unused ring variables `c_1` and `x` make every solve-mode run positive-dimensional by construction; the archive's 1800 s solve-mode timeouts on these files were structural. Sanitized copies (unused variables dropped, coefficient range and round-trip checked) are the inputs below.

## 05:10Z Launched

- `msolve -g 2 -t 2` on sanitized p108_192622 (38 vars, 139 gens) and p108_525122 (26 vars, 140 gens) at p=65521, 6.2 GB address-space cap each. Status: running.
- Regeneration of all ten (72,108) shape systems from `trackD_extract.build_singular` at p=65521, to recover the six shapes with no committed export and to check the four committed ones by content.

## 05:20Z Which (9,27) shape is the paper's

GGHV 2022 (arXiv:2204.14178) Proposition 4.1 reduces the (9,27) case to exactly one pair in L^(1) = K[x, x^-1, y] with [P,Q] = x:

    N(P) = {(0,0), (1,1), (6,16), (6,18), (0,18)},  N(Q) = {(0,0), (1,0), (9,24), (9,27), (0,27)}

This is the compiler's c' = 9, eps = ((1,1),(1,0)) shape, and it is the polygon Corollary 5.7 (the broken proof) claims to kill. The compiler's c' = 0, 3, 6 shapes and the swapped-eps shapes are supersets the campaign added; p108_192622 (c' = 0) and p108_584120 (c' = 3) are not the paper's case. The paper's shape has no committed export and no recorded run. It is being regenerated now (`p108_80de5c`), and it is the priority run of the night.

For (8,28) with (m,n) = (3,2), Proposition 4.3 gives cases (1) pentagon = compiler c' = 4, eps = ((1,0),(2,1)) [P has (1,0), Q has (2,1)] and (2) quadrilateral = c' = 0 with the same eps. Case (2) is the campaign's EMPTY at five primes; case (1) has no verdict. The extractor export of case (1) is `p108_638901` (no committed .ms), being regenerated.

The original pair for (9,27): N(P) = 2·{(0,0),(1,0),(9,24),(9,27),(0,9)}, N(Q) = 3·(same), degrees (72,108), edge forms x^2 (x y^3 - a)^16 on the lower-right edge, x^18 y^48 (b y^3 + c)^2 on the vertical edge and y^18 (x y^2 - a')^18 on the upper-left edge (from the proof of Proposition 4.1). Recorded here for the mu-first analysis; deriving the covering degree from the chain data needs the full Puiseux resolution of the three clusters and is deferred.

## 05:22Z Positive controls of the extractor

Two realizable shapes were pushed through `trackD_extract.run` at p = 65521:

| control | pair with [P,Q] = x^r | shape | verdict |
|---|---|---|---|
| posctl_a | P = x + x^6 y^3, Q = x^2 y, r = 2 | N(P) = {(0,0),(1,0),(6,3)}, N(Q) = {(0,0),(2,1)} | 1 live component, as required |
| posctl_b | P = x + x^6 y^2 + x^15 y^5, Q = x^3 y, r = 3 | N(P) = {(0,0),(1,0),(6,2),(15,5)}, N(Q) = {(0,0),(3,1)} | 1 live component, as required |

Both pairs come from the automorphism (x + y g(y), y) of K[x,y] pushed through psi(y) = x^k y, which multiplies the bracket by x^k. Coverage is weak: each control produces only 2 support conditions because N(Q) has a single row above j = 0. A control with several Q-rows needs an automorphism fixing the x-axis pointwise with a non-trivial second component and was not found by hand. The extractor's tail script has a Singular syntax error in the per-component `dim` print (ternary operator), which is cosmetic: the VERDICT line is still printed.
