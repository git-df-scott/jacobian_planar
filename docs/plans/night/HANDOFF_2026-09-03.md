# Handoff: JC2 counterexample hunt, session of 2026-09-03

Written at the close of the overnight run for a fresh session. Read this file, then `docs/plans/night/RUN_LOG_2026-09-03.md` (the verified record), then `docs/plans/HANDOFF_CE_HUNT.md` and `docs/plans/CE_HUNT_PLAN.md` on branch `claude/jc2-handoff-audit-hartnc` for the campaign context. Nothing below is a counterexample. Every claim below is in the run log with its label and resource wall.

## 1. Where things are

| Item | Location |
|---|---|
| This branch | `claude/jc2-counterexample-hunt-handoff-w369mc`, draft PR #23 against `main`, head ab5c6d9 plus this file |
| Run log | `docs/plans/night/RUN_LOG_2026-09-03.md` (chronological, every verdict labelled) |
| Tools | `docs/plans/night/tools/` (all python3 + python-flint, plus shell drivers) |
| Data | `docs/plans/night/artifacts/` (`sweep_table.md`, `sweep_mono.json`, `certified.json`, `library.json`, queue and decision logs) |
| Previous handoff and plan | branch `claude/jc2-handoff-audit-hartnc`: `docs/plans/HANDOFF_CE_HUNT.md`, `docs/plans/CE_HUNT_PLAN.md`, `docs/plans/groundcover/` |
| Campaign branches | canon `claude/opus-5-counterexample-plan-sep6yk` 24a06fc; mailbox `codex/claude-opus5-mailbox` 156ba7a; p11 `codex/pentagon-p11-zero-search` e4fa5ce; l16 `codex/pentagon-level16-exact` 1e3ac1f |
| Papers as text | regenerate with `pdftotext -layout` from `canon/papers/*.pdf`; GGHV 2022 is `2204.14178.pdf`, Prop 4.1 (case (9,27)) at text line ~228, Prop 4.3 (case (8,28)) at ~492, Cor 5.7 at ~982 |

## 2. Recreating the environment (about 15 minutes)

```
cd /home/user/jacobian_planar
git fetch origin claude/opus-5-counterexample-plan-sep6yk codex/claude-opus5-mailbox codex/pentagon-p11-zero-search codex/pentagon-level16-exact claude/jc2-handoff-audit-hartnc
mkdir -p /tmp/wt
git worktree add /tmp/wt/canon   origin/claude/opus-5-counterexample-plan-sep6yk
git worktree add /tmp/wt/mailbox origin/codex/claude-opus5-mailbox
git worktree add /tmp/wt/p11     origin/codex/pentagon-p11-zero-search
git worktree add /tmp/wt/handoff origin/claude/jc2-handoff-audit-hartnc
export DEBIAN_FRONTEND=noninteractive
apt-get update -q && apt-get install -y -q singular msolve poppler-utils libflint-dev libgmp-dev libmpfr-dev autoconf automake libtool build-essential pari-gp python3-pip
pip install numpy sympy mpmath python-flint
git clone --depth 1 --branch v0.10.1 https://github.com/algebraic-solving/msolve.git /tmp/msolve-src
cd /tmp/msolve-src && ./autogen.sh && ./configure --prefix=/tmp/msolve-0.10.1 && make -j4 && make install
```

The tools hardcode `/tmp/wt/canon/campaign/audit_tracks` (for `trackD_chain_map`, `trackB1_polygon`), `/tmp/wt/fastx`, `/tmp/wt/charts`, `/tmp/msolve-0.10.1/bin/msolve`. Copy `docs/plans/night/tools/*` to `/tmp/wt/fastx/` and `/tmp/wt/charts/` (both; the scripts import each other by those paths) or fix the paths first.

Smoke tests before any run: `canon/wave6/bottomedge/be_c2is0_p1000003.ms` must return `[-1]` in about 1 s; a file with a constant generator equal to a multiple of p must produce a parse error, never `[-1]`; `python3 fastx.py` must print `equal sets: True` against `p11/wave6/ms/p108_525122.ms`; the positive controls `posctl_a`, `posctl_b` in the run log must come back non-empty at jextra 2, 6, 12.

Box facts: 4 cores, 15 GB, no swap, kernel OOM killer active (a process at 9 GB was killed once). Use `ulimit -v` on every solver; a cap makes allocation fail inside the process instead of the kernel killing a neighbour. Do not `pgrep -f` a pattern that occurs in your own command line: it kills your shell (this happened three times).

## 3. What was verified last session, in one screen

- The committed "(9,27) test systems" are mislabelled: `p108_525122` is GGHV Prop 4.3 case (2) (the quadrilateral, orientation (108,72)); `p108_192622` is a compiler stratum (c' = 0) of the (9,27) orientation that Prop 4.1 excludes because the actual case has the (0,18)/(0,27) vertices (c' = 9). The paper's (9,27) polygon has 172 driver parameters in the y-adic extractor and is not buildable here (recursion to row 29 in 172 unknowns). The pentagon (case (1)) is 61 driver parameters and also not buildable in this formulation; its live form is the bilinear monolith `mailbox/wave6/frontier/trackB1_sat_Q.ms` (166 vars, 284 gens, c-block linear given (d, s)).
- The y-adic shape systems are exactly triangular: block 0 is nonlinear in the first row's coefficients; every later block is linear in its three new variables. Intermediate ideals are positive-dimensional and generators reach degree 55, so every one-shot Groebner run walls on this box (msolve segfault at 4 GB, OOM at 9 GB, Singular timeouts) and so did the campaign's. True torus rank is 3 on every built shape and is absorbed by the three Rabinowitsch-forced variables; the register's larger ranks were capped upper bounds.
- The exact eliminator (`lincascade3.py` + `decide_ends.py`) decides these where engines cannot: the (9,27) c' = 0 eps-swap stratum is a one-parameter family at depth 4 (a conic in two coefficients) that dies at depth 6.
- Monomial certificates: a generator of the form (nonzero rational) x (monomial in vertex coefficients and w) kills a polygon pair outright, over Q, in one line. A library-wide sweep found 25 such kills (all certified in exact rational arithmetic), 17 survivors to depth 12, 27 unbuildable under a 3 GB cap. Kills depend only on the reduced polygon pair, so chains sharing polygons share certificates. One kill is a (108,144) stratum, the first exact closure above max degree 125 in the campaign, under the compiler's unverified A'_t and c'-ladder assumptions for chains above 125.
- Undecided after the night: the `p108_192622` stratum at depth 6 (20 of 35 terminal branches empty, 7 walled, branch limit hit) and the paper's case (2) quadrilateral at depth 6 (eliminator timed out at 3000 s; no monomial certificate to depth 24). d=8 chart N: degBound ladder clean through 7, rung 8 out of memory at 6 GB.

## 4. Attempts for the next session, in order

Each has a gate, a command shape, a stop rule and a label. Never report a timeout, OOM, segfault or empty output as a verdict.

### Attempt 1: finish the two undecided (72,108) strata with the eliminator

Why first: the tools exist and the walls are precisely located. Both are strata of the two orientations of the only open sub-125 case.

1. Gate: rerun the smoke tests and the positive controls; rerun `decide_shape.sh s4f232a 65521 6` and confirm it reproduces `BRANCHES Counter({'EMPTY': 1})` in seconds (the -(20/187) c_18^2 w^3 kill).
2. Fix the branch explosion first. Non-monomial pivots currently spawn a constraint branch per pivot with no reduction, which hit the branch limit on `sab7d9e`. Reduce every new constraint and every row entry modulo the current constraint set (a small Groebner basis in the free parameters, recomputed per stage), and drop a pivot-zero branch whenever the pivot reduces to a nonzero constant. This keeps the enumeration complete.
3. Run `sab7d9e` (p108_192622 polygons) and `scb0881` (case (2) quadrilateral) at depth 6 with per-call caps of 6 GB and 1800 s, one at a time, then at p = 1000003 and 1000033.
4. For walled terminal branches, apply the same zero-branching to the terminal constraint system (branch on each free parameter being zero or not; the all-nonzero part is a saturated system in fewer variables).
5. Labels: EMPTY at one prime is replication-grade; three primes is EMPTY-mod-p; then the exact-Q route: the eliminator over Q (`fastx_q.py` shows the recursion is cheap over Q for these sizes) gives a characteristic-free certificate. A NONUNIT terminal branch is ADMISSIBLE-SHAPE (truncated), not a candidate; raise the depth until it dies or stabilises; a family that stabilises across depths 6 to 16 is worth reconstructing as explicit (P, Q) in L^(1) and checking [P,Q] = x^r directly.

### Attempt 2: monomial-certificate sweep on the corrected compiler library, above max degree 125

Why: the cheapest exact kill available, and 24 published cases above 125 are untouched.

1. Gate: put the ground-cover corrections into the compiler first: the cmax key (`cmax = min(int(b0 - (b_t/(a_t - a') - 1) a0), b)`) so the c' ladder is complete, and the (10,40) lower corner (2,0). Check the invariant eps_P + eps_Q = (r + 1, 1) and the six published reduced pairs as a regression.
2. Run `sweep_mono.py` with `MAXPAR=120` and a 6 GB build cap, one shape at a time, depths 2 to 12; certify every kill with `certify_all.py` (exact over Q). Extend `fastx.py`'s coefficient extraction to avoid `monoms()` on the full row polynomial (iterate `terms()` or extract x-coefficients by repeated derivative at x = 0) so the 61-parameter shapes build.
3. Report per published case: which of its compiler strata are killed, which survive, which are unbuildable. A case is closed only when every stratum of its corrected ladder in both eps orientations is killed or decided EMPTY.
4. Stop: 8 hours; label everything.

### Attempt 3: the pentagon by the linear-chain idea in the bilinear formulation

Why: the only branch with no verdict by any method, and the c-block is linear given (d, s) exactly as the y-adic rows are linear in the new row.

1. Gate: reproduce the facts in `handoff/docs/plans/groundcover/artifacts/PENTAGON_STRUCTURE.md` on `trackB1_sat_p1000003.ms`: 283 rows bilinear in c and d, c-rank 51, augmented rank 52, no torus grading.
2. Order the 283 rows by the d-variables they involve and look for a triangular filtration of the d-block analogous to the y-adic rows (the d's are the non-driver polynomial's coefficients; expect rows to add a few d's at a time). If it exists, run the same fraction-free elimination: c's linear, then compatibility rows in d and s, then zero-branching on monomial constraints.
3. If no filtration exists, do the c-elimination on a rank-51 row subset over F_p(d, s) only after substituting the bottom-edge seeds (one per Galois class: two rational, one quadratic, one quintic, from `resid6_P1.out` on the handoff branch), which pins 18 of the (c, d) coordinates and shrinks the function field.
4. Stop: if the compatibility ideal exceeds about 120 variables after elimination, record the size and stop.

### Attempt 4: d=8 chart N rung 8 with the box to itself

1. `docs/plans/night/tools` has no d8 script; the ladder scripts are three lines (run log 05:30Z section). Rung 8 needs more than 6 GB; try 12 GB with nothing else running, 40 minutes.
2. If it walls again, the graded cofactor certificate route (handoff Attempt 2 Route B) and msolve `-g 2` at two compliant primes under a 10 GB cap are the alternatives; a NON-EMPTY there is a lift target with gate H0 (reconstruct P, Q of degrees 16(3d-2), 16(2d-1)) then H1 to H6.

## 5. Guardrails that mattered last session

- Read stderr on every solver call; a verdict is read only from a non-empty output matching a documented pattern (`[1]:` for the unit basis in `-g 2` mode; `[-1]:` in solve mode for EMPTY).
- msolve `-g 2` output uses `c_3^1`-style exponents; strip `^1` before feeding a basis back in.
- Every export through a sanitizer: unused ring variables dropped (they made every solve-mode run positive-dimensional), coefficients in [0, p), no constant generators, round-trip re-parse.
- Gauge only the Rabinowitsch-forced variables to 1 without a chart split; split every other variable into v = 0 and v = 1 charts. Emptiness of all charts over F_p-bar is what msolve's `[1]` decides.
- A monomial generator in nonzero variables is an exact kill; certify it over Q with `fastx_q.py` before quoting it, since a term can vanish mod p and not over Q.
- A NONUNIT on a truncated y-adic system is ADMISSIBLE-SHAPE at most. Raise the depth before drawing any conclusion.
- Positive controls on every new pipeline stage: the two realizable control shapes must stay non-empty at every depth used.
- Push after every result. Set `ulimit -v` on every process. Never `pgrep -f` your own command line.

## 6. Probability, stated once

Unchanged from the previous handoff: of order one in a thousand for a verified plane counterexample from this campaign. The guaranteed product of Attempts 1 and 2 is exact closure or exact NONUNIT structure for the compiler strata of both (72,108) orientations and a per-case stratum table for the 24 untouched published cases above 125. Attempt 3 is the only route to the one branch with no verdict.
