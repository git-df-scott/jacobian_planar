option(redSB);
ring R = 1000003,(a2,b2,mu0,mu3),dp;
ideal I = a2-mu0-mu3, b2-mu0*mu3, mu0^2+mu3^2-1;
ideal E = eliminate(I,a2*b2);
E = std(E);
int i; for (i=1;i<=size(E);i++) { print(E[i]); }
print("SINGULAR_DONE");
quit;
